# 배포 환경변수 예시 (실제 .env 는 커밋 금지 — .gitignore 처리됨)

# 데이터(SQLite DB + 물건 사진·감정요항) 저장 루트.
# 배포 시 영속 디스크(볼륨) 마운트 경로를 지정한다. 미지정 시 ./data.
DATA_DIR=/var/data

# 서버 포트는 플랫폼(Railway/Render)이 $PORT 로 주입한다. Procfile 참조.
# PORT=8000

# 한글 콘솔 출력(선택)
PYTHONIOENCODING=utf-8
