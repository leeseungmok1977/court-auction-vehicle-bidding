# 법원경매 차량 입찰가 산정 시스템 — 통합 설계서 v1.0 (Claude Code 실행용)

- **문서 버전**: v1.0 (v0.1 초안 + v0.2 Power Automate 아키텍처 + v0.3 사전분석 가이드 통합)
- **작성일**: 2026-08-17
- **이 문서의 사용법**: 본 문서 1개를 VS Code 프로젝트 루트에 두고 Claude Code에 전달한다. Claude Code는 **Part C 작업 지시서**의 태스크를 순서대로 수행하여, 검증 코드 작성 → 실행·구조 학습 → 수집 URL 정의서 생성 → Power Automate 패키지(definition.json) 생성 → 테스트까지 완료한다. 사람은 **C.2에 정의된 입력 제공**과 **승인 지점**에서만 개입한다.

---

# Part A. 시스템 설계

## A.1 목적과 범위

대법원 법원경매정보 사이트의 **차량 경매물건**을 조회·수집하고, 감정평가서 PDF·현황 사진·사고유무 정보를 물건별로 저장관리하며, SK엔카·케이카의 동일·동급 매물 시세와 비교하여 **최종 입찰가 산정**을 지원한다.

| 구분 | 내용 |
|---|---|
| 전제 | 모든 대상 사이트에 공식 API가 없다고 가정 |
| 수집 실행 | **Power Automate 클라우드 흐름** (HTTP 액션 기반) |
| 개발 방식 | 흐름 정의(definition.json)를 **VS Code + Claude Code로 작성** → 패키지(.zip) **가져오기**로 배포 |
| 저장소 | SharePoint 문서 라이브러리(파일) + 목록(메타데이터) |
| 제외 | 전자입찰 자동 제출(수동), 상시 서버 운영 |

## A.2 아키텍처

```
사용자 (Teams/메일 알림 · SharePoint 리포트 · 최종입찰가 확정)
        │
Power Automate 클라우드 흐름
  [수집] FLOW-01 물건목록  FLOW-02 상세·첨부  FLOW-03 엔카  FLOW-04 케이카
  [처리] FLOW-05 시세요약·입찰가산정   FLOW-06 알림·리포트
  [공통] FLOW-90 HTTP 호출(지연·재시도·로깅)
        │
SharePoint
  문서 라이브러리: 경매차량자료/{연도}/{사건번호}_{물건번호}/(appraisal|photos|docs|history)
  목록 7종: 물건 / 기일이력 / 시중매물 / 시세요약 / 입찰검토 / 수집로그 / 설정
        │
[개발·배포] VS Code + Claude Code (definition.json 작성, Git 관리)
            → package.zip → Power Automate "가져오기"
```

## A.3 흐름(Flow) 정의

| 흐름 | 트리거 | 역할 |
|---|---|---|
| FLOW-01 물건목록 수집 | 예약(1일 1~2회) | 용도=자동차 검색, 페이지네이션 순회, 신규/변동 감지, FLOW-02 호출 |
| FLOW-02 상세·첨부 수집 | 자식 흐름(물건 단위) | 상세 파싱, 감정평가서 PDF·사진 다운로드 → SharePoint 저장, 목록 갱신 |
| FLOW-03/04 시세 수집 | 예약 + 신규 물건 시 | 엔카/케이카 동급 매물 수집(모델·연식±1년·주행거리±30%) |
| FLOW-05 산정 | FLOW-03/04 완료 후 | 시세 통계(평균/중앙값/최저, 표본수) → 입찰가 산정 → 입찰검토 기록 |
| FLOW-06 알림·리포트 | 기일 D-7/3/1, 신규 발생 | HTML 리포트 조립 → Teams/메일 발송 |
| FLOW-90 공통 HTTP | 자식 흐름 | 3~10초 지연, 재시도, 헤더 관리, 상태코드 분기, 수집로그 기록 |

## A.4 수집 설계 요점

1. **파싱 우선순위**: ① 내부 JSON 요청(Parse JSON) → ② HTML 수식 파싱 → ③ Office Script 위임 → (예비) Power Automate Desktop
2. **세션**: 첫 요청의 `Set-Cookie`를 변수로 받아 후속 요청 헤더에 전달하는 2단계 패턴. 캡처 시점 쿠키를 하드코딩하지 않는다
3. **변경 감지**: 사건번호+물건번호 키로 비교, 최저가·기일 변동은 기일이력에 회차별 적재
4. **사고유무**: 감정평가서 키워드("사고, 판금, 교환, 부식, 침수") 1차 판정 + 카히스토리 수동 조회 결과 병기 (1단계는 수동 판정, AI Builder는 추후)
5. **모델매핑**: 경매 차명 표기 ↔ 엔카/케이카 검색 코드 매핑 테이블(설정 목록). 미매핑 시 알림
6. **준법**: 요청 간 3~10초 랜덤 지연, 약관·robots.txt 준수, 3회 연속 실패 시 중단·알림

## A.5 SharePoint 목록 스키마 (핵심)

| 목록 | 주요 열 |
|---|---|
| 물건 | 사건번호, 물건번호, 법원, 제조사, 모델, 연식, 주행거리, 연료, 변속기, 감정가, 최저매각가, 유찰횟수, 매각기일, 상태, 사고판정(무사고/단순수리/사고/침수의심/미확인), 사고메모, 폴더링크 |
| 기일이력 | 물건ID, 회차, 기일, 최저가, 결과 |
| 시중매물 | 플랫폼, 물건ID, 가격, 연식, 주행거리, 사고표기, URL, 수집일 |
| 시세요약 | 물건ID, 산출일, 표본수, 평균가, 중앙값, 최저가, 신뢰도 |
| 입찰검토 | 물건ID, 권장하한, 권장상한, 최종입찰가, 산정근거(JSON), 결과 |
| 수집로그 | 흐름ID, 실행일시, 대상, 성공여부, 오류메시지 |
| 설정 | 검색조건, 산정파라미터, 모델매핑(경매표기↔엔카코드↔케이카코드) |

## A.6 입찰가 산정 로직 (FLOW-05)

```
기준시세     = 시세요약.중앙값 × 플랫폼 가중 (엔카 1.0 / 케이카 0.95)
입찰 상한가  = 기준시세
              − 예상 수리비(사용자 입력)
              − 사고 감가(단순수리 5% / 사고 10~20%)
              − 리스크 프리미엄(기준시세의 5~10%)
              − 취득 부대비용(취득세 7% 산식 + 이전·탁송 고정비)
              − 목표 절감/마진(10~15%)
권장 범위    = [현재 최저매각가, 입찰 상한가]
자동 판정    : 상한가 < 최저매각가 → "유찰 대기"
              표본 < 5건 → "시세 신뢰도 낮음, 수동 검토"
              침수·전손 키워드 → "입찰 보류"
```

모든 파라미터는 "설정" 목록에서 읽어 흐름 수정 없이 조정 가능하게 한다.

## A.7 라이선스·제약 (선결조건)

| 항목 | 내용 |
|---|---|
| **HTTP 커넥터 = 프리미엄** | Power Automate Premium 라이선스 필요. **전체 설계의 선결조건 — 착수 전 확인** |
| JS 렌더링·캡차 불가 | 클라우드 흐름은 브라우저 렌더링 불가 → 내부 JSON 요청 확보가 최우선. 불가 영역은 Desktop 병행/수동 |
| 실행 한도 | 라이선스별 일일 API 호출 한도 → 호출량 산정 후 주기·범위 조정 |
| 대용량 | HTTP 응답 크기 제한 → 감정평가서(수 MB)는 문제 없음 |

---

# Part B. 사전분석 방법 (사람 수행 구간)

브라우저 개발자도구 Network 탭에서 요청 정보를 **통째로 복사해 Claude Code에 전달**하는 방식을 표준으로 한다. 헤더·파라미터를 개별 분석하지 않는다.

## B.1 복사 3종 세트 (요청 1건당)

```
① F12 → Network 탭 → 로그 지우기
② 대상 동작 1개 수행 (검색/상세/다운로드 클릭)
③ 새로 생긴 요청 클릭 후 복사:
   - 헤더 > 일반        : Ctrl+A 전체 복사
   - 헤더 > 요청 헤더    : "원본 보기" 체크 → Ctrl+A 전체 복사
   - 페이로드           : "소스 보기" → Ctrl+A 전체 복사 (GET이면 생략)
④ C.2 규약에 따라 capture/ 폴더에 파일로 저장
```

- 동적 페이지이므로 URL 주소창은 안 바뀐다. **버튼 클릭 "순간" 생기는 요청**이 대상이며, 응답 탭에 실제 데이터(JSON)가 보이는 것을 고른다.
- 복사한 요청 헤더의 쿠키는 **복사 시점의 세션값**이다. 검증에는 그대로 쓰되, 운영 구현은 A.4-2의 2단계 패턴을 따른다.

## B.2 캡처 체크리스트 (차량 경매 특화)

**법원경매정보** — 목록→상세→첨부의 3단 연쇄 구조

| ID | 캡처 동작 | 확인 목표 | 대응 |
|---|---|---|---|
| L1 | 물건상세검색, **용도=자동차** 검색 클릭 | 목록 URL·페이로드(용도/법원/기간 코드) | FLOW-01 |
| L2 | 목록 **2페이지** 클릭 | 페이지네이션 파라미터 위치 | FLOW-01 |
| L3 | 물건 1건 **상세 진입** | 상세 요청 키(사건번호/내부 ID) | FLOW-02 |
| L4 | **감정평가서 다운로드** 클릭 | PDF URL, 파일 ID, Referer/쿠키 의존 | FLOW-02 |
| L5 | **사진 원본** 표시 | 사진 URL 패턴(키로 조립 가능 여부) | FLOW-02 |
| L6 | **기일내역** 조회 | 회차·기일·최저가 이력 요청 | 기일이력 |

**SK엔카(E) / 케이카(K)**

| ID | 캡처 동작 | 확인 목표 |
|---|---|---|
| E1/K1 | 모델 검색(예: 그랜저) 목록 표시 | 매물 목록 URL, 모델 코드 |
| E2/K2 | **연식·주행거리 필터** 적용 | 필터를 요청 파라미터로 직접 걸 수 있는지 (동급 매칭 핵심) |
| E3/K3 | 목록 2페이지 | 페이지네이션 파라미터 |

---

# Part C. Claude Code 작업 지시서

> Claude Code는 이 Part를 태스크 순서대로 수행한다. 각 태스크는 **완료 기준(DoD)** 을 충족하고 테스트를 통과해야 다음으로 진행한다.

## C.1 리포지토리 구조 (TASK-00에서 생성)

```
auction-vehicle/
├─ CLAUDE.md                  ← 본 문서의 C.4 실행·안전 규칙을 요약 기재
├─ 통합설계서_v1.0.md          ← 본 문서
├─ capture/                   ← [사람 입력] 복사 3종 세트 (git 제외)
│   ├─ L1_general.txt  L1_headers.txt  L1_payload.txt
│   ├─ L2_... ~ L6_...,  E1_... ~ E3_...,  K1_... ~ K3_...
├─ src/
│   ├─ collect/               ← 검증용 수집 코드 (요청별 모듈)
│   ├─ parse/                 ← 응답 파싱·필드 매핑
│   └─ bidcalc/               ← 입찰가 산정 로직 (A.6 산식)
├─ data/                      ← 중간 JSON·PDF·사진 (git 제외)
├─ tests/                     ← pytest 단위 테스트
├─ docs/
│   └─ 수집URL정의서.md        ← TASK-04 산출물
├─ powerautomate/
│   ├─ FLOW-01/definition.json 등 흐름별 정의
│   └─ package/               ← 가져오기용 .zip 구성
└─ .gitignore                 ← capture/, data/, *.har, *.json(중간산출물)
```

## C.2 사람이 제공하는 입력 (파일 규약)

| 입력 | 파일 | 시점 |
|---|---|---|
| 복사 3종 세트 | `capture/{ID}_general.txt`, `{ID}_headers.txt`, `{ID}_payload.txt` (GET은 payload 생략) | TASK-02 전 |
| HTTP 프리미엄 라이선스 확인 결과 | 사용자 구두 확인 | TASK-00 |
| SharePoint 사이트 URL·라이브러리/목록 이름 | `capture/sharepoint.txt` | TASK-06 전 |
| 골격 흐름 내보내기 패키지(스키마 참조용) | `powerautomate/sample_export.zip` — 웹 디자이너에서 임의 흐름 1개 내보내기 | TASK-06 전 |

capture 파일이 없는 ID는 해당 태스크를 **보류하고 사람에게 요청**한다. 추측으로 URL·파라미터를 만들어내지 않는다.

## C.3 태스크 정의

**TASK-00. 프로젝트 초기화**
- C.1 구조 생성, .gitignore 작성, CLAUDE.md에 C.4 규칙 기재, README에 진행 현황 표 생성
- DoD: 구조 생성 완료, 사람에게 "capture/ 입력과 라이선스 확인" 요청 메시지 출력

**TASK-01. 산정 로직 선행 구현 (캡처 불필요 — 즉시 가능)**
- `src/bidcalc/`에 A.6 산식 구현. 파라미터는 `config.yaml`로 외부화
- `tests/test_bidcalc.py`: ①정상 산정 ②상한가<최저가→유찰대기 ③표본<5→신뢰도낮음 ④침수키워드→보류 ⑤경계값(감가 0%, 유찰 0회)
- DoD: pytest 전체 통과

**TASK-02. 요청 재현 검증 코드 (캡처 ID별 반복)**
- capture/{ID} 3종을 읽어 동일 요청을 보내는 모듈 작성 (`src/collect/{id}.py`)
- 응답을 `data/응답_{ID}.json`(또는 바이너리)으로 저장. 요청 전 5초 대기, **1회 호출**
- DoD: 각 ID 실행 성공, 응답 파일 생성. 실패 시 상태코드·응답 요약을 보고하고 사람에게 재캡처 요청

**TASK-03. 수집 파이프라인 검증 (소량)**
- L1+L2 기반 목록 순회(최대 **3페이지**), L3~L5 기반 상세·첨부 다운로드(최대 **물건 2건**)
- 물건별 폴더 `data/{사건번호}_{물건번호}/(detail.json, appraisal/, photos/)` 생성
- 요청 간 5~10초 랜덤 대기. 세션 쿠키는 L1 응답에서 획득해 재사용(하드코딩 금지)
- `tests/test_parse.py`: 저장된 실제 응답 JSON을 픽스처로 필드 추출 함수 테스트 (사건번호, 차명, 연식, 주행거리, 감정가, 최저가, 매각기일, 유찰횟수)
- DoD: 물건 2건 폴더 완성(PDF 열림 확인 포함), 파싱 테스트 통과

**TASK-04. 수집 URL 정의서 생성**
- `data/`의 실제 응답을 근거로 `docs/수집URL정의서.md` 작성. 요청별: URL, 메서드, 페이로드 파라미터(의미·가변부 표시), 필수 헤더(쿠키/Referer 여부), 응답 형식, **응답 필드명 ↔ A.5 물건 목록 열 매핑 표**
- DoD: 캡처된 모든 ID 수록, 가변 파라미터(페이지·기간·모델코드)에 치환 표시

**TASK-05. 시세 매칭·통계 검증**
- E/K 응답으로 동급 필터(연식±1년, 주행거리±30%) 및 통계(평균/중앙값/최저/표본수) 구현, TASK-01 산정 로직과 연결한 엔드투엔드 계산 1건 시연
- `tests/test_match.py`: 매칭 경계값(±1년 경계, 표본 0건) 테스트
- DoD: 실제 물건 1건에 대한 "권장 입찰가 범위" 산출 리포트(`docs/샘플리포트.md`) 생성

**TASK-06. Power Automate 패키지 생성**
- `sample_export.zip`을 해부해 definition.json 스키마·manifest 구조 파악(추측 금지)
- 정의서 기반으로 FLOW-90 → FLOW-01 → FLOW-02 순서로 definition.json 작성:
  HTTP 액션(URI/Method/Headers/Body), Parse JSON 스키마(실제 응답에서 생성), Do until 페이지네이션, Set-Cookie 2단계 패턴, 재시도 정책·timeout 명시, 액션명은 한글 기능명(`HTTP_L1_물건목록조회`)
- SharePoint 액션의 사이트·목록명은 `capture/sharepoint.txt` 값 사용, 환경 의존값은 상단 Compose "설정" 액션으로 모아 하드코딩 최소화
- 가져오기용 package.zip 구성 + `docs/배포가이드.md`(가져오기 절차, 연결 매핑, 업데이트 배포)
- DoD: JSON 스키마 유효성 검사 통과(json.load + 필수 키 검증 스크립트), 배포가이드 완성

**TASK-07. 흐름 테스트 지원**
- 사람이 Power Automate에서 가져오기 후 수동 실행 → 실패 시 실행 기록(오류 액션·메시지)을 붙여넣으면 definition.json 수정 → 재패키징 반복
- `docs/흐름테스트체크리스트.md` 생성: ①가져오기 성공 ②연결 매핑 ③FLOW-90 단독 실행 ④FLOW-01 3페이지 제한 실행 ⑤FLOW-02 물건 1건 실행(PDF SharePoint 저장 확인) ⑥수집로그 기록 확인 ⑦지연시간 동작 확인
- DoD: 체크리스트 전 항목 통과 확인을 사람이 회신

**TASK-08. 마무리**
- README 갱신(실행법·운영 주기·파라미터 조정법), 미결사항 목록 갱신, capture/·data/ 민감 파일 삭제 안내
- DoD: 문서 3종(정의서·배포가이드·체크리스트) 최신화

## C.4 실행·안전 규칙 (CLAUDE.md에 기재, 항상 준수)

1. **소량 원칙**: 검증 실행은 최대 3페이지·물건 2건. 전체 수집을 파이썬으로 실행하지 않는다 (실운영은 Power Automate)
2. **지연 필수**: 모든 외부 요청 전 5~10초 대기. 재시도는 최대 2회, 간격 30초
3. **추측 금지**: capture 파일에 없는 URL·파라미터·스키마를 임의 생성하지 않는다. 없으면 사람에게 요청
4. **비밀정보**: capture/의 쿠키·헤더와 data/ 산출물은 git 커밋 금지(.gitignore), 코드에 세션값 하드코딩 금지
5. **중단 조건**: 403/429 응답, CAPTCHA 감지, 비정상 응답 3회 연속 시 즉시 중단하고 보고
6. **승인 지점**: 외부 요청이 발생하는 최초 실행(TASK-02, 03, 05)은 실행 전 사람에게 대상·건수·예상 요청 수를 보고하고 승인 후 실행

## C.5 테스트 요약

| 레벨 | 대상 | 방법 |
|---|---|---|
| 단위 | 산정 로직, 파싱·필드 매핑, 동급 매칭 | pytest (실제 응답 JSON을 픽스처로) |
| 통합(로컬) | 목록→상세→첨부 파이프라인 | 소량 실수집 (3페이지·2건) |
| 정의 검증 | definition.json | JSON 유효성 + 필수 키 검증 스크립트 |
| 흐름(운영) | FLOW-90/01/02 | C.3 TASK-07 체크리스트 (사람 실행, Claude Code 수정 지원) |

---

# Part D. 진행 순서·검수

```
[사람]  라이선스 확인 → L1~L6 / E1~E3 / K1~K3 캡처 → capture/ 저장
[CC ]  TASK-00~01 (즉시) → 02~05 (캡처 도착분부터, 승인 후 실행)
[CC ]  TASK-04 정의서 → TASK-06 패키지 생성
[사람]  Power Automate 가져오기 → TASK-07 체크리스트 실행 → 실패 로그 회신
[CC ]  수정·재패키징 → 통과 시 TASK-08 마무리
```

**최종 검수 기준**: ① 물건 2건의 폴더(PDF·사진 포함)가 SharePoint에 흐름 실행으로 생성됨 ② 그중 1건에 대해 시세 표본·권장 입찰가 범위가 입찰검토 목록에 기록됨 ③ 수집로그에 성공 기록 존재 ④ 문서 3종(수집URL정의서·배포가이드·흐름테스트체크리스트) 완비

## 미결정 사항

- [ ] HTTP 프리미엄 라이선스 보유 여부 (TASK-00 선결)
- [ ] 사용 목적(자가사용 vs 재판매) → 마진 파라미터
- [ ] 대상 차종·가격대 (검색조건 초기값)
- [ ] 캡처 결과에 따른 사이트별 수집 가능 범위 (클라우드 단독 / Desktop 병행 / 수동)
- [ ] AI Builder 사용 여부 (감정평가서 자동 판정)
