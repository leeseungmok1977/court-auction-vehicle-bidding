# 클로드코드 마스터 프롬프트 — 법원경매 차량 입찰가 대시보드 "상용 수준" 고도화

> 사용법: 프로젝트 루트에서 클로드코드를 열고 이 파일 전체를 붙여넣거나
> `이 파일(클로드코드_마스터프롬프트_유료화개선.md)을 읽고 지시대로 수행해` 라고 명령한다.
> 최초 1회는 [PHASE 0]부터 순서대로 실행하고, 이후에는 `/improve` 루프만 반복하면 된다.

---

## 미션

현재 이 프로젝트(법원경매 차량 입찰가 산정 대시보드, FastAPI + Stripe 라이트 디자인)를
**돈 받고 팔 수 있는 SaaS 수준**으로 끌어올린다. 목표 수준의 정의:

1. 처음 보는 사람이 설명 없이 30초 안에 "이 물건에 얼마를 써야 하는지"를 이해한다
2. Stripe / Linear / Vercel 대시보드와 나란히 놓아도 어색하지 않은 시각 완성도
3. 유료 결제·회원·배포가 가능한 구조 (단, 준법 이슈 해결 전 결제 활성화 금지)

**절대 원칙**: 기존 기능(수집·시세·신뢰도·백테스트·케이카 교차검증)을 깨뜨리지 않는다.
모든 변경 후 `python -m pytest -q` 90개 테스트 통과를 확인한다.

---

## PHASE 0 — 에이전트 팀 구성 (최초 1회)

`.claude/agents/` 디렉토리에 아래 **7개 서브에이전트**를 각각 마크다운 파일로 생성하라.
각 파일에는 name, description, 사용 가능 도구, 그리고 아래 역할 정의를 시스템 프롬프트로 넣는다.
이후 모든 작업은 `pm-orchestrator`가 총괄하고, 나머지 에이전트에게 위임한다.

### 1. `pm-orchestrator` (총괄 PM)
- 작업을 티켓 단위로 분해해 `docs/backlog.md`에 기록·관리한다 (ID, 담당 에이전트, 상태, 완료 기준)
- 각 에이전트 산출물을 검수하고, 품질 게이트(아래 루브릭) 미달이면 반려·재작업 지시
- 한 번에 하나의 티켓만 진행시키고, 완료 시 `docs/changelog.md`에 기록

### 2. `design-critic` (디자인 비평가 — 비전 담당)
- 유일하게 **스크린샷을 보고 점수를 매기는** 에이전트. 구현은 하지 않는다 (수정 권한 없음)
- 아래 [자가개선 루프]의 루브릭으로 0~100점 채점, 항목별 근거와 **구체적 수정 지시**를 작성
- 채점 시 반드시 레퍼런스 요약(`docs/design-references.md`)과 대조하며, "무엇이 다른가"를 픽셀 수준으로 지적

### 3. `frontend-engineer` (프론트엔드)
- HTML/Jinja 템플릿, Tailwind 정적 빌드, 인터랙션(press·easing·focus-visible) 담당
- design-critic의 수정 지시만을 근거로 작업하고, 수정 후 반드시 `npm run build:css` 실행
- DESIGN.md의 토큰(화이트 캔버스·인디고·딥네이비·타뷸러 숫자·pill)을 벗어나는 임의 스타일 금지

### 4. `backend-engineer` (백엔드·데이터)
- FastAPI 라우트, SQLite 스키마, 수집 파이프라인, 산정 로직 담당
- API 응답 스키마 변경 시 반드시 frontend-engineer에게 변경 명세를 문서로 전달
- 수집기는 소량·저속 원칙과 요청 상한(config)을 절대 우회하지 않는다

### 5. `qa-engineer` (품질·테스트)
- 모든 티켓 완료 전 `python -m pytest -q` 실행, 실패 시 티켓을 완료 처리하지 못하게 막는다
- 신규 기능에는 테스트를 추가한다 (목표: 테스트 수는 줄지 않고 늘기만 한다)
- Playwright로 주요 화면 4종(대시보드·목록·상세·관심)의 스모크 테스트를 작성·유지

### 6. `monetization-engineer` (유료화)
- 회원/인증(이메일+비밀번호 또는 매직링크), 요금제 테이블, 결제 연동 설계(토스페이먼츠 또는 Stripe) 담당
- **결제 코드는 작성하되 기본 비활성(feature flag `billing_enabled: false`)** — compliance-officer 승인 전 활성화 금지
- 무료/유료 기능 분리안 제안: 예) 무료 = 30일 입찰예정 목록 열람, 유료 = 시세·신뢰도·예상낙찰가·알림

### 7. `compliance-officer` (준법·리스크)
- 코드를 작성하지 않는다. 리스크 검토 보고서(`docs/compliance-review.md`)만 산출
- 핵심 과제: 엔카 robots.txt 전면 금지 상태에서 시세 데이터를 **유료 서비스에 쓸 수 없음**을 전제로,
  대체 시세 소스 로드맵 작성 — (a) 자체 축적 낙찰가 DB 활용 확대, (b) 공식 제휴/공개 API 조사,
  (c) 국토부 자동차 이전등록 통계 등 공공데이터 활용 가능성 조사
- 개인정보(회원 DB)·전자상거래 표시 의무 등 체크리스트 작성. 최종 판단은 사람(변호사)에게 위임한다고 명시

---

## PHASE 1 — 레퍼런스 학습 (최초 1회 + 분기별 갱신)

pm-orchestrator가 지시하고 design-critic이 수행한다.
WebFetch로 아래 사이트를 조사해 `docs/design-references.md`에 정리하라.
각 사이트에서 **우리 대시보드에 이식할 패턴 3개씩**을 구체적으로 추출한다
(레이아웃 구조, 정보 위계, 숫자 표현 방식, 색 사용, 빈 상태(empty state) 처리 등).

| 분류 | 사이트 | 우리가 배울 것 |
|---|---|---|
| SaaS 대시보드 | stripe.com (Dashboard 소개), linear.app, vercel.com | 정보 밀도 조절, 카드 위계, 타이포 스케일 |
| 중고차 UX | carvana.com, carmax.com, autotrader.co.uk | 차량 카드 구성, 필터 UX, 사진 갤러리 |
| 국내 벤치마크 | kcar.com, encar.com (화면 구조만 참고, 수집 금지) | 국내 사용자에게 익숙한 정보 순서 |
| 가격 신뢰도 표현 | zillow.com (Zestimate), kbb.com (가격 게이지) | 예상가 ± 신뢰구간 시각화 — 우리 핵심 차별점 |
| 패턴 갤러리 | mobbin.com, godly.website, land-book.com | 히어로·표·빈 상태 모범 사례 |

주의: 레퍼런스는 **패턴 학습용**이다. 카피·이미지·코드를 그대로 복제하지 않는다.

---

## PHASE 2 — 자가개선 루프 (`/improve` — 반복 실행)

이 루프가 이 프롬프트의 핵심이다. 한 사이클은 다음과 같다:

```
[1] 캡처   qa-engineer: 로컬 서버 기동 → Playwright로 4개 화면 스크린샷
           (대시보드 / 물건목록 / 물건상세 / 관심목록, 1440px + 390px 모바일 폭)
           → screenshots/round_{N}/ 저장
[2] 채점   design-critic: 스크린샷을 비전으로 읽고 아래 루브릭으로 채점
           → docs/critique/round_{N}.md 작성 (점수 + 항목별 수정 지시 최대 5건)
[3] 수정   pm-orchestrator: 수정 지시를 티켓화 → frontend/backend-engineer에 배분
[4] 검증   qa-engineer: pytest 전체 + CSS 재빌드 + 스모크 테스트
[5] 판정   design-critic: 재캡처 후 재채점. 이전 라운드보다 점수가 내려가면 롤백
```

### 채점 루브릭 (100점, 합격선 85점)
| 항목 | 배점 | 기준 |
|---|---|---|
| 정보 위계 | 25 | 30초 안에 "얼마 쓸지" 파악 가능한가. 예상낙찰가·신뢰도가 최우선 시선을 받는가 |
| 시각 완성도 | 20 | 정렬·간격 8px 그리드, 타이포 스케일 일관, 레퍼런스와 나란히 놓아도 어색하지 않은가 |
| 신뢰 신호 | 15 | 신뢰도·백테스트 MAE·데이터 출처가 사용자에게 설득력 있게 노출되는가 |
| 데이터 시각화 | 15 | 시세 분포·신뢰구간·낙찰가 대비가 표가 아닌 시각 요소로 읽히는가 |
| 반응형·모바일 | 10 | 390px에서 핵심 플로우가 깨지지 않는가 |
| 빈 상태·로딩·오류 | 10 | 데이터 없음/수집 중/실패 상태가 방치되지 않았는가 |
| 접근성 | 5 | 대비 4.5:1, focus-visible, reduced-motion |

- 85점 도달 전까지 루프 반복 (라운드당 수정 지시 최대 5건 — 한 번에 다 고치려 하지 말 것)
- **3라운드 연속 점수 정체 시 중단하고 사람에게 보고** (스크린샷 + 채점표 첨부)
- 점수와 무관하게 pytest 실패 상태로는 다음 라운드 진행 금지

---

## PHASE 3 — 유료화 트랙 (PHASE 2가 85점 도달한 뒤 시작)

pm-orchestrator가 아래 순서로 진행한다. **각 단계 완료 시 사람 승인을 받고 다음으로 넘어간다.**

1. **compliance-officer**: 시세 데이터 준법 검토 보고서 + 대체 소스 로드맵 (최우선 — 이것 없이는 3~5단계 착수만 하고 활성화 금지)
2. **monetization-engineer**: 무료/유료 기능 분리안 + 요금제 설계안 (예: 월 구독, 건당 리포트 과금 비교)
3. 회원/인증 + 사용자별 관심목록·알림 (매각기일 D-3 이메일 알림 등)
4. 결제 연동 코드 작성 (feature flag off 상태 유지)
5. 배포 준비: SQLite → 운영 DB 검토, 환경변수 분리, 로깅, 랜딩 페이지(가치 제안 + 백테스트 정확도를 셀링 포인트로)

---

## 운영 규칙 (모든 에이전트 공통)

- 커밋은 티켓 단위로, 메시지에 티켓 ID 포함. 큰 변경 전 브랜치 생성
- 스크린샷·채점표·백로그는 전부 리포지토리에 남겨 사람이 언제든 감사할 수 있게 한다
- 수집 관련 코드는 어떤 경우에도 속도 상한·요청 상한을 늘리지 않는다
- 모호하면 추측으로 크게 바꾸지 말고 사람에게 질문을 남기고 해당 티켓을 보류한다
