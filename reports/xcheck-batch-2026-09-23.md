# 교차검수 — 템플릿 8건 배치 (2026-09-23)

- 지시서 25 `app-design-expert` / 지시서 26 `design-critic` — **서로의 결론을 모른 채** 진행
- ⚠ 둘 다 파일 쓰기가 제약에 걸려 본문으로 회신했다. 이 문서는 Steward 가 두 보고를 합쳐 기록했고,
  [Steward 재현 검증]은 **내가 직접 센 결과**다(ORG §2.3).
- 대상 커밋: `8829498`

## [결론] 두 자리 모두 **조건부 — ④ 라벨 이관 전에는 배포하지 않는다**
나머지 7건(①②③⑤⑥⑦⑧)은 방향이 맞다. ④만 단독으로 나가면 **지금보다 나쁘다.**

## ★ ④ — 색은 톤, 글자는 judgment. 한 칩 안에서 서로 반대를 말한다

`detail.html:45` 는 색을 `bidst.tone`(→`_TONE_CLS`), 글자를 `v.judgment`(→`_display_judgment`)에서 가져온다.
`service.py:3869`·`3874` 가 `out("blocked", "이번 회차 입찰 부적합", "stop")` 을 내는 동안 `v.judgment` 는
'유찰 대기' 가 통과한다 → **로즈 칩에 "유찰 대기".**

**`app-design-expert` 가 라이브에서 실물 재현**(`2026타경30644_1`):

| 화면 | 같은 물건이 하는 말 |
|---|---|
| 목록 카드 칩 | **이번 회차 입찰 부적합** |
| 상세 상단 칩 | **유찰 대기** |
| 상세 히어로 | **"이번 회차는 입찰하지 마세요"** |

> 사용자는 **색을 먼저 믿는다**(전주의적 처리). 색은 "멈춰", 글자는 "기다리면 된다" —
> 고관여 금전 판단에서 **자기를 스스로 취소하는 칩**이다. (`app-design-expert`)

> 전에는 칩이 자기완결적이었고(유찰 대기→앰버) 불일치를 알려면 **두 화면을 오가야** 했다.
> 지금은 불일치가 **한 칩 안, 2cm 안**에 있다. 오독 경로가 짧아졌다. (`design-critic`)

**판정: 되돌리지 않는다 — 라벨을 `bidst.label` 로 이관한다.** 근거가 저장소 안에 있다.
- `detail.html:39` 주석이 이미 *"판정은 bid_state 가 **단일 진실원천**"* 이라 선언했고,
  **바로 윗줄 `:44`(nomarket)는 이미 `bidst.label` 을 쓴다.** `:45` 만 예외인 자기모순이다.
- `app.py:659-662` 가 목록이 같은 길을 간 이유를 적어 뒀다 — *"상세에서 '이번 회차 입찰 부적합'인 차가
  목록에서는 앰버 '유찰 대기'로 보였다(4회차 디자인·경매 P0)."* **지금은 그 문장의 좌우가 뒤바뀐 상태다.**
- 되돌리면 09-23에 고친 "목록 회색 ↔ 상세 앰버"가 되살아난다.
- ⚠ 단, `bidst.label` 은 길다 → `:318` 이 이미 쓰는 **`ui.judge_label()`** 을 함께 적용해야 한다.

## 두 자리가 새로 짚은 것 — Steward 가 전부 재현했다

| # | 지적 | Steward 확인 |
|---|---|---|
| ② | `detail.html:307` 이 톤 체계를 **뒤집어** 쓴다 — `wait→text-amber-300`, `caution→text-sky-300` | **확인.** `_TONE_CLS` 는 wait→slate, caution→amber. 판정 배너 아이콘과 바로 아래 히어로 칩이 같은 물건에 반대 색 |
| ③ | `vehicles.html:256-260` 행 좌측 악센트가 아직 judgment **문자열** 기반 | **확인.** `border-amber-500 if v.judgment=='유찰 대기'` 등. 한 행에서 칩은 slate, 악센트는 앰버. 레거시 **네 번째 자리** |
| ④ | `report.html:1073` 제목이 `모델 검증 실적` — 지난 배치가 **두 줄 위**를 놓쳤다 | **확인.** `:1075` 부제만 고쳤다. `report.html` 에 `모델` 3회(`:1073`·`:1083`·`:1401`). 홈은 `dashboard.html:425` `실측 검증` |
| ⑤ | `detail.html:344` `text-white/40` ≈**3.9:1** (12px, AA 미달) — 주 경로 | **확인.** `:344` 에 `/55`·`/40`, `:350` 에 `/50`. 같은 문장이 분기마다 다른 불투명도 |
| ⑥ | 모바일 고지가 "한 줄" 약속을 어긴다 | **확인, 그리고 보고보다 나쁘다.** `:372` 주석이 *"한 줄로 유지한다"* 라고 적혀 있는데 `:373` 소스는 **110자**(표시 ~78자). 큰글씨에서 4~5줄 ≈96~120px — 주석이 기록한 **바로 그 110px 급** |

## 두 자리가 스스로 밝힌 한계 — 이게 다음 회차의 숙제다
- **배치 이후 캡처가 0장**이다. 두 자리 모두 코드·CSS 계산으로만 판정했다.
  CLAUDE.md 검수 순서 **2번(눈으로 확인)이 이 배치에서 수행되지 않았다.**
- `design-critic` 은 셸이 없어 `git diff` 를 못 돌렸다 — "무엇이 바뀌었나"를 지시서와 주석에 의존했다.
- `/watchlist` 는 QA 스모크 8개 페이지에 **없다**(커버리지 0).

## PANEL-15 영향 범위 재판단 (`design-critic`)
폰 프레임 사실(`base.html:6` `not is_admin` → `frame.html:20` 420px iframe)을 반영하면,
데스크톱 표(`hidden lg:block`, lg=1024px)는 **420px iframe 에서 절대 렌더되지 않는다.**
→ PANEL-15 의 노출 대상은 **관리자 + 데스크톱 PWA 설치자**뿐. 우선순위를 '출시 블로커'에서 내린다.
**★ 다만 닫지는 않는다** — `frame.html:39` 가 *"PC에 **설치**하면 넓은 데스크톱 화면으로 열립니다"* 라고
**설치를 권유한다.** 노출 대상이 0명인 게 아니라 **우리가 늘리고 있는 집단**이다. 예리한 지적이다.

## 두 자리가 함께 인정한 잘된 점
1. **③ 표 칩 sky 이관이 정확히 권고대로 됐다** — 표 칩·상세 배지·범례가 sky-50/700/200 한 세트로 수렴.
   대비 실측 5.93:1 이 주석의 "5.9:1" 과 일치. 카드 칩만 다른 값인 것도 근거(`:429-435`)가 남아 있어
   다음 사람이 "통일 안 됐다"고 되돌리는 사고를 막는다.
2. **⑥ 의 "행마다 붙이지 않는다"가 `service.py:4119` 독스트링과 정확히 일치한다** —
   정확도 전이 금지를 UI 층에서 지킨 드문 사례.
3. **⑦⑧ 이 문구 대신 폭으로 푼 판단** — 드리프트 가드가 칩 전문을 물고 있다는 제약을 읽고
   `min-w-[108px]`·`flex-wrap` 으로 해결하고, 이유를 주석에 남겨 다음 사람이 문구를 줄이는 실수를 막았다.
4. **담당이 ④의 절반 미완을 스스로 신고했다.** 즐겨찾기에 `bidst` 가 없는데 밀어붙였다면
   `_tcls(None)` 이 기본값 wait 를 반환해 **침수·전손(stop)까지 전부 회색**이 될 뻔했다.
