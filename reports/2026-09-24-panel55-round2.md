# PANEL-55 2회차 + PANEL-56 — 검수 지적 3건 반영 (지시서 2026-09-24-09)

- 담당: frontend-engineer · 입력: `reports/2026-09-24-panel55-design-review.md`(지적 1·2·3 + Steward 재현 표)
- 기준 커밋: 작업 시작 `d10d290` → 도중 `b6990a1`·`1f8f33c`·`4ed11ab`·`d019cc4` 가 들어옴(docs·`tools/store_overlay.py` 뿐 —
  `git diff --stat b6990a1 d019cc4 -- web/templates/report.html tests/` **빈 출력**, 내 대상 파일 무관). 캡처 `HEAD` = `d019cc4`.
- 손댄 파일(4): `web/templates/report.html`(2블록) · `tests/test_panel55_report_nomarket_copy.py` · `tests/test_report_sections.py`(앵커 1줄) ·
  `tests/test_panel56_masthead_badge.py`(신규 13건)
- 손대지 않은 것: `detail.html` · `market_match.py` · `service.py` 판정 로직 · `.note`/`.sec-empty` CSS 규칙 · `app.css`(md5 `bcfe1da6` 전후 동일) · 운영 DB
- ⚠ 작업트리에 **다른 담당의 진행 중 파일**이 있다: `tools/store_overlay.py`(M) · `tests/test_store_overlay.py`(??). 내 것이 아니다 — `git add` 때 위 4개만.
- **커밋하지 않았다.**

## 1. 바꾼 것 (앵커·함수명으로)

### ① PANEL-56 — 마스트헤드 라벨 배지를 점수 배지와 같은 게이트 안으로
위치: `report.html` `<!-- 마스트헤드 -->` 의 `<div class="badges">`.
- 전: `{% if conf %}<div class="badge fill">시세 신뢰도 …</div>{% endif %}` 다음 줄에 게이트 없는 `<div class="badge">{{ v.market_confidence_label or '—' }}</div>`
- 후: 라벨 배지를 `{% if conf %}` 안으로 옮기고 `{% elif bidst %}<div class="badge">{{ ui.judge_label(bidst.label) }}</div>{% endif %}`.
  `bidst` 없으면 아무 배지도 안 그린다. 색·클래스는 `.badge` 그대로(의미색 없음). 접기는 `detail.html` h1 칩과 같은 `ui.judge_label`(' — ' 뒤에서만).
- 주석은 `{% if conf %}` 와 **같은 줄에서 끝나게** 넣었다 — 줄을 더하면 시세 있는 물건의 렌더 바이트가 바뀐다(아래 md5 테스트가 감시).
- 본문 02 근거 점검의 `{{ v.market_confidence_label or '—' }}` 는 손대지 않았다(`{% if report %}` 안).

### ② 지적 2 — 안내 그릇을 각주(`.note`)에서 `.sec-empty` 로
위치: `{% if report %}` 의 `{% else %}` 가지, 앵커 `산정되지 않아 종합 리포트를`.
- 전: `<section class="section"><div class="card"><p class="note" style="margin:0">…</p></div></section>`
- 후: `<section class="section"><div class="sec-empty">…</div></section>` — `.card` 를 벗겼다(빈 상태를 카드 안에 넣으면 프레임 두 겹, `test_empty_sections_are_not_double_framed` 와 같은 규칙). 첫 문장(사실)만 `<b style="color:var(--ink)">`. 관리자 분기도 같은 그릇(문구만 다름). 새 CSS 없음.
- 실측(390 공개): `.sec-empty` 12.5px · 본문 `rgb(75,90,115)`(=`--ink-soft`) · 첫 문장 `rgb(22,35,58)`(=`--ink`) · 박스 350×104(전: `.note` 316×55).

### ③ 지적 3 — 공개 문구 `아직` 삭제 + '물건 상세' 링크
- 공개: `시세가 산정되지 않아 종합 리포트를 만들 수 없습니다. 감정가·최저매각가 등 법원 자료는 [물건 상세]에서 그대로 확인하실 수 있습니다.`
- 관리자: `시세가 아직 산정되지 않아 … 물건 상세에서 [다시 분석] 후 다시 시도하세요.` — **`아직` 유지.**
- 링크: `<a href="/vehicle/{{ v.id }}" onclick="return _reportBack(event)" style="color:var(--ink);text-decoration:underline">물건 상세</a>` — sticky 바 `상세로` 와 **같은 핸들러**(페이지 내 `_reportBack` 사용처 정확히 2곳: 버튼+링크, 테스트가 센다). 리포트에 본문 링크 색 규칙이 없어(`.rtoc a`·`.secnav a` 뿐) 지시대로 잉크색+밑줄 인라인.
- 테스트 앵커: `test_report_sections.py::test_unpriced_report_drops_the_tabs_instead_of_dangling_them` 과 `test_panel55…::_ANCHOR` 를 `시세가 아직 산정되지 않아 …` → **`산정되지 않아 종합 리포트를`**(두 분기 공통 조각). `_TIMING_WORDS` 에 `아직` 추가. `_note()`/원문 검사는 `<p>…</p>` → `<div class="sec-empty">…</div>` 로.

## 2. 테스트 — 전/후 · 반증

| | 결과 | 조건 |
|---|---|---|
| 전 | **1226 passed** | `git worktree` 분리 체크아웃(HEAD `b6990a1`), 내 편집과 격리 |
| 후 | **1241 passed** (= 1226 + 신규 15) | 주 작업트리, 편집 완료 후. ⚠ 다른 담당의 `tests/test_store_overlay.py` 는 실행 **뒤에** 나타났다 — 포함 여부 미검증 |

신규 15 = `test_panel56_masthead_badge.py` 13 + `test_panel55…` 2(`test_public_copy_links_detail_with_the_same_back_handler`, `test_copy_is_body_text_not_a_footnote`).
지시서 검증 항목 대응: ⑴ `test_unpriced_masthead_never_shows_a_bare_low_badge`(공개·관리자 × closed·lowconf) + `test_unpriced_masthead_says_what_detail_says`(`service.bid_state` 라벨과 대조) + `test_closed_vehicle_masthead_reads_closed_not_low` ·
⑵ `test_priced_masthead_unchanged_byte_for_byte` — 시세 있는 픽스처(priced_1) 마스트헤드(`<!-- 마스트헤드 -->`~`</header>`, 발행 시각만 `<NOW>` 치환) md5 **`e87fa43a022c6aa8db63d822988fda14`** 를 변경 **전** 템플릿으로 재고, 변경 후 같은 값(공개·관리자 동일) ·
⑶ `test_public_copy_promises_no_timing`(`아직` 금지어) · ⑷ `test_public_copy_links_detail_with_the_same_back_handler`.
구조 감시: `test_template_gates_label_badge_with_score_badge`(라벨 배지가 `{% if conf %}` 세그먼트 안, `{% elif bidst %}` 존재, `{% else %}` 없음).

**반증(확인된 사실, 바이트 백업 → 복원 → md5 대조):**
- (a) 라벨 배지를 게이트 밖 원래 자리로 되돌림 → `test_panel56…` **9 failed / 4 passed**(never_shows 4·says_what 2·closed 1·folds 1·template 1). 복원 후 md5 `785a9f44`.
- (b) 공개 문구에 `아직` 되돌림 → `test_panel55…` **1 failed / 8 passed** — 실패는 `test_public_copy_promises_no_timing` 하나. 복원 후 md5 `785a9f44`.
- 복원은 편집본 바이트 사본(`cp`)으로 했다 — CRLF 1658 / bare LF 0 유지 확인. (⚠ 처음 `grep -c $'\r$'` 로 잰 줄끝 판정은 모든 파일에서 줄 수와 같게 나와 **가짜**였다. 실제: `report.html`·`test_report_sections.py` CRLF, `test_panel55…py` LF — 편집 스크립트가 파일별 줄끝을 따르게 고쳤다.)

## 3. 캡처 — `screenshots/panel55b/` (기준 커밋 `HEAD` 파일 = `d019cc4`, before 3장은 `b6990a1` 시점 — report.html 동일)

조건: 로컬 uvicorn `127.0.0.1:8794` · `DATA_DIR`=스크래치 DB 사본 · `NC_NO_SCHEDULER=1`·`NC_NO_BACKGROUND=1` · 공개 = `X-Forwarded-For: 203.0.113.9`(`/admin` 의 "SSH 터널로만" 문구로 자가검증) · 관리자 = 헤더 없음 · Chromium DPR2 · 844 높이. 34파일, md5 전부 상이.

| 파일 | md5 | 배지 텍스트(w×h, 잘림) | 문구 박스 |
|---|---|---|---|
| `before-public-repro-390` | `032f8a72` | **`낮음`**(전) | `.note` |
| `before-public-upcoming-390` | `310b38b6` | **`낮음`**(전) | `.note` |
| `before-priced-390` | `291e8562` | `시세 신뢰도 81/100 …` + `높음` | — |
| `public-repro-390/360/320` | `ccdfaa93`/`476674fe`/`c154310c` | `매각 종료` 72×29 F | 350/320/280 ×104 |
| `public-upcoming-390/360/320` | `ae3b0fdf`/`850235af`/`adefe242` | `시세 신뢰도 낮음 — 판정 보류` 174×29 F | 같음 |
| `public-pastdue-390/360/320` | `17844b9e`/`ed079658`/`4866c2c8` | `지난 기일 — 다음 기일 공고 대기` 191×29 F | 같음 |
| `public-flood-390` | `ccf4fc10` | `침수·전손 의심 — 입찰 보류` 165×29 F | 350×104 |
| `admin-repro-390` | `36a34dd5` | `매각 종료` | 350×82, `[다시 분석]` 굵게 |
| `priced-390` | `d18445ab` | `시세 신뢰도 81/100 …` + `높음`(전과 동일 구성) | — |

- 기계 측정(`measure-after.json`·`measure-extra.json`): 시세 없는 물건 3종(repro·upcoming·pastdue) × 320·360·390·430·768·1440 = **18/18 가로 스크롤 0 · 화면 밖 0**. `낮음` 단독 배지 0/18. 공개 문구 `아직` 0.
- 눈으로 본 것(390·320 repro, 320 upcoming, 320 pastdue, 390 flood, 390 admin, 390 priced 를 Read): 배지 글자는 `.badge` 안에서 잘리지 않는다. 320 에서는 배지가 브랜드 줄 **아래로 내려와**(`.mh-top` flex-wrap) 한 줄 알약으로 온전히 보인다 — 191px 최장 라벨도 한 줄이라 ' — ' 접기는 **발동하지 않았다**(발동 조건은 §5). 문구 블록은 왼쪽 레일 + 첫 문장 잉크색 굵게 + `물건 상세` 밑줄, 면책보다 진하다.
- ⚠ 시세 **있는** 물건(k7 `2026타경50403_1`)은 320/360 에서 `scrollWidth` +91px 이 **간헐적으로**(16회 중 4회) 잡혔다. 변경 **전** 템플릿을 띄운 별도 서버(worktree, :8795)에서도 같은 빈도로 나왔다 → **기존 현상, 이 변경과 무관**(마스트헤드 md5 동일·본문 미변경). 원인 미조사(범위 밖). 잡힌 요소는 `.secnav` 의 마지막 칩 `절차`(가로 스크롤 컨테이너 안이라 측정 오탐 가능성).

## 4. 기일 미도래 물건의 실제 라벨 (검수자가 실행 못 한 것)

- `2026타경51876_1`(포터II, 기일 09-28, median NULL, confidence 0/`낮음`): `bid_state` = **`lowconf` / `시세 신뢰도 낮음 — 판정 보류`**. `동급 시세 없음`(nomarket)이 **아니다** — `no_market_reason()` 은 건설기계·선박 낱말에만 반응하고 포터II 는 해당 없음 → `not med` 분기.
  마스트헤드는 이제 이 라벨을 상세 h1 칩과 같은 글자로 찍는다. 라벨 안에 `낮음` 이 있지만 명사(`시세 신뢰도`)가 붙어 있고 판정 단일 소스가 낸 말이다. (판정 로직은 범위 밖 — `service.py` 주석의 "비교했는데 못 믿겠다 / 비교할 대상이 없다" 구분을 이 물건에 어떻게 적용할지는 별도 판단.)
- 이 분기 공개 431건(`list_vehicles(hide_incomplete=True)` ∧ `median_price IS NULL`, 스크래치 사본)이 앞으로 마스트헤드에 찍을 말 — **확인된 사실**:

| 건 | state | 라벨 | 예 |
|---:|---|---|---|
| 261 | wait | 지난 기일 — 다음 기일 공고 대기 | `2025타경12403_1` |
| 114 | closed | 매각 종료 | `2025타경56677_1`(라이브 재현) |
| 32 | lowconf | 시세 신뢰도 낮음 — 판정 보류 | `2026타경51876_1` |
| 13 | lowconf | 시동·운행 불가 — 판정 보류 | `2025타경6000_1` |
| 6 | blocked | 침수·전손 의심 — 입찰 보류 | `2025타경53062_1` |
| 5 | nomarket | 동급 시세 없음 | `2026타경3204_1` |

전 431건이 `낮음`(296)·`—`(135: confidence NULL) 대신 위 여섯 말 중 하나를 단다. 최다는 `지난 기일 — …`(60.6%) — 그래서 그 라벨을 320 에서 따로 찍었다.

## 5. 못 한 것 · 확신 없는 것

- **라이브 재검증 안 함(사실)** — 로컬 사본 렌더. 배포 뒤 `/vehicle/2025타경56677_1/report` 를 XFF 로 다시 봐야 한다.
- **큰글씨 모드 미캡처(사실)** — `html.nc-large .badge{font-size:13px}`. 320 의 배지 가용폭 ≈216px(마스트헤드 280 − 좌우 32×2)인데 최장 라벨 191px × 13/10.5 ≈ 236px → **그때는 ' — ' 에서 두 줄로 접힐 것(추정)**. `ui.judge_label` 의 nowrap 두 조각이 그 접힘을 낱말 중간이 아니라 ' — ' 뒤로 보낸다 — 렌더 HTML 은 테스트로 확인했으나 화면은 안 찍었다.
- **링크 히트영역(사실)** — 인라인 텍스트 링크라 높이 15px(줄 22px). 40px 기준 미달. 새 CSS 금지라 그대로 뒀다. 40px 주동작은 sticky 바 `상세로` 가 맡는다.
- **`<b style="color:var(--ink)">` 의 인라인은 중복(사실)** — `.sec-empty b{color:var(--ink);font-weight:600}` 규칙이 이미 같은 값을 준다(실측 색 동일). 지시 원문대로 넣었다. 지워도 화면은 같다(추정 아님 — 같은 규칙).
- **브랜드마크 `A`**(검수자 추정: 점수 없는 페이지에서 등급처럼 읽힐 수 있음) — 손대지 않았다, 범위 밖.
- **후 테스트 수에 다른 담당 파일 포함 여부 미검증** — 1241 = 1226 + 내 15 로 정확히 맞아 떨어지므로 `test_store_overlay.py` 는 실행 뒤에 생긴 것으로 **추정**.
