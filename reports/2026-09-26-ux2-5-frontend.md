# UX-2 탭 복귀형 · UX-5 조건 표면 · UX-1 칩 ✕ 교정 · UX-3 구분 줄 — frontend 보고 (지시서 2026-09-26-23)

기준 커밋 `576de11`(작업 트리 dirty — 1차 backend `web/db.py`·`web/app.py`, 1차 frontend `web/templates/vehicles.html`·`tests/test_ux1_ux4_form_and_chips.py`, Steward `docs/backlog.md` 가 커밋 없이 있고 그 위에서 작업). 커밋·배포·라이브 요청 없음. 운영 DB 는 `mode=ro` 사본만. 8765 무접촉.

## [요약]
- **UX-2**: 하단 탭·사이드바 `차량목록`(`data-nav="vehicles"`)이 **30분 안에 본 마지막 목록 URL**로 돌아가고 스크롤까지 복원한다(Playwright 재현: 필터 3개+2페이지+scrollTop 300 → 달력 → 탭 → URL·셀렉트 3개·13–15/15·scrollTop ≥250 복원). 목록에서 재탭 = 맨 위. 31분 지나면 `/vehicles`. PC 프레임은 iframe 이동마다 해시를 갱신해 **F5 뒤 2페이지가 유지**된다. 상세 ← 는 `목록으로` 라벨.
- **UX-5**: `총 N건` 줄 위 **조건 요약 한 줄**(`현대 · 1,000~2,000만 · 매각기일순 · 2/13페이지`) — 같은 문자열을 큰글씨 접힌 머리(`검색 · 필터 — …`)에도 붙였다(한 `{% set %}`). 정렬 라벨은 `sort_labels` dict 하나로 셀렉트·요약이 함께 돌린다. 검색어 활성 칩(`“쏘나타” ✕`) + `type="search"`. [적용]은 폼이 바뀌면 primary 채움(`is-dirty`). 가격대 셀렉트 title.
- **UX-1 뒷부분**: 날짜·결과·법원 칩 ✕ → `qs_no_date/result/court`(그 조건만 해제, `/courts` 이탈 제거). 법원·실사용·유망 칩 아이콘 제거로 **모든 활성 칩 높이 26 = 이웃**(34·74 → 26). `검토 추천` 칩 nowrap + 이모지 유지(320 ✕ 우변 250.9 < 페이드 275).
- **UX-3**: `매각기일순` 경계 카드 앞 구분 줄 `지난 기일 1,019건 — 낙찰 결과·다음 기일 대기 참고`(사본 13페이지 0번, 카드·표 둘 다). `app.py` 조건 한 줄로 segment·bucket 경로도 계산.
- **판단 2건(지시서와 다른 곳)**: ⑴ `필터 초기화` 칩은 **sm 이상에서만** 칩 줄에 두고, 좁은 폭은 요약 줄 끝 링크가 맡는다 — 지시서 원문대로 320~430 에도 넣으면 첫 활성 칩 ✕ 가 전부 페이드 뒤로 밀려 UX-4 DoD 가 회귀한다(§3 mid 실측). ⑵ 상세 액션 줄의 `sm:shrink-0` 을 뺐다 — 라벨이 붙자 640~655px 에서 잘렸다. 둘 다 아래에 수치.
- 테스트: 신규 `tests/test_ux2_ux5_nav_state.py` **33**(템플릿 27 + Playwright 6) 초록. 지시서 6파일 **212 passed, 2 xfailed**. 전체 스위트 **1,524 passed, 2 xfailed, 0 failed**(164s; 1차 보고의 1,491 + 신규 33).

## 1. 바꾼 것 (파일 · 앵커 — 줄 번호 없음)

### `web/templates/base.html`
| 앵커 | 변경 |
|---|---|
| `{% macro navitem(` · `{% macro tab(` | `<a href="{{ href }}"{% if href == '/vehicles' %} data-nav="vehicles"{% endif %}` — `/vehicles` 항목에만. href 는 그대로(JS 없는 폴백 = 예전 동작) |
| 스크롤 복원 IIFE(`var KEY = 'nc:scroll'`) | `viaTab = sessionStorage['nc:restoreScroll']==='1'` 를 읽고 **즉시 removeItem**. `restore()` 호출 조건 `isBackForward() \|\| viaTab`(pageshow 도 같이). back_forward 판정 무변경 |
| 새 `<script>`(그 바로 아래, 주석 `탭 복귀(UX-2…)`) | `NC_LASTLIST_TTL_MS = 30 * 60 * 1000`(이유 주석: 다음날은 깨끗한 최근 등록순, 영속은 `검색 저장`). `remember()` — `location.pathname === '/vehicles'` 일 때 `nc:lastList={url,ts}` 저장(로드 + `pagehide`). 클릭 위임: `[data-nav="vehicles"]` → 목록이면 `preventDefault` + `#appscroll.scrollTo(0)`(reduced-motion 이면 `auto`, 아니면 `smooth`); 아니면 `lastList()`(TTL 안 · `/vehicles` 로 시작 · `//` 금지) 있으면 `nc:restoreScroll='1'` 세우고 `location.assign`. 없으면 기본 href. 수정키·비주클릭·`defaultPrevented` 는 건드리지 않음 |

### `web/templates/detail.html`
| 앵커 | 변경 |
|---|---|
| `onclick="return _goBack(event)"` | `aria-label="목록으로" title="목록으로"`, 클래스 `btn-ghost text-sm flex items-center gap-1.5`(이웃 버튼과 동일), `style="…width:2.75rem"` 제거, 아이콘 뒤 텍스트 ` 목록으로`. 위 주석 본문도 갱신(예전 '아이콘 전용' 사유 → 라벨을 붙인 사유). `_goBack`·`href="{{ back_url or '/vehicles' }}"` 무변경 |
| `<div class="flex flex-wrap gap-2 sm:shrink-0">`(액션 줄 컨테이너) | `sm:shrink-0` 제거(§3-④ 이유). 주석 추가 |

### `web/static/frame.html`
`document.getElementById("f").src = p;` 아래에 iframe `load` 리스너: `contentWindow.location.pathname + search` 를 읽어 내부 경로(`/` 시작·`//` 아님)면 `history.replaceState(null, "", "#"+np)`(같으면 생략). try/catch. 초기 로드·SW 등록 무변경.

### `web/templates/vehicles.html`
| # | 앵커 | 변경 |
|---|---|---|
| V1 | `{% block content %}` 직후 | `{% set sort_labels = {…8종…} %}` · `{% set bucket_labels = {…} %}`(버킷 칩의 인라인 `_bkl` 사전을 여기로 올림 — 칩은 `{% set _bkl = bucket_labels %}` 로 같은 원천) · `namespace(parts=[])` 로 **조건 요약 조립** → `cond_summary`(`' · '` join) · `has_filter`(judgment·maker·q·price·upcoming·cond·date·court·segment·bucket·usepick·picks·result·promising 중 하나). 순서: 판정(`\|jshort`)·제조사·가격대(`price_bands` key==price)·`“q”`·`입찰예정 N일`·검사 경과/외관 손상·`M/D 매각`·법원(칩과 같은 식)·차종(`segment_presets`)·결과·버킷·실사용·유망·검토 추천·정렬 라벨(`sort_labels`, recent 제외; usepick 이면 `이득 큰 순`)·`page/total_pages페이지`(**page ≥ 2 일 때만** — 1페이지가 `1/110페이지` 를 말하면 첫 진입이 기본 상태가 아니게 된다) |
| V2 | `#listFilter > summary` | `whitespace-nowrap` 추가 + `{% if cond_summary %}<span class="min-w-0 truncate text-mut font-normal" title="…">— {{ cond_summary }}</span>` — 큰글씨에서만 보이는 머리(U8). nowrap 이 없으면 `검색 · 필터` 텍스트 노드가 3줄로 접혔다(실측 80px → 30px) |
| V3 | `<select name="price"` | `title="괄호 건수는 현재 적용된 조건 기준"`(E3) |
| V4 | 정렬 셀렉트 | 옵션 8줄 → `{% for k, lbl in sort_labels.items() %}` 루프(usepick 의 `이득 큰 순` 옵션·selected 규칙 그대로) |
| V5 | `<input name="q"` | `type="search"` |
| V6 | `검색 저장` 버튼 뒤 | `{% if has_filter %}<a href="/vehicles" class="hidden sm:inline-flex … border-line text-mut …">필터 초기화</a>` — 비활성 톤. **sm 미만 숨김**(§3-③) |
| V7 | usepick·picks 칩 | `directions_car`·`check_circle` 아이콘 제거(높이 34→26). 문구·href 그대로 |
| V8 | promising 칩 | 줄 끝(상태 토글 뒤) → usepick·picks 칩 옆으로 **이동** + `inline-flex … whitespace-nowrap shrink-0`(74px 4줄 → 26). 이모지 유지(§3-②) |
| V9 | result·date·court 칩 | href `/vehicles{% if qs_no_X %}?{{ qs_no_X }}{% endif %}`; result 에 `title="결과 필터 해제"`; court `title="법원 필터 해제"`·`gavel` 제거·`/courts` 제거 |
| V10 | court 칩 뒤 | 검색어 칩 `{% if q %}<a href="/vehicles{% if qs_no_q %}?{{ qs_no_q }}{% endif %}" … title="검색어 해제"><span class="truncate max-w-[9rem]">“{{ q }}”</span> ✕</a>` — 긴 검색어는 낱말만 말줄임, ✕ 는 남는다 |
| V11 | `<details id="listLegend"` 앞 | 래퍼 `<div class="space-y-1.5">` 안에 `<p id="condSummary" class="px-1 text-xs text-mut leading-5 tnum">`(값 `text-txt whitespace-nowrap`, 구분점 mut) + sm 미만 전용 `<a href="/vehicles" class="sm:hidden inline-flex items-center min-h-[40px] -my-2.5 …text-primary">필터 초기화</a>`(히트영역 40px, 줄 높이 20px 유지). 래퍼는 `#appscroll` 의 `space-y-6` 슬롯을 범례와 나눠 쓰기 위함(줄이 따로 24px 을 먹지 않게). `</details>` 뒤 `</div>` |
| V12 | 표 루프 `<tr class="even:bg-slate-50/60` 앞 · 카드 루프 `<a href="/vehicle/{{ v.id }}" class="block` 앞 | `{% if sale_split and loop.index0 == sale_split.page_first_past_idx %}` → 표: `<tr data-sale-split class="bg-background"><td colspan="9" …>` / 카드: `<div data-sale-split class="px-4 py-2 text-xs text-mut bg-background">`. 문구 `지난 기일 <b class="text-txt tnum">{{ '{:,}'.format(sale_split.past) }}</b>건 — 낙찰 결과·다음 기일 대기 참고`. 카드 마크업 무변경 |
| V13 | 하단 스크립트(`listSkeleton` IIFE 앞) | `#listFilter form` 의 `button.btn-ghost:not([type])` 두 개에 `change`/`input` → `is-dirty`, 로드·`pageshow` → 제거. ⚠ `form[action="/vehicles"]` 는 헤더 검색 폼이 먼저 잡혀 쓰지 않았다. 자동 제출 없음 |

### `web/static/tailwind_input.css` → `npm run build:css` 3회(마지막 diff 1줄 = minified 한 줄)
`.btn-ghost:disabled` 아래 `.btn-ghost.is-dirty { background:#533afd; color:#fff; border-color:transparent }` · `:hover #4434d4` — `.btn-primary` 와 같은 토큰. 새 유틸리티(`min-h-[40px]`·`-my-2.5`·`max-w-[9rem]`·`sm:inline-flex`·`space-y-1.5`)가 app.css 에 들어갔는지 `grep -F` 로 확인.

### `web/app.py` — 허용된 한 줄
`sale_split = None` 아래 `if sort == "sale_date" and not _py_filtered:` → `if sort == "sale_date" and not (usepick in USEPICK_VALUES or picks == "1"):`. 바로 위 주석 두 줄이 "파이썬 경로 → None" 이라 말하고 있어 **주석 본문도 같이** 고쳤다(CLAUDE.md 규칙 8 — 가리키는 곳이 아니라 말하는 내용이 거짓이 되는 것을 막음). 그 밖 `app.py` 무접촉.

### `tests/test_ux3_sort_rules.py` ④
`test_python_paths_with_sale_date_sort_render_and_have_no_sale_split` 파라미터 8 → usepick·picks 4(None 단정 유지) + 신규 `test_order_preserving_python_paths_keep_sale_split` 4(segment=sedan/suv·bucket=wait/review → dict · 합=총수 · `segment=sedan` 은 `{upcoming 4, past 3, undated 2, idx 4}` 정확히). 파일 총수 53 → 53. 독스트링 ④ 한 줄 갱신.

### `tests/test_ux2_ux5_nav_state.py`(신규, 33)
A. 템플릿(TestClient — host `testserver` 라 **공개 뷰**로 렌더, 헤더 흉내 없음) 27: data-nav 두 곳/다른 항목 없음 · 스크립트 앵커 3종+`isBackForward() \|\| viaTab` · 상세에서 탭 활성 유지 · 필터 초기화 칩(있음/없음/정렬·페이지만이면 없음) · 요약 줄 정확 문자열(`현대 · 1,000~2,000만 · 매각기일순 · 2/2페이지`; 기본 상태 None; `?page=2` → `2/2페이지`) · 요약 부품 7케이스(q+segment·date·court 2곳+upcoming·judgment+cond·maker·sort=mileage·page=2) · 큰글씨 머리 같은 문자열+title, 기본 상태엔 `—` 없음 · `sort_labels` 한 원천(옵션 리터럴 부재 + 렌더 일치) · 검색어 칩 href == `qs_no_q`·`type="search"` · date/result/court(단일·다중) ✕ href == `qs_no_*`(html.unescape 뒤) · court 칩 gavel 없음·`/courts` 없음 · usepick/picks 아이콘 없음 · promising nowrap · price title · dirty 스크립트+CSS 토큰 · [적용] 마크업 불변(다른 테스트의 정규식 보호) · 구분 줄(3·3 → 카드 3장 뒤·1회·표 tr / 12·3 → 1p 없음·2p 0 / 과거만 → 맨 위 / segment 경로도·picks 는 없음) · 상세 ← 라벨·고정폭 제거 · frame.html `replaceState`·`//` 거름·초기 로드 불변.
B. 동작(Playwright, 스레드 uvicorn, OS 가 비운 포트 — 금지 6개 제외 단정) 6: ① S1 재현(필터 3+2페이지+scrollTop 300 → 달력 탭 → 차량목록 탭 → URL 동일·셀렉트 3개·`13–15`·scrollTop ≥250·플래그 소비됨) ② 재탭 → URL 불변·scrollTop 0 ③ 상세 → 탭 → 마지막 목록 ④ TTL: 달력에서 `nc:lastList.ts` 를 31분 전으로 → 탭 → `/vehicles` ⑤ 1440 프레임(`/static/frame.html#…` 직접 — loopback 은 관리자라 자동 리다이렉트가 꺼짐) → 2페이지 클릭 → 바깥 해시 `page=2` → reload → iframe URL `page=2` 유지 ⑥ 큰글씨 → `#listFilter` 접힘·summary 보임·같은 문자열·높이 <48.
⚠ 탭 셀렉터는 `nav[class~="lg:hidden"] a[data-nav="vehicles"]` — 사이드바의 같은 항목이 폰 폭에서 화면 밖에 렌더돼 `:visible` 로도 잡혀 "outside of the viewport" 로 실패했다(실측). 주석에 남김.

## 2. 판단 항목
| 항목 | 판단 | 근거 |
|---|---|---|
| 상세 화면의 `차량목록` 탭 활성 표시 | **유지** | 탭이 실제로 "내 목록"으로 가므로 활성 표시가 한 약속("내가 있던 섹션의 화면으로")이 지켜진다. usedcar 지적 2 의 2안(활성 끄기)은 1안이 되면 불필요 — 테스트 `test_active_state_on_detail_kept` 로 고정 |
| `검토 추천` 칩 이모지 | **유지** | 이동 후 320px 첫 활성 칩 자리에서 ✕ 포함 pill 우변 250.9 < 페이드 275(390 도 345 앞). 이모지가 없어도 되지만 규칙상 뺄 이유가 없다. ⚠ 원래 자리(줄 끝)에서는 이모지와 무관하게 ✕ 가 화면 밖이라 규칙 자체가 성립하지 않아 **다른 진입 칩 옆으로 옮겼다**(DOM 순서 변경 1건) |
| TTL | **30분, 상수 하나** | 디자인 ①("짧은 TTL, 예 30분"). `pagehide` 마다 ts 갱신 → '목록을 마지막으로 본 때'부터. sessionStorage 라 브라우저를 닫으면 어차피 사라진다 |
| 요약 줄의 인용부호 | 곡선 `“ ”` | 지시서는 칩에 `“{{ q }}”`, 요약에 `"쏘나타"` 로 적혀 있었다. 한 화면에 두 종류가 보이면 어휘 불일치라 **칩과 같은 곡선**으로 통일. ncSaveSearch 의 저장 라벨(직선 `"`)은 즐겨찾기 탭 문제라 손대지 않았다 |
| 요약 줄의 부품 범위 | 지시서 11종 + 결과·버킷·실사용·유망·검토 추천 5종 **추가** | 다섯은 기존 칩·배너 문구를 그대로 재사용(새 낱말 없음). 빼면 `?bucket=wait` 처럼 "활성 조건이 있는데 요약이 비어" sm 미만의 초기화 링크가 자리를 잃는다(§3-③ 구조 때문). 순서는 지시서 11종 뒤 |
| 페이지 위치 | **2페이지부터** | 지시서 "2페이지 이상일 때" 를 `page ≥ 2` 로 읽음. `total_pages ≥ 2` 로 읽으면 첫 진입 `/vehicles`(110페이지)가 `1/110페이지` 줄을 그려 "기본 상태에서는 그리지 않는다" 와 충돌 |
| 구분 줄 "예정 기일 없음" 케이스 | 루프 규칙 하나로 처리 | `upcoming == 0` 이고 과거가 있으면 `page_first_past_idx == 0` 이라 1페이지 첫 카드 위에 같은 줄이 선다(테스트 `test_split_line_top_when_only_past`). 과거도 없고 미상만 있으면 아무 줄도 안 그린다 — 백엔드 계약대로 `undated` 는 '지난 기일'이 아니고 운영 사본에 0행 |
| 필터 초기화 칩 위치 | **sm 이상만 칩 줄**, 좁은 폭은 요약 줄 끝 링크 | §3-③ |
| 상세 액션 줄 `sm:shrink-0` | **제거** | §3-④ |

## 3. 측정 (조건 → §5)

### ① 가로 스크롤 — 6폭 × 3URL, `documentElement`·`#appscroll` 둘 다: **전부 없음**(before·mid·after 동일)

### ② 칩 높이·첫 활성 칩 ✕ 위치(pill 우변, 320px 기준 — 360·390·430 값 동일, 페이드 = 줄 우변−16 = 275/315/345/385)
| 칩 | before 높이 | **after 높이** | before 우변 | **after 우변** | 판정(320) |
|---|---|---|---|---|---|
| `1,000~2,000만 ✕`(combo) | 26 | 26 | 264.7 | 264.7 | 통과 |
| `2026-09-01 매각 ✕`(date) | 26 | 26 | 276.5 | 276.5 | 1차와 같음(pill 1.5px 페이드 안, ✕ 글리프는 앞) — 무변경 |
| `“쏘나타” ✕`(q, 신규) | — | 26 | — | 229.5 | 통과 |
| `수원지법 ✕` / `법원 2곳 ✕` | **34** | **26** | 256.4 / 256.0 | 228.4 / 228.0 | 통과(아이콘 제거) |
| `🎯 검토 추천 ✕` | 74(1차 보고, 줄 끝) | **26** | (줄 끝, 화면 밖) | 250.9 | 통과(이동+nowrap) |
| `실사용 추천 ✕` | **34** | **26** | 269.8 | 241.8 | 통과 |
| `유망 물건 N ✕` | **34** | **26** | 269.9 | 241.9 | 통과 |
| `결과=낙찰 ✕` · `유찰 대기 827 ✕` | 26 | 26 | 235.9 · 255.3 | 동일 | 통과 |
`검색 저장`(btn-ghost) 38 은 예전부터 — 범위 밖. 768·1440 은 `sm:contents` 라 줄이 form 안으로 풀려 페이드 개념 없음, 높이 전부 26.

### ③ **mid — 필터 초기화 칩을 지시서 원문대로 sm 미만에도 노출했을 때**(캡처 `mid-*`)
`검색 저장` 우변 140.1 → 칩 우변 228.9 → 첫 활성 칩 시작 236.9. 첫 활성 칩 ✕ 우변: 가격대 **353.5**, 날짜 **365.4**, 검색어 **318.4**, 법원 317.3, 검토 추천 339.8, 실사용 330.6, 유망 330.8, 결과 324.8, 버킷 344.2 — **320(275)·360(315) 전부 위반**, 390(345) 은 날짜·가격대 위반. UX-4 DoD("320·360·390 에서 해제 ✕ 첫 화면 안") 회귀가 확정이라 지시서 원문 위치를 그대로 두지 않았다. 처방: 칩은 `hidden sm:inline-flex`(sm 이상 = 지시서대로 `검색 저장` 바로 뒤), sm 미만은 조건 요약 줄 끝에 같은 href·같은 낱말의 링크(디자인 ④ 원문이 그 자리에 `[초기화]` 를 그려 뒀다). 부작용: 요약 줄이 없는 조합은 이제 없다(§2 부품 범위) — `has_filter` 이면 요약 부품이 최소 하나 있다.

### ④ 조건 요약 줄(`#condSummary`, `leading-5` = 20px/줄)
| URL | 320 | 360·390·430 | 768·1440 |
|---|---|---|---|
| combo(`현대 · 1,000~2,000만 · 매각기일순 · 2/13페이지 · 필터 초기화`) | **2줄(40px)** — ` · ` 에서만 접힘, 낱말 안 절단 없음 | 1줄 | 1줄(링크 없음) |
| date(`9/1 매각 · 매각기일순 · 필터 초기화`) | 1줄 | 1줄 | 1줄 |
| q(`“쏘나타” · 세단 · 필터 초기화`) | 1줄 | 1줄 | 1줄 |
기본 `/vehicles` 는 줄 없음(래퍼 div 만, 렌더 무변화). 큰글씨(360, combo) 접힌 머리: `display:flex`·open=false·높이 **30**(nowrap 전 80)·요약 span 말줄임(`title` 에 전문).

### ⑤ 상세 액션 줄(`/vehicle/2025타경57868_1`, 사본 첫 예정기일·감정평가서 있음)
| 폭 | before | **after** |
|---|---|---|
| 320·360·390·430 | ← 44×42, 줄 높이 142(3줄), 넘침 없음 | ← **112×42** `arrow_back 목록으로`, 줄 높이 142, 넘침 없음(`overflow False`, `#appscroll` 가로 스크롤 없음) |
| 768(관리자 뷰 = `다시 분석` 포함 6버튼) | 한 줄 752 우변, 넘침 없음 | `sm:shrink-0` 그대로였다면 **우변 780 > 768(12px 잘림)** → 제거 후 두 줄(92px), 넘침 없음 |
| 1440 | 42, 넘침 없음 | 42, 넘침 없음 |
공개 뷰(5버튼: 즐겨찾기 112 · 감정평가서 155 · 리포트 100 · 공유하기 112 · 목록으로 112 + gap 32 = **623px**, 추정 — 관리자 측정값에서 `다시 분석` 116+8 을 뺀 산술): 768 은 736 안에 들어 한 줄(제목 97px), **640~655 는 헤더 폭(vw−32)을 넘어** shrink-0 이면 오른쪽 버튼이 잘렸을 구간 → 제거로 두 줄 접힘. 들어갈 때(768+) 렌더 무변화.

### ⑥ 구분 줄 — 사본 `?sort=sale_date&page=13`(1~20페이지 순회, 13페이지에만 존재 = backend 보고와 일치)
카드: `지난 기일 1,019건 — 낙찰 결과·다음 기일 대기 참고`, 높이 33, 첫 지난 기일 카드 **앞**(이전 형제 = 카드 아님·다음 형제 = 카드), 프레임 안 캡처. 표(1440): `<tr>` colspan 9, 높이 33, 같은 문구. combo(2페이지) 130건·date 59건 도 각 경계 페이지에서 1회.

### ⑦ PC 프레임(1440, `/static/frame.html#/vehicles?maker=현대&price=1000-2000` 직접)
2페이지 클릭 → iframe `…&page=2` · 바깥 해시 `#/vehicles?maker=…&sort=recent&price=1000-2000&page=2` → `reload()` → iframe **`page=2` 유지**. (`sort=recent` 가 붙는 것은 `qs` 가 sort 를 항상 싣는 기존 동작.)

## 4. 캡처 — `screenshots/ux2-5/`(36장 md5 전부 상이, `HEAD.txt` 에 기준 커밋 `576de11`+dirty 목록·조건·md5)
| 파일 | 내용 |
|---|---|
| `before-{360,390,1440}-{combo,date,q}.png` · `before-320-{date,q}.png` | 이 회차 변경 전(1차 -22 반영 상태) |
| `mid-320-date.png` · `mid-320-q.png` · `mid-390-combo.png` | 필터 초기화 칩 전 폭 노출 — 첫 활성 칩 ✕ 가 페이드 뒤로 밀린 증거(§3-③) |
| `after-{360,390,1440}-{combo,date,q}.png` · `after-320-{date,q}.png` | 최종. `after-1440-combo` 에 칩 줄의 `필터 초기화`(검색 저장 뒤) + 요약 줄(링크 없음) |
| `after-360-large-collapsed.png` | 큰글씨 접힌 머리 `검색 · 필터 — 현대 · 1,000~2,…` 한 줄 + 아래 요약 줄 |
| `after-390-split-p13.png` · `after-1440-split-p13.png` | 구분 줄(카드·표) |
| `before-/after-{320,360,390}-detail.png` · `after-390-detail-actions.png` | 상세 ← (`목록으로`) |
| `after-1440-frame-page2.png` | F5 뒤 프레임 안 2페이지 |
Read 로 열어 확인한 것: after-390-combo · after-320-date · after-320-q · after-360-large-collapsed(nowrap 전·후 둘 다 — 전은 `검색/·/필터` 3줄로 깨져 있었고 그걸 보고 고쳤다) · after-390-split-p13 · after-390-detail-actions · after-1440-combo.

## 5. 측정 조건
로컬 `127.0.0.1:8931`(NC_NO_SCHEDULER=1 NC_NO_BACKGROUND=1; 8765·8888 LISTENING 무접촉, 금지 포트 미사용). DB = `data/auction.db` 를 `mode=ro` 로 열어 `sqlite3.backup` 한 사본 `ux25.db`(1,418행, md5 `3530d885…` = 1차·FEAT-1 r2 사본과 동일). 앱은 `db.DB_PATH` 를 사본으로 바꿔 기동 → 운영 DB 쓰기 없음. Playwright 1.60 Chromium headless, DPR 1, 높이 900(테스트는 640), ko-KR, `has_touch=True`(데스크톱 프레임 자동 리다이렉트 회피 — 1440 도 앱 화면 그대로), `networkidle`+300ms, `getBoundingClientRect` 소수 1자리.
⚠ **뷰 모드**: loopback 접속은 `is_admin=True`(관리자 뷰). `x-forwarded-for` 헤더 흉내는 도구 정책에 걸려 쓰지 않았다 → 캡처·측정은 전부 관리자 뷰. 목록의 칩 줄·요약 줄·구분 줄 마크업은 공개 뷰와 같다(차이: 사이드바 하단 데이터 문구, 표의 표본수). 상세는 `다시 분석`(116px) 버튼이 하나 더 있어 공개 뷰 폭은 §3-⑤ 처럼 산술로 적었다(추정). TestClient 테스트는 host 가 `testserver` 라 공개 뷰다.
before 캡처는 템플릿 변경 전에 같은 서버에서 먼저 찍었다(Jinja 자동 리로드라 편집 전에 찍는 순서가 중요했다). `app.py` 한 줄은 서버 재기동으로 반영.

## 6. 테스트
- 신규 `tests/test_ux2_ux5_nav_state.py`: **33 passed**(15.8s; Playwright 6 포함).
- 지시서 6파일(`test_ux2_ux5_nav_state` 33 · `test_ux1_ux4_form_and_chips` 23 · `test_ux3_sort_rules` 53 · `test_feat1_price_select` 21 · `test_feat1_qa_adversarial` · `test_price_band_filter`): **212 passed, 2 xfailed**(xfail 2 = `test_feat1_qa_adversarial` 의 기존 strict xfail, 무변화). 중간에 `test_template_source_has_no_hardcoded_band_labels_or_keys` 1건이 빨간불 — 내가 템플릿 **주석**에 `1,000~2,000만` 을 예시로 적어서(FEAT-1 규칙 "라벨 리터럴 금지" 가 주석까지 본다). `<가격대 라벨>` 로 바꿔 초록.
- 전체 스위트(`NC_NO_SCHEDULER=1 NC_NO_BACKGROUND=1 python -m pytest -q -p no:cacheprovider`): 1차 실행 `1 failed, 1523 passed, 2 xfailed`(165.5s — 실패 1 = 위 주석 리터럴) → 주석 수정 후 재실행 **`1524 passed, 2 xfailed`**(164.0s, exit 0). 1차 보고 기준 1,491 + 신규 33 = 1,524 로 산술이 맞는다.
- 지운 기존 테스트 0. 고친 기존 테스트 1파일(`test_ux3_sort_rules.py` ④ — 지시서가 허용한 segment/bucket None 단정만).

## 7. 못 한 것 · 남긴 것
1. **공개 뷰 실측 없음**(§5). 상세 액션 줄 640~655 의 공개 뷰 수치는 산술. qa 반증 회차가 라이브(공개 뷰)에서 ⑴ 상세 ← 줄 640·768 ⑵ 프레임 자동 리다이렉트 경로(`/vehicles` → `frame.html#…`)에서의 해시 갱신을 한 번 봐 주면 좋다.
2. 날짜 칩 pill 우변 276.5 vs 페이드 275(1.5px) — 1차 보고 §5-3 그대로(문구·패딩은 범위 밖).
3. `검색 저장`(38px) 이 이웃 칩(26px)보다 큰 것 — 예전부터, 범위 밖.
4. 상세 헤더에서 제목 열이 `flex-1`(basis 0%) 라 액션 줄이 꽉 차는 폭(관리자 640~768·공개 ~655)에서 제목 폭이 0 에 가깝게 눌리는 것 — **예전부터 있던 구조**(내 변경 전 640 에서도 37px). 이번엔 `sm:shrink-0` 제거로 잘림만 막았다. 제목 최소 폭을 주려면 헤더 구조를 바꿔야 해서 남긴다.
5. 요약 줄 320px combo 2줄(40px) — ` · ` 에서 접히는 정상 줄바꿈. 1줄로 만들려면 부품을 줄여야 해 두지 않았다.
6. 정렬 라벨 `sort_labels` 는 템플릿 안 dict 다(`service` 로 올리지 않음 — 지시서 "템플릿에 `{% set sort_labels %}`"). `db.sort_cols` 키와의 동기화는 기존 `tests/test_sort.py` 의 소스 불변식이 지킨다(변경 없음).
7. 라이브 확인 없음(배포 대기).

## 8. 확인된 사실 / 추정
**확인된 사실**: §1 diff(`git diff HEAD`) · §3 수치(스크립트 출력 JSON `measure-{before,mid,after}.json`, 캡처와 같은 세션) · §4 md5(`hashlib`, 36 distinct) · §6 pytest 출력 · app.css 에 새 유틸리티 존재(`grep -F`) · 운영 DB ro 열기 + 사본 · 8765 무접촉 · 구분 줄 페이지 13(1~20 순회) · 프레임 F5 유지(Playwright 두 번 — 스크립트·테스트).
**추정**: 공개 뷰 상세 액션 줄 폭 623(산술) · 라이브 재현(배포 후) · `smooth` 스크롤이 실제 폰에서 어떻게 보일지(헤드리스는 reduced-motion 컨텍스트로 `auto` 만 검증).
