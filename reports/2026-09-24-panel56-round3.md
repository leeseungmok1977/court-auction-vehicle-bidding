# PANEL-56 3회차 — 교차검수 처방 반영 (지시서 2026-09-24-13)

- 담당: frontend-engineer · 입력: `reports/2026-09-24-panel56-cross-review.md` 의 **[처방 결정]** 만. 갈린 두 항목(알약 위치 이동 → PANEL-57, 면책 빨간 문장 → 오너 판단)은 **손대지 않았다.**
- 기준 커밋: 작업 시작 `9d1c41c` → 도중 `071d8ce`(docs/backlog.md 1파일, 내 대상 파일 무관 — `git diff --stat 9d1c41c 071d8ce -- report.html tests/…` 빈 출력). 캡처 `HEAD` 파일 = `071d8ce`, `TEMPLATE_MD5` 파일 = 작업트리 `report.html` md5 `740036ab`(미커밋).
- 손댄 파일(3): `web/templates/report.html` · `tests/test_panel55_report_nomarket_copy.py` · `tests/test_panel56_masthead_badge.py`
- 손대지 않은 것: `detail.html` · `service.py` · `_macros.html` · `.badge`/`.sec-empty`/`.note` 공용 규칙 · `app.css`(`npm run build:css` 전후 md5 **`bcfe1da6…` 동일**) · 운영 DB. **커밋하지 않았다.**

## 1. 바꾼 것 (앵커·클래스명으로 — 줄 번호 없음)

### ① stop 톤이면 본문 첫 문장이 판정 (두 검수자 공통 지적 · design-critic 처방)
위치: `report.html` `{% if report %}` 의 `{% else %}` 가지, 앵커 `<section class="section"><div class="sec-empty{{ ' is-stop' if _stop0 }}">`.
- `{% set _stop0 = (_tone0 == 'stop') %}` — `_tone0` 는 마스트헤드가 이미 쓰는 `bidst.tone`(`{% set _tone0 = … %}` 는 템플릿에 **한 번**, `<!-- 마스트헤드 -->` 와 `<header>` 사이). 판정을 새로 계산하지 않았다(테스트가 `bid_state(` 부재를 검사).
- 렌더(실측, 스크래치 서버):
  - 공개 stop: `<b>침수·전손 의심 — 입찰 보류 판정입니다.</b> 시세가 산정되지 않아 종합 리포트도 만들 수 없습니다. 감정가·최저매각가 등 법원 자료는 [물건 상세]에서 그대로 확인하실 수 있습니다.` — 라벨은 `{{ bidst.label }}` 원문(접기 없음), 굵기는 첫 문장에만.
  - 관리자 stop: 같은 첫 문장 + `시세가 아직 산정되지 않아 종합 리포트도 만들 수 없습니다. [물건 상세]에서 [다시 분석] 후 다시 시도하세요.` — `아직`·`[다시 분석]` 유지. ※ 판단: "뒤는 관리자 문구"를 **문장 내용 유지**로 읽고, 조사만 공개와 같은 `도` 로 했다(첫 문장이 앞서므로 `를` 이면 두 사실이 병렬로 읽힌다). 아니면 `도`→`를` 한 글자.
  - wait 톤(공개·관리자): 3회차 전과 **바이트 동일**한 첫 문장(테스트 `test_wait_tone_copy_is_unchanged`).
- 그릇: `.sec-empty.is-stop` 클래스 → 인라인 `<style>` 의 `.verdict.is-stop{border-top-color:var(--red)}` 바로 아래 `.sec-empty.is-stop{border-left-color:var(--red)}` 1줄(사이 주석 1줄). 실측 레일 `rgb(190,42,47)` 3px(stop) / `rgb(201,209,221)`(wait). 글자색은 그대로(`--ink` 굵게 / `--ink-soft`).
- 마스트헤드에 빨강은 **넣지 않았다.** stop 물건의 알약 실측: color `rgba(255,255,255,.9)` · border `rgba(255,255,255,.3)` · bg 투명 — wait 와 동일. `.masthead.is-stop .badge.judge` 규칙 부재를 테스트가 검사.

### ② 알약 서체 — 시세 없는 분기만 `class="badge judge"` (app-design-expert 1)
위치: 마스트헤드 `{% elif bidst %}<div class="badge judge">{{ ui.judge_label(bidst.label) }}</div>` — **같은 줄**, 줄 수 불변(주석은 주석 안에서만 2줄 늘렸다).
- `<style>`: `.badge.fill…` 줄 아래 `.badge.judge{font-family:var(--sans);font-size:12px;font-weight:600;letter-spacing:0;padding:6px 12px}` · `html.nc-large .badge{font-size:13px}` 아래 `html.nc-large .badge.judge{font-size:14px}`. `--sans` 는 리포트 `:root` 에 이미 있는 스택(Pretendard …) — 새 스택 없음.
- 실측(390 flood): `Pretendard` 12px 600 `letter-spacing:normal` padding `6px 12px`, 알약 156×33(전 회차 mono 165×29). 큰글씨: 14px, 205×36(pastdue) / 178×36(flood).
- 공용 `.badge` 규칙 원문 불변(테스트가 문자열 그대로 검사). 시세 있는 물건(k7) 마스트헤드: `badge judge` 0, `시세 신뢰도 81/100`(mono) + `높음` — md5 테스트 `e87fa43a…` **그대로 초록**.

### ③ 자잘한 셋 (design-critic 3)
- 공개 문구(wait·stop) `<span style="white-space:nowrap">감정가·최저매각가</span>` — 390 공개 캡처에서 전 회차의 `감정가·` 줄끝 고아가 사라졌다(`getClientRects` 1). 관리자 문구엔 이 구절이 없다.
- 관리자 동사 `생성할 수 없습니다` → `만들 수 없습니다`. `report.html` 전체 `생성할` **0건**(`생성된` 은 면책의 다른 낱말).
- 관리자 `물건 상세` → 공개와 같은 `<a href="/vehicle/{{ v.id }}" onclick="return _reportBack(event)" …>`. 페이지 내 `_reportBack` 사용처는 여전히 정확히 2(버튼+링크 — 분기당 하나만 렌더).

## 2. 테스트 — 전/후 · 반증

| | 결과 | 조건 |
|---|---|---|
| 전 | **1248 passed** | 편집 전 작업트리(HEAD `9d1c41c`), `NC_NO_SCHEDULER=1 NC_NO_BACKGROUND=1`, 7분 12초. 지시서의 1241 과 다르다 — 그 사이 다른 담당 테스트 7건이 들어온 것(사실: 내 편집 전 측정) |
| 후 | **1267 passed, 0 failed** (= 1248 + 신규 19, 딱 맞다) | 같은 조건, 편집 완료 후, 8분 26초 — 캡처 서버와 동시에 돌렸다(CPU 경합만, DB 는 각자 tmp/스크래치) |

신규 = `test_panel55…` +8 함수(파라미터 포함 **+16 케이스**: 4+2+4+2+1+1+1+1, 파일 9→25) · `test_panel56…` +3 함수(13→16) = **+19 케이스** → 두 파일 41 passed(단독 실행 실측). 기대 후 수치 = 1248 + 19 = 1267. 지시서 검증 항목 대응:
⑴ `test_stop_tone_copy_leads_with_the_verdict`(침수·시동불가 × 공개·관리자 4케이스: 첫 문장 `{label} 판정입니다.` 시작 + `is-stop` + 알약 의미색 없음) + `test_stop_fixtures_actually_hit_stop_tone`(공허 통과 방지: `bid_state` 가 실제로 blocked/stop · lowconf/stop) ·
⑵ `test_wait_tone_copy_is_unchanged`(공개·관리자 첫 문장 바이트 동일, `판정입니다`·`is-stop`·`리포트도` 부재) ·
⑶ `test_judge_class_only_in_the_unpriced_branch`(priced 렌더 0 · nomed 3종 각 1 · 원문 1, `{% elif bidst %}` 세그먼트 안) + `test_judge_badge_rules_and_base_badge_untouched` ·
⑷ `test_priced_masthead_unchanged_byte_for_byte` — 기존 md5 상수 그대로 통과 ·
⑸ `test_copy_never_says_generate`(wait·stop × 공개·관리자) + `test_appraisal_pair_never_leaves_an_orphan` + `test_admin_detail_link_uses_the_same_back_handler` + `test_stop_rail_rule_sits_beside_the_verdict_rule` + `test_stop_lead_uses_the_label_verbatim_from_the_same_source`.

기존 테스트 갱신(처방이 요구한 것): `_PUBLIC_LINE not in admin note` → 동사가 통일됐으니 공개 **전용** 조각(`_PUBLIC_TAIL`·nowrap 쌍)으로; `<div class="badge">` 카운트 → `badge judge`; 원문 검사의 그릇 앵커 `_BOX_OPEN_SRC`(그릇을 먼저 찾고 그 뒤에서 앵커 — 내가 더한 주석에도 같은 문장이 있어 앵커부터 찾으면 주석에 걸렸다, 첫 실행에서 실제로 걸렸다).

**반증(확인된 사실, 바이트 백업 → 복원 → md5 대조):** `{% if _stop0 %}…{% elif is_admin(request) %}` 를 `{% if is_admin(request) %}` 로, `sec-empty{{ ' is-stop' if _stop0 }}` 를 `sec-empty` 로 지움(md5 `e77546b8`) → 두 파일 **6 failed / 35 passed** — `test_stop_tone_copy_leads_with_the_verdict` 4 + `test_stop_lead_uses_…` + `test_report_template_gates_…`. `cp` 복원 후 md5 **`740036ab`**(편집본과 동일), CRLF 1678 / bare LF 0.

## 3. 캡처 — `screenshots/panel56c/` (HEAD `071d8ce`, 작업트리 report.html md5 `740036ab`)

조건: 로컬 uvicorn `127.0.0.1:8796` · `DATA_DIR`=스크래치 사본(auction.db + 3물건 폴더) · `NC_NO_SCHEDULER=1`·`NC_NO_BACKGROUND=1` · 공개=`X-Forwarded-For: 203.0.113.9` · 관리자=헤더 없음(같은 서버에서 관리자 렌더에만 `[다시 분석]` 이 나오는 것으로 자가검증) · Chromium DPR2 · 높이 844 · 큰글씨=`localStorage naechaget:large=1`. **14파일 md5 전부 상이**(처음 찍은 `-copy.png` 7장은 뷰포트 샷과 md5 가 같아서 — 문구 박스가 첫 화면 안(390: y 595~699)이라 스크롤이 0 — **지웠다**).

| 파일 | md5 | 알약(서체·w×h·줄) | 문구 박스(w×h·줄) · 레일 | 첫 문장 |
|---|---|---|---|---|
| `public-flood-390` | `147eec17` | sans 12/600 · 156×33 · 1 | 350×104 · 3줄 · **rgb(190,42,47)** | `침수·전손 의심 — 입찰 보류 판정입니다.` |
| `public-flood-360` | `aa44adda` | 156×33 · 1 | 320×126 · 4줄 · 빨강 | 같음 |
| `public-flood-320` | `cf1d238c` | 156×33 · 1(브랜드 아래 줄) | 280×126 · 4줄 · 빨강 | 같음 |
| `admin-flood-390` | `bd82f005` | 156×33 · 1 | 350×104 · 3줄 · 빨강 | 같음 + `아직`·`[다시 분석]`·링크 밑줄 |
| `public-pastdue-390` | `fd99d141` | 179×33 · 1 | 350×104 · 3줄 · rgb(201,209,221) | `시세가 산정되지 않아 종합 리포트를 만들 수 없습니다.`(불변) |
| `public-pastdue-320-large` | `14bcee82` | **14px · 205×36 · 1줄** | 280×147 · 4줄(15.5px) · 회색 | 불변 |
| `public-priced-390` | `d617c9c9` | `badge judge` 없음, mono 점수 배지 + `높음` | (본문 01~12) | — |

`*-full.png` 7장 별도(같은 이름 `-full`). `measure.json` 에 전 항목 원값.

- 기계 측정: flood·pastdue·priced × 320·360·390·430·768·1440 + flood 큰글씨 320 = **19/19 가로 스크롤 0 · 화면 밖 요소 0**(`scrollWidth == innerWidth`). (전 회차에 있던 priced 320/360 의 간헐 +91px 은 이번 1회 측정에선 안 나왔다 — 재현성 미확인, 이 변경과 무관은 전 회차에서 이미 확인.)
- 눈으로 본 것(390·320 flood, 390 admin, 390 pastdue, 320 큰글씨 pastdue, 390 priced 를 Read): 빨간 레일과 첫 문장이 보인다. 알약은 전 회차 `panel55b/public-flood-390.png`(mono, 어절 벌어짐)와 나란히 보면 상세 h1 칩과 같은 sans 판정문으로 읽힌다. 마스트헤드 색은 stop/wait 동일(의도). 390 에서 `감정가·최저매각가 등 법원` 이 한 줄.
- **큰글씨 × 320 × pastdue — 접히지 않는다(확인된 사실).** 두 검수자의 "≈231~236px > 215 라 접힐 것"은 **mono 13px** 기준 추정이었고, 처방 ②로 서체가 sans 14px/600 이 되면서 라벨 폭이 **205px**(알약 x 52~257, 마스트헤드 안쪽 268 이내)로 줄어 한 줄. flood 라벨은 178px. `ui.judge_label` 의 ' — ' 접기는 이 조합에서도 발동하지 않았다(`innerLines 1`). 접힐 조건은 이제 "sans 14px 에서 라벨 ≥ ~216px" — 현재 7종 라벨 중 없음(추정: 최장이 pastdue 205px).

## 4. 못 한 것 · 확신 없는 것

- **라이브 미검증(사실)** — 로컬 사본 렌더. 배포 뒤 `/vehicle/2025타경53062_1/report`(침수) 를 XFF 로 봐야 한다. 배포는 지시서대로 하지 않았다.
- **`시동·운행 불가 — 판정 보류 판정입니다.`(사실)** — 라벨 원문 + `판정입니다` 라 `판정` 이 두 번 온다(13건). 지시서가 "라벨 원문 그대로, 새 낱말 없음"이라 그대로 뒀다. 어색하면 라벨 쪽(`service.py`, 범위 밖) 또는 접미 문구를 검수자가 정할 일 — 내 재량으로 바꾸지 않았다. 렌더는 `tests/test_panel55…::test_stop_tone_copy_leads_with_the_verdict[…nomed_nostart]` 가 잡고 있다.
- **관리자 stop 문장의 `도`(판단)** — §1 ① 참조. 한 글자라 되돌리기 쉽다.
- **`-copy.png` 삭제(사실)** — 뷰포트 샷과 같은 파일이라 지웠다. 문구 박스는 모든 캡처 폭에서 첫 화면 안에 있다(390: y 595 / 320: y 673 / 320 큰글씨: y 706 — 844 안).
- **전 회차 priced 320/360 간헐 +91px(미검증)** — 이번엔 0/6 이었으나 1회뿐. 전 회차가 "변경 전 서버에서도 같은 빈도"로 무관을 확인했다.
- **후 테스트 수의 타 담당 포함 여부** — 전 1248 은 지시서의 1241 보다 7 많다. 내 편집 **전** 에 잰 값이라 그 7은 남의 것(사실). 후 수치와의 차이 = 내 신규 케이스 수(19)여야 한다(§2).
- **줄끝(사실)** — `git status` 가 두 테스트 파일에 "LF will be replaced by CRLF the next time Git touches it" 경고를 낸다. 두 파일은 내가 손대기 **전**에도 LF(CRLF 0 / bare LF 163·169 실측)였고 편집도 LF 로 유지했다 — autocrlf 설정과 저장 상태의 기존 어긋남이지 이번 변경이 만든 것이 아니다. `report.html` 은 CRLF 1678 / bare LF 0 유지.
