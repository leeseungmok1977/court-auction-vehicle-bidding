# 스토어 제출 4·5번(상세) 교체 촬영 + 도구 선결 과제 — 2026-09-24 (3차)

> 작업 지시서 2026-09-24-03 / qa-engineer
> 촬영 대상 서버 `https://naechaget.co.kr` · 논리폭 540 × DPR 2 = 1080×1920 · 공개 사용자(`X-Forwarded-For: 203.0.113.9`)
> **확인된 사실 / 추정을 절마다 구분해 적는다.** 커밋하지 않았다(Steward 가 재현 뒤 한다).

---

## 0. 한 줄 결론

`07_detail`·`08_detail_lower` 를 **제네시스 G90(`/vehicle/2025타경34553_1`)** 으로 교체 촬영했다.
후보 6건의 첫 사진을 직접 열어 골랐고, 두 장 모두 캡션이 약속한 요소(감정가·유찰횟수·당시 출시가 /
입찰 상한선 값 블록 + 판정 칩)가 프레임 안에 있음을 도구가 좌표로 확인했다. 나머지 8장(제출 6 + 비제출 2)은
md5 가 바뀌지 않았다. 도구에는 `--hero`·디스크 캐시(`hero_scan.json`)·`--scan` 을 넣었고, 캐시 덕에
**외부 이동 22회**(전 회차 101회)로 끝났다 — 두 번째 조사는 **0회**였다.

---

## 1. 후보 — 아무거나 고르지 않았다 (6건 조사, 3건 이상 요구)

조사 방법: `--scan` 이 목록 2페이지에서 후보 18개를 모아 최대 10개를 열었다. 캡션 조건은
`document.body.innerText` 로, 사진은 캐러셀 1/N(`#heroImg`, 페이지가 이미 내려받은 이미지)을
잘라 `screenshots/store/hero_scan/<물건>.png` 에 두고 **Read 로 직접 열어** 봤다.

⚠ 사진 판단은 **캐러셀이 렌더한 크롭**(`object-cover`, 540×176 논리px)을 본 것이다. 원본 사진은
더 넓을 수 있다 — 하지만 4번에 찍히는 것은 그 크롭이므로, 스토어 관점에서는 그게 맞는 대상이다.

| 후보 | 캡션 조건(감정가·유찰·출시가·상한선) | 첫 사진 판단 근거 | 기일 | 채택/탈락 |
|---|---|---|---|---|
| **G90** `/vehicle/2025타경34553_1` | **넷 다 렌더** (육각 6/6) | ① 검은 차체 전면(그릴·양쪽 헤드램프·후드)이 프레임을 채운다 ② 광택, 낙서·스티커·오염 없음 ③ 우측 끝에 다른 차 일부가 걸리나 조각 수준 | 10-07 | **채택** — 깨끗한 후보 중 기일이 가장 멀다(13일) |
| 아우디 A5 `/vehicle/2026타경50731_1` | 넷 다 렌더 (육각 5/6 — 4·5번에는 무관) | ① 측면~전면 3/4, 차 전체가 프레임 안 ② 광택, 깨끗 ③ 뒤 검은 세단이 작게 걸림 | 09-28 | **차점** — 사진은 가장 좋으나 기일이 4일 남았다 |
| 카니발 `/vehicle/2026타경50344_1` | 넷 다 렌더 (5/6) | ① 흰색 전면 3/4 ② 깨끗(도어에 작은 파란 점 하나) ③ 뒤 제네시스 그릴이 작게 보임 | 09-28 | 탈락 — A5 와 같은 기일, 사진은 A5 가 낫다 |
| 짚 체로키 `/vehicle/2026타경50005_1` | 넷 다 렌더 (5/6) | ① 차 전체가 들어오나 **광각이라 주인공이 작다**(프레임 폭의 1/3) ③ 좌측 흰 트럭이 비슷한 크기 | 09-29 | 탈락 — ③ 경계, 첫인상 약함 |
| GLE350d `/vehicle/2026타경30385_1` | 넷 다 렌더 (6/6) | ② **후드에 먼지·물자국** ③ 우측 은색 차가 바짝 붙어 크게 걸림 | 09-28 | 탈락 — ②③ |
| SM6 `/vehicle/2026타경3364_1` (현행 4·5번) | 넷 다 렌더 (6/6) | ② **보닛에 파란 `SECUT` 낙서**, 차체 오염 | 10-08 | 탈락 — 이번 교체의 원인 |
| 그랜저·BMW 620d·G80·화물 소형 (4건) | **`당시 출시가` 없음**(2건은 상한선도 없음) | 조사 안 함(캡션 탈락) | 08-18(지난 기일) | 탈락 — 리포트는 열지 않았다(요청 절약) |

> ★ 지시서 경고대로 G90·GLE350d 를 "6/6축이라서" 고르지 않았다. 상세 4조건 + 사진 + 기일로
> 다시 판단했고, GLE350d 는 6/6 인데도 사진 때문에 탈락했다.

---

## 2. 8장 전/후 md5 — 바뀐 것은 `07`·`08` 둘뿐

| # | 파일 | 전(작업 시작 07:50) | 후 | 판정 |
|---|---|---|---|---|
| 1 | `06_landing.png` | `f0c3db0d4d16f5db944f5c555c1d853f` | 동일 | 유지 |
| 2 | `01_home.png` | `55f36ece007fb37f94497d609ae4d2a6` | 동일 | 유지 |
| 3 | `02_accuracy.png` | `a4ae5529beab58a6497c4fa092a8d569` | 동일 | 유지 |
| 4 | `07_detail.png` | `2fa84743e8df367114422a350f07ab43` | **`443d52ece69556195c8a315f192c95d8`** | **교체(G90)** |
| 5 | `08_detail_lower.png` | `549ee04648d163378676110ee8352c50` | **`4eda6f5d34ea53961d98d49123046af5`** | **교체(G90)** |
| 6 | `09_report.png` | `a85aa16ea3f4247aca140afd3204795f` | 동일 | 유지(SM6, 오너 결정) |
| 7 | `10_report_lower.png` | `503b97dde514507180c3d7dd07aa9295` | 동일 | 유지(SM6, 오너 결정) |
| 8 | `05_calendar.png` | `f04f88e7c7711c4fd952bac7bd92eb34` | 동일 | 유지 |
| — | `03_review.png` · `04_usepick.png` (비제출) | `675644cd…` · `7268e179…` | 동일 | 손대지 않음 |

- `07`≠`08`(md5 상이). 디렉터리 10장 **중복 0쌍**(`--audit` 합격).
- **재현성**: 같은 물건을 두 번 찍었더니(시간대 표기 수정 후 재촬영) **두 장 모두 md5 가 첫 촬영과 동일**했다.
  도구가 `※ 이전 판과 md5 가 같다` 로 알렸고, 이번엔 그게 정상이다(같은 페이지·같은 프레임).

---

## 3. 규격 · 육안 2가지 — 장별

규격: 8장 전부 **1080×1920**(PIL 실측, `--audit`). 비율 1.78 ≤ 2.

| 장 | 파일명 = 실제 화면? | 번호판 | VIN | 미출시 기능 표시 |
|---|---|---|---|---|
| `07_detail.png` | 상세 **상단**(제목·칩·캐러셀 1/11·물건 정보) — 맞음 | 전면 번호판 자리가 찍혔으나 **빈 브래킷**(4배 확대 크롭으로 확인: 나사 구멍만 있는 검은 플라스틱 판, 글자 없음). 판독 가능한 번호 없음 | 없음(프레임 안 필드: 주행거리·배기량·감정가·당시 출시가·최저매각가·유찰횟수·다음 기일·매각 일시·장소·보관장소) | 없음(`FORBIDDEN`·`RETIRED` 검사 통과) |
| `08_detail_lower.png` | 상세 **하단**(사고판정·낙찰 절차 아코디언·AI 카드 전체) — 맞음 | 사진 없음 | 없음 | 없음 |
| 나머지 6장 | 전 회차와 동일 파일 | 전 회차 보고 그대로(판독 가능 번호판 없음) | — | — |

> 오너 방침대로 **가리지 않았다.** 이 표는 "찍혔는지 알고 있으라"에 대한 답이다.

**프레임 안 확인(도구 좌표 로그, 뷰포트 960·고정바 위 960):**
- `07`: `감정가` 587~607 · `당시 출시가` 617~637 · `유찰횟수` 685~705 — 셋 다 한 프레임. 판정 칩 `지금 사면 이득` 은 제목 옆에 그려졌다(육안).
- `08`: `AI 낙찰 예측 분석` 302~336 · `여기까지만` 476~490 · `산정 기준` 786~799 — 카드가 통째로 들어왔고, 같은 행 왼쪽 판정 칩 `지금 사면 이득`, 상한선 **27,600,000원**, 예상낙찰가 19,200,000원(이 유형 ±9.6%), 시세 신뢰도 65/100, 산정 기준(×1.18 · 유찰 2회 116건) 이 육안으로 확인된다.

### 육안 관찰 — 결함 제기가 아니라 알림 (판단은 Steward·디자인)
- `07` 물건 정보에 **`배기량 — cc`** 가 보인다(G90 의 배기량 자료 없음). 스토어 그림에 빈 값이 하나 들어간다. 다른 후보의 배기량 유무는 **확인하지 않았다**(추가 요청 필요) — 바꾸라는 뜻이 아니라 알고 있으라는 뜻이다.
- `08` 상단에 `사고판정 [사고] 사고이력있음`(빨강 칩)이 들어온다. 정직한 화면이고 캡션과 무관하다.
- 4·5번(G90)과 6·7번(SM6)이 **다른 차**가 됐다 — 오너 결정이다. 스토어에서 나란히 걸리면 4장의 이야기가 두 차로 갈린다는 점은 알고 있어야 한다.

---

## 4. `HEAD.json` 발췌 — `07`·`08`

촬영 시작 시점에 내가 읽은 HEAD: **`b641f931a563d7f725817950fb7fe5f3be2d6c51`**
(지시서에는 해시가 없었고, 세션 시작 때 읽은 `11c18ac` 는 07:55 에 `b641f93`(docs 전용: `STORE_LISTING.md`·`backlog.md`)으로 **이미 움직여 있었다** — 지시서 경고 그대로다.)

```json
"07_detail.png":       {"commit": "b641f931a563d7f725817950fb7fe5f3be2d6c51", "at": "2026-09-24T08:03:02+09:00", "md5": "443d52ece69556195c8a315f192c95d8", "size": "1080x1920"}
"08_detail_lower.png": {"commit": "b641f931a563d7f725817950fb7fe5f3be2d6c51", "at": "2026-09-24T08:03:12+09:00", "md5": "4eda6f5d34ea53961d98d49123046af5", "size": "1080x1920"}
```

- 한 줄 `HEAD` 는 **`931a134` 그대로**다 — 이번엔 두 장만 찍었으므로 갱신하지 않는 것이 맞다(§5 의 `stamp_head` 수정).
  `--audit` 은 세 커밋(`7b78b2e`·`931a134`·`b641f93`)이 섞였다고 경고한다. 그게 사실이다.
- ⚠ 이 커밋은 **로컬 저장소의 HEAD** 이고, 찍힌 픽셀은 라이브 서버가 그린 것이다. 라이브 배포 커밋은 여전히 모른다(전 회차 §5 한계 그대로). 게다가 촬영 시점의 도구 자체는 **커밋되지 않은 수정본**이다.
- ⚠ **촬영 뒤에도 HEAD 가 두 번 더 움직였다**: `153cda6`(오버레이 도구 `tools/store_overlay.py`, PANEL-54) → `13512ea`(08:08, `docs/STORE_LISTING.md`). 보고서를 닫는 시점의 HEAD 는 `13512ea` 다. `HEAD.json` 의 `b641f93` 은 **찍은 순간의 값**이라 그대로 참이다 — 장별 기록이 왜 필요한지가 한 회차 안에서 또 증명됐다. 두 커밋 모두 내 파일(`capture_store_shots.py`·`test_store_shot_tool.py`)은 건드리지 않았다.

---

## 5. 도구 변경 — 함수명·인자명으로 (줄 번호 금지)

`tools/capture_store_shots.py`

| 이름 | 하는 일 |
|---|---|
| `--hero HREF` / `set_hero()` | 대상 물건을 직접 지정, 탐색 생략. `/vehicle/<id>` 꼴만 받는다 — **첫 실행에서 실제로 잡았다**: Git Bash 가 `/vehicle/…` 을 `C:/Program Files/Git/vehicle/…` 로 바꿨다(MSYS 경로 변환). `MSYS_NO_PATHCONV=1` 로 우회, 독스트링에 적어 뒀다. 지정이 건너뛰는 것은 '한 물건이 네 캡션을 전부 떠받친다'는 묶음 조건뿐이고, 각 장의 캡션 조건은 촬영 함수가 각자 검사한다 |
| `HERO_CACHE` = `hero_scan.json` / `HERO_CACHE_HOURS` = 24 / `load_hero_cache()` / `save_hero_cache()` / `_fresh()` | 조사 결과를 `screenshots/store/hero_scan.json` 에 **항목마다 조사 시각·커밋과 함께** 남긴다. 24시간 안이면 다시 열지 않는다. 후보 하나 볼 때마다 저장(중간 예외에도 본 것은 남는다) |
| `candidate_hrefs()` | 목록 2페이지 → 후보 href. 캐시가 신선하면 **목록 페이지도 열지 않는다** |
| `survey_one()` | 후보 하나의 상세·리포트 사실. 캐시 우선. **상세에서 탈락하면 리포트를 열지 않는다**(`report: null`) — 이번 조사에서 4회 절약 |
| `hero_ok(d, r=None)` | `r=None` = 리포트 미조사 → 상세 4조건만 판정 |
| `_save_first_photo()` / `HERO_THUMB_DIR` | 캐러셀 1/N(`#heroImg`)을 `hero_scan/<물건>.png` 로 잘라 둔다. 이미 내려받은 이미지라 **요청이 늘지 않는다**. 미관 판정은 사람이 한다 |
| `--scan` / `scan()` | 찍지 않고 조사만. 네 캡션 전부 합격과 별개로 **상세(4·5번) 조건 합격 목록**을 따로 낸다 — 4·5 와 6·7 을 다른 물건으로 찍는 경우를 위해. 지시서 항목 밖이지만 (3)의 캐시를 채우고 후보를 고르는 유일한 경로라 넣었다 |
| `pick_hero_vehicle()` | 위 캐시·조사 함수로 재구성. 선정 규칙(합격 중 기일 최원)은 그대로 |
| `_MOVES` / `polite_goto()` / `main()` | 외부 페이지 이동을 세어 회차 끝에 출력(전 회차는 손으로 셌다) |
| `main()` → `stamp_head(full=not failed and len(todo) == len(SHOTS))` | **버그 수정**: 두 장만 찍어도 `full=not failed` 가 참이라 한 줄 `HEAD` 가 현재 커밋으로 덮였다(나머지 여섯 장은 옛 커밋인데). 여덟 장 전부 찍은 회차에서만 갱신. `stamp_head()` 의 안내 문구도 그 두 사유를 다 말하게 고쳤다 |
| `_now_iso()` | `astimezone()` 으로 `+09:00` 병기 — 기존 `HEAD.json` 항목과 표기를 맞춘다. 첫 촬영분은 시간대가 빠져 있어 **재촬영으로 다시 찍었다**(md5 동일). `_fresh()` 는 시간대 유무가 섞여도 터지지 않는다 |

범위 밖 손대지 않은 것: `tools/store_overlay.py`(오버레이 별도 지시서 — 내 작업 중 미추적 파일로 있다가 `153cda6` 으로 커밋됨) · `docs/STORE_LISTING.md` · `docs/backlog.md`.

---

## 6. 외부 이동 횟수 — 전 회차 101회 → 이번 22회

| 무엇 | 이동 |
|---|---|
| `--scan` 1회차 (목록 2 + 상세 10 + 리포트 6) | 18 |
| `--hero … --detail --slide5` 1회차 | 2 |
| `--hero … --detail --slide5` 2회차 (시간대 표기 재촬영) | 2 |
| `--scan` 2회차 (전부 캐시) | **0** |
| `--hero` 첫 시도(경로 변환으로 거부, 브라우저 뜨기 전) | 0 |
| **합계** | **22** |

- 전 회차 같은 조사가 22회였는데 18회로 줄었다(상세 탈락 4건의 리포트를 안 열었다). 세 번 반복하면 전엔 66회, 이제 **18 + 0 + 0**.
- C.4: 이동마다 7초 대기. **403/429/CAPTCHA·비정상 응답 0건.** 중단 사유 없음. 조사 상한 `HERO_SCAN_MAX=10` 유지.
- curl 사전 확인은 이번엔 하지 않았다(0회).

---

## 7. 테스트 게이트

```
작업 전  python -m pytest -q  →  1212 passed  (112.38s)
작업 후  python -m pytest -q  →  1226 passed  (138.77s)   실패 0
```

- `tests/test_store_shot_tool.py` **14 → 21건(+7)**. 줄어든 테스트 없음.
- ⚠ 나머지 **+7 은 내 것이 아니다**: 작업 중 다른 담당의 미커밋 파일이 트리에 들어왔다 —
  `tests/test_panel55_report_nomarket_copy.py`(신규, 7건) · `tests/test_report_sections.py`(단언 1줄 수정) ·
  `web/templates/report.html` · `tools/store_overlay.py`. **손대지 않았고**, 게이트 수치에는 포함돼 있다.

추가한 7건(모두 `ast`/소스 패턴): ① `--hero` 인자 + `main()`→`set_hero()` + href 꼴 검사 ② 캐시 파일·유효시간·조사 시각(`"at": _now_iso()`) 기록 ③ **신선한 캐시면 `polite_goto` 전에 `_fresh()` 를 보고 `return`** ④ 상세 탈락 후보의 리포트 미조사 + `hero_ok(d, None)` ⑤ 한 줄 HEAD 는 8장 전부 찍은 회차만 ⑥ 이동 횟수 집계 ⑦ `--scan` 이 상세 조건 합격을 따로 셈.

**변이 시험 8종 — 전부 잡혔다**(각각 해당 테스트만 FAIL, 나머지 20 통과):

| 변이 | 빨간불 |
|---|---|
| M1a `survey_one` 캐시 블록 삭제(캐시 무시) | ③ |
| M1b `survey_one` 캐시 보고도 `return`→`pass`(검사만 하고 진행) | ③ |
| M2 `candidate_hrefs` 캐시 블록 삭제 | ③ |
| M3 상세 탈락해도 리포트를 연다(`if True:`) | ④ |
| M4 `stamp_head(full=not failed)` 로 되돌림 | ⑤ |
| M5 `--hero` 인자 제거 | ① + 독스트링·argparse 대조 테스트(2건) |
| M6 `_MOVES += 1` 제거 | ⑥ |
| M7 조사 시각을 `""` 로 | ② |

> 변이 시험 중 함정 1건: 복원을 `write_text` 로 했더니 **LF 가 CRLF 로 바뀌어** `diff -q` 가 "다르다"고 했다. 내용은 같았고, 바이트 백업으로 되돌려 `git ls-files --eol` 이 `w/lf` 임을 확인했다.

---

## 8. 못 한 것 · 확신 없는 것

### 확인된 사실
- `07`·`08` 은 G90 으로 교체됐고, 캡션 요소가 프레임 안에 있음을 좌표와 육안으로 확인했다.
- 다른 8장(제출 6 + 비제출 2)의 md5 는 작업 시작 시점과 같다.
- `07` 의 전면 번호판 자리는 빈 브래킷이다(4배 확대). `08` 에는 사진이 없다. 두 장 모두 VIN 없음.
- 캐시가 실제로 동작한다: 두 번째 `--scan` 외부 이동 0회.
- 이번 회차 외부 이동 합계 22회. 차단·이상 응답 0건.
- HEAD 는 세션 시작 후 `11c18ac`→`b641f93` 으로 움직였다(07:55, docs 전용).

### 추정 / 미검증 / 못 한 것
- **캐시 유효시간 24시간은 내 판단**이다("유찰·기일·시세는 하루 단위로 움직인다"는 가정). 데이터로 검증하지 않았다. 월 1회 재촬영 사이에 후보를 다시 여는 건 어차피 필요하므로 보수적으로 잡았다.
- 사진 판단은 **캐러셀 크롭**을 본 것이다. 원본 전체는 열지 않았다(추가 요청이 되므로). 4번에 찍히는 것은 크롭이라 스토어 판단으로는 충분하다고 본다 — 추정.
- `배기량 — cc` 가 다른 후보에서는 어떤지 확인하지 않았다.
- 라이브에 배포된 커밋은 여전히 모른다(전 회차 §5 한계).
- 후보 18개 중 **10개만** 열었다(`HERO_SCAN_MAX`). 나머지 8개에 더 좋은 사진이 있을 가능성은 배제하지 못한다 — 추정.
- Play 콘솔에 올라간 그림은 볼 수 없다(오너 권한).
- `hero_scan/*.png` 10장과 `hero_scan.json` 은 `screenshots/` 아래라 **git 에 안 올라간다**(.gitignore). 이 PC 에만 있다.
- 6·7번은 지시대로 다시 찍지 않았다. 그 SM6 리포트가 지금 라이브에서 또 움직였는지는 **재확인하지 않았다** — 미검증.
