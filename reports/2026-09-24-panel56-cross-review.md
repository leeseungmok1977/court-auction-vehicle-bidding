# PANEL-56 마스트헤드 판정 배지 — 교차검수 (지시서 2026-09-24-12A · 12B)

- 판정 표시 변경이라 CLAUDE.md 규칙대로 **두 자리**가 봤다: `app-design-expert`(-12A) · `design-critic`(-12B). 둘 다 파일을 쓰지 못하는 규칙이라 **Steward 가 옮겨 적었다.** 끝에 [Steward 재현]과 [처방 결정].
- 대상: `screenshots/panel55b/` 30장(기준 `d019cc4`), `report.html`·`detail.html`·`_macros.html`·`service.py`·`app.py`. 커밋 `ca04ad8` + 2회차(지시서 -09).

## 공통 결론 — **배포 가능**
- before: 시세 없는 물건에 명사 없는 `낮음`. after: `매각 종료` / `시세 신뢰도 낮음 — 판정 보류` / `지난 기일 — 다음 기일 공고 대기` / `침수·전손 의심 — 입찰 보류` — 상세 h1 칩과 같은 말. 431건 전건이 틀린 말에서 맞는 말로.
- 회귀 없음: 시세 있는 물건(priced-390) before/after 마스트헤드가 발행시각만 다르고 픽셀 동일.
- `app-design-expert` 라이브 확인: 프로덕션은 **아직 변경 전**(옛 문구 `아직 … 생성할 수 없습니다` 가 그대로).

## 두 자리가 같이 잡은 것 — ★ stop 톤 판정이 리포트에서 무게를 전부 잃는다
- 확인(둘 다): 시세 없는 분기의 라벨 7종 중 wait 톤 5종, **stop 톤 2종**(`침수·전손 의심 — 입찰 보류` 6건 · `시동·운행 불가 — 판정 보류` 13건 = 19건/431). 상세 h1 칩은 stop=rose-50/700 인데 리포트 `.badge` 는 톤 무관 흰 30% 외곽선 10.5px mono. `public-flood-390.png` 와 `public-pastdue-390.png` 의 마스트헤드가 색·굵기·크기 전부 동일 — **한쪽은 "입찰하지 마라", 한쪽은 "기다려라"인데 화면은 같은 말.**
- 확인(둘 다): 마스트헤드에 `is-stop` 클래스는 붙는데(`_tone0 == 'stop'`) 그 유일한 규칙이 `.masthead.is-stop .badge b{color:#C9D4E8}` — 이 분기 배지에 `<b>` 가 없어 **효과 0**.
- 왜 문제: 이 페이지는 본문(핵심 결론·리스크)이 없어 stop 물건에서 이 알약이 **유일한 위험 신호**다. 침수차에 대해 페이지가 하는 말이 "시세만 없다"로 읽힌다. PDF 로 저장하면 경고 없는 문서가 남는다.
- **처방이 갈린다**:
  - `design-critic`: **마스트헤드에 남색 위 빨강을 넣지 말 것** — 리포트는 남색 위에서 의미색을 일부러 중화하는 관례(`.masthead.is-stop .badge b` 가 빨강이 아니라 연한 청회색인 것이 그 증거). 대신 **본문 첫 문장으로 판정을 올린다**: stop 이면 `.sec-empty` 공개 문구를 `<b>침수·전손 의심 — 입찰 보류 판정입니다.</b> 시세가 산정되지 않아 종합 리포트도 만들 수 없습니다. …`(라벨은 `bidst.label` 원문, "도" 한 글자로 두 사실의 순서). 선택: `.sec-empty.is-stop{border-left-color:var(--red)}` 1줄 — `.verdict.is-stop{border-top-color:var(--red)}` 와 같은 꼴, 흰 카드 위 색 레일은 리포트 안 관례.
  - `app-design-expert`: 기존 `bid_state` 톤을 **알약에 연장** — `.masthead.is-stop .badge.judge{border-color:rgba(253,164,175,.85);color:#FDA4AF;background:rgba(225,29,72,.16)}`. 431건 중 19건(4.4%)에만 색이 붙어 남발 아님.

## `app-design-expert` 고칠 것
1. **판정 알약이 메타데이터 서체·크기라 '태그'로 읽힌다** — `.badge` 는 원래 `시세 신뢰도 81/100` 숫자용(mono 10.5px `.06em`). 한글 판정문을 고정폭으로 찍으니 어절 사이가 벌어져 타자기 태그처럼 보이고, 바로 아래 eyebrow(mono 11.5px 흰 60%)와 한 묶음으로 묶인다. → **시세 없는 분기의 알약만** `class="badge judge"` 로 sans 12px/600, letter-spacing 0, padding 6px 12px(상세 h1 칩과 같은 12/600). 색·외곽선 그대로. `nc-large` 14px. 시세 있는 물건은 그 분기를 안 타므로 md5 테스트 그대로 통과.
2. (위 stop 톤)
3. **알약이 둘째 줄로 내려오면 브랜드 락업의 태그라인으로 묶인다** — 320 만이 아니라 **390·360 에서도** 긴 라벨 3종은 둘째 줄(x=52, 브랜드마크와 왼쪽 끝이 같다). 라벨 분포상 390 에서 둘째 줄이 되는 물건 ≥312/431(72%). "이 차의 판정"이 "이 앱의 슬로건" 자리에 앉는다. → elif 분기의 판정을 `.badges` 가 아니라 **차량명 바로 아래** 한 줄로 옮긴다(`.mh-judge`). 시세 있는 물건의 `.badges` 는 손대지 않는다. (`design-critic` 은 같은 현상을 "priced 의 신뢰도 배지도 같은 방식으로 내려가므로 `.badges` 원래 거동 — 고치지 말 것"으로 봤다 → **갈림**.)
- (c) `.sec-empty` 위계 — **통과.** (d) `A` 브랜드마크 — 여전하나 악화되지 않음, 차단 아님.
- 확인 필요: 큰글씨 × 320 × 191px 라벨 미캡처(2줄로 꺾일 것 — 추정) · 관리자 분기 `물건 상세` 는 링크가 아님(의도?) · 범위 밖: 푸터 "유효기간 매각기일까지" 가 기일 지난 물건에도 찍힌다.

## `design-critic` 고칠 것
1. (위 stop 톤 — 본문 첫 문장 처방)
2. **면책의 빨간 굵은 문장이 "권장가는 무효입니다"인데 이 분기에는 권장가가 없다** — 페이지에서 채도가 가장 높은 텍스트가 존재하지 않는 것을 무효로 선언한다. 같은 문단이 MAE 절을 `{% if expected and expected.acc %}` 로 게이트하니 빨간 `<b>` 도 같은 조건으로 게이트하거나 시세 없는 분기에서 굵기·빨강을 뺄 것 — **면책 문구라 오너 판단 항목.** (`app-design-expert` 도 같은 nit 를 적었다.)
3. **390 줄끝 `감정가·` 고아**(공개 4장 전부, 360·320 은 안 갈림) → `<span style="white-space:nowrap">감정가·최저매각가</span>`. **관리자 문구 동사 이탈** — 관리자 `생성할 수 없습니다` vs 공개 `만들 수 없습니다`, 관리자 `물건 상세` 는 링크가 아닌데 거기 가서 `[다시 분석]` 을 누르라는 사람이 관리자다 → 동사 통일 + 같은 `_reportBack` 링크.
- (b) ` — ` 뒤 접힘: 320 캡처 3장 모두 1줄, 접힘 없음. **큰글씨(13px) × 320 × pastdue 만** 접힐 것(≈231px > 215, 추정). 접혀도 뜻을 잃지 않는다 — `judge_label` 은 자르는 게 아니라 두 `nowrap` span 사이에서만 줄을 바꾸고 1행 끝 대시가 이어짐을 말한다. 권고: 그 한 장을 찍어 추정을 확인으로.
- 참고(허용): 긴 라벨이 둘째 행으로 내려가는 것은 `.badges` 원래 거동 — 고치지 말 것.

## 잘된 점 (둘 다)
- 같은 물건·두 화면·한 말 — 판정 소스가 하나가 됐다. '목록·상세·리포트가 다른 말' 계열의 마지막 자리.
- 새 CSS 없이 위계를 올렸다(`.sec-empty` 재사용). `아직` 을 빼고 관리자에겐 남긴 구분. 해당 없음에 빨강·앰버를 안 쓴 것. 시세 있는 물건 픽셀 불변. `물건 상세` 링크가 `_reportBack` 을 타 뒤로가기 동선이 갈리지 않음.

---

## [Steward 재현 — ORG §2.3]
| 주장 | 결과 |
|---|---|
| `is-stop` 훅은 있고 효과 0 | **확인** — `report.html:844` `masthead{{ ' is-stop' if _tone0 == 'stop' }}`, 규칙은 `:115` `.masthead.is-stop .badge b{color:#C9D4E8}` 뿐(연한 청회색 — 빨강이 아니다). `:113` `.verdict.is-stop{border-top-color:var(--red)}` 는 본문 카드용 |
| `.badge` 가 mono 10.5px | **확인** — `:99` |
| stop 톤 두 라벨 | **확인** — `service.py` `"침수·전손 의심 — 입찰 보류", "stop"` · `"시동·운행 불가 — 판정 보류", "stop"` |
| flood 와 pastdue 마스트헤드가 같은 알약 | **확인** — `public-flood-390.png` 를 열어 봄. 본문은 "시세가 산정되지 않아…" 뿐 |
| 관리자 동사 이탈 | **확인** — `생성할 수 없습니다` 1 · `만들 수 없습니다` 1 |

## [처방 결정 — 갈린 곳은 근거가 강한 쪽]
- **stop 톤**: `design-critic` 안을 택한다 — 마스트헤드의 유일한 `is-stop` 규칙이 **빨강이 아니라 청회색으로 중화**하고 있어 "남색 위 의미색 중화"가 설계 의도로 읽히고, 본문 첫 문장은 PDF 에도 남는다. `.sec-empty.is-stop` 왼쪽 레일 빨강은 `.verdict.is-stop` 과 같은 꼴이라 함께 넣는다. 알약에 rose 는 넣지 않는다.
- **알약 서체**(`app-design-expert` 1): 채택 — `badge judge` 로 그 분기만 sans 12/600. `design-critic` 도 mono 자간이 "코드/메타" 인상을 보탠다고 같은 관찰.
- **알약 위치 이동**(`app-design-expert` 3): **보류 → PANEL-57** — 두 자리가 갈렸고, 서체·굵기 처방으로 brand-sub 와는 구분된다. 작게 먼저.
- **면책 빨간 문장 게이트**: **오너 판단**(면책 문구). 
- **`감정가·` nowrap · 동사 통일 · 관리자 링크**: 채택.
- **큰글씨 × 320 × pastdue** 캡처: 검증 항목으로.
- 처리: 지시서 -13(`frontend-engineer`).
