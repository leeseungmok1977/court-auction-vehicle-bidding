# 스토어 제출 스크린샷 재촬영 — 2026-09-24

> 작업 지시서 2026-09-24-01 / qa-engineer
> **기준 커밋 `7b78b2e0b68b310d4a1dc76c5e941a6f6a1b9818`** (= `screenshots/store/HEAD` 에 기록)
> 촬영 대상 서버 `https://naechaget.co.kr` (2026-09-24 01:49) · 논리폭 540 × DPR 2
> 공개 사용자 화면(`X-Forwarded-For: 203.0.113.9`)

---

## 1. 무엇을 다시 찍었고, 무엇을 안 찍었나

### 다시 찍은 3장 (확인된 사실)

| 파일 | 왜 다시 찍었나 (캡처 → 라이브) |
|---|---|
| `02_accuracy.png` | 라벨 `평균 오차`→`전체 평균 오차` · ±9.3%→**±9.5%** · 95%→**94%** · 249건→**275건** |
| `01_home.png` | 히어로 문구 `실제 낙찰 249건과 대조 · 평균 오차 ±9.3%` → `실제 낙찰 275건과 대조 · 전체 평균 오차 ±9.5%` |
| `06_landing.png` | 증명 스트립 라벨 `예상 오차 (정직 공개)`→**`전체 평균 오차`** · ±9.3%→±9.5% · 낙찰이력 413→**444** · 대조 249→**275** · 분석중 1278→**1297** |

라이브 값은 `naechaget.co.kr` 과 `naechaget.duckdns.org` **두 호스트에서 각각 조회해 일치**함을
확인했다(±9.5 / 94 / 61 / 275 — 같은 값). Steward 의 대조표와도 일치한다.

### 안 찍은 5장과 이유

| 파일 | 조치 | 이유 |
|---|---|---|
| `05_calendar.png` | 안 찍음 | 화면에 `평균 오차`·옛 적중률 수치 **없음**. 지시서 범위 밖. (아래 §4 주의 1건) |
| `07_detail.png` | 안 찍음 | 화면에 `평균 오차`·적중률 수치 **없음**. (아래 §4 주의) |
| `08_detail_lower.png` | 안 찍음 | 전역 `평균 오차` 아님 — `이 유형(수입, 61건) 실측 오차 ±10.7%` 로 **현행 템플릿 어휘와 일치**. (아래 §4 주의) |
| `09_report.png` | **안 찍음 — 그러나 낡았다(§4 ★)** | 화면에 `실측 평균오차 ±9.3%` 가 박혀 있는데 이 어휘는 **현재 코드에 존재하지 않는다** |
| `10_report_lower.png` | 안 찍음 | 화면에 `평균 오차` 표기 없음. 단 `09` 와 같은 구버전 렌더에서 나온 장이다(§4) |

---

## 2. 촬영 전/후 md5 — 10장 전부

`screenshots/store/` 에는 10장이 있고 그중 8장이 제출 대상이다. **10장 전부** 대조했다.

| 파일 | 촬영 전 md5 | 촬영 후 md5 | 제출 | 결과 |
|---|---|---|---|---|
| `01_home.png` | `cf88ed507438f8068e2a91383c9ed113` | `55f36ece007fb37f94497d609ae4d2a6` | 2번 | **바뀜** |
| `02_accuracy.png` | `f569bf8208e8b7e478d192730c603c59` | `a4ae5529beab58a6497c4fa092a8d569` | 3번 | **바뀜** |
| `03_review.png` | `675644cdcea750676be217ee2dee8a1a` | (동일) | — | 유지 |
| `04_usepick.png` | `7268e179394cf12ba892ba564450e302` | (동일) | — | 유지 |
| `05_calendar.png` | `9674b7c43f0a516a024d27272d88d223` | (동일) | 8번 | 유지 |
| `06_landing.png` | `8a55e12efa6304228f5278d0e2f55f57` | `f0c3db0d4d16f5db944f5c555c1d853f` | 1번 | **바뀜** |
| `07_detail.png` | `8e6ff630d9c3483bc82bb6a1d3004b81` | (동일) | 4번 | 유지 |
| `08_detail_lower.png` | `c2d4476ea20e6855b9f517d0eccb0f94` | (동일) | 5번 | 유지 |
| `09_report.png` | `5165300e1a0bbc7859e33dea2bb036fd` | (동일) | 6번 | 유지 |
| `10_report_lower.png` | `e1682111198ecc63543bf147b9cb2eaf` | (동일) | 7번 | 유지 |

**중복 0쌍** — `python tools/capture_store_shots.py --audit` 가 촬영 전·후 모두 "합격 중복 없음"
(종료코드 0). 재촬영한 3장은 이전 판과 md5 가 **모두 달라** 같은 그림을 다시 저장한 것이 아니다.

## 3. 규격 실측 (픽셀, PIL 로 측정)

10장 전부 **1080 × 1920**. 재촬영 3장도 동일.
(Play 세로 스크린샷 최소 1080×1920, 최대변÷최소변 = 1.78 ≤ 2 — 둘 다 충족.)

## 4. 육안 확인 2가지

이미지를 **직접 열어 봤다**(숫자만 본 것이 아니다). 차량 사진 부분은 2배 확대해 따로 봤다.

### ① 번호판 — 장별

| 장 | 차량 사진 | 번호판 |
|---|---|---|
| `01_home.png` | 있음(아우디 A5 · 추천 카드) | **없음.** 전면 번호판 브래킷이 **비어 있다**(2배 확대 확인). 옆 차량들은 휠·측면만 |
| `07_detail.png` | 있음(같은 아우디 A5 · 1/3) | **없음.** 같은 사진, 같은 결과 |
| `02_accuracy.png`·`06_landing.png`·`05_calendar.png`·`08`·`09`·`10` | 없음 | 해당 없음 |

> 오너 방침(대표 결정 2026-09-23, `docs/compliance-review.md` §7)대로 **가리지 않는다.**
> 이 보고는 "어느 장에 번호판이 찍혔는지 알고 있어라"는 요구에 대한 답이며, 결함 제기가 아니다.
> 이번 8장에는 **판독 가능한 번호판이 한 장도 없다** — 대상 차량 자체에 번호판이 달려 있지 않다.

### ② '준비 중' 류 미출시 기능 표시

- 재촬영 3장: **없음.** 도구가 촬영 전에 `document.body.innerText` 에서 자동 검사한다
  (`준비 중`·`다시 분석`·`운영 도구`·`분석 대기` — 뒤 셋은 관리자 UI 누수 탐지).
- 나머지 5장: 육안으로 **없음**.
  - `/landing` 소스에 `<!-- 가격(건당 리포트, 준비 중) -->` 이 있으나 **HTML 주석이라 렌더되지 않는다.**
    화면에 보이는 것은 `무료`·`지금 이용 가능` 칩뿐이다(확인함).
  - `watchlist.html` 에 `※ 브라우저 알림(푸시)은 준비 중입니다` 가 있으나 **제출 8장에 즐겨찾기
    화면은 없다.**

### ★ 찍지는 않았지만 보고해야 할 것 — `09_report.png` 는 낡았다 (확인된 사실)

`09_report.png`(9/22 22:22 촬영)에 이렇게 박혀 있다:

> `예상낙찰가 — 분위수 밴드 ? · 실측 평균오차 ±9.3%`

**이 어휘는 지금 코드 어디에도 없다.** `grep -rn "평균오차" web/ src/` → 0건.
없어진 커밋이 특정된다:

- `69e1c47` (2026-09-23 13:50) `fix(리포트): 물건에 전체평균 오차를 붙이던 것을 유형별로 고친다 (PANEL-01)`
- 현행 `report.html` 은 `이 유형({{label}}, {{n}}건) 실측 오차 ±{{mae}}%` 로 **유형별**을 쓴다.

즉 이 장은 **단순히 숫자가 낡은 게 아니라, 팀이 "틀렸다"고 판정해 고친 표기 방식**을 그대로
스토어에 싣게 된다. 캡션은 "물건마다 한 장짜리 종합 분석 리포트" 다.
지시서 범위 밖이라 **찍지 않았다.** 재촬영 여부는 Steward 판단.

### 그 밖의 시점 위험 (확인된 사실 + 추정 구분)

**확인된 사실** — 9/22 22:21(`359e2bb`) 이후 각 템플릿이 움직인 횟수:
`report.html` 8회 · `detail.html` 6회 · `dashboard.html` 6회 · `landing.html` 3회 ·
`calendar.html` 2회 · `accuracy.html` 2회 · `base.html` 2회.
특히 `08_detail_lower.png`(9/23 04:51 촬영) 이후 `detail.html` 에
`bb28dd8 판정 표시: 칩의 색과 글자를 한 원천으로 — 배포 차단 P0 해소`,
`ff87396 히어로: 색의 원천을 판정 칩 하나로` 가 들어갔다. 이 캡처의 히어로에는 판정 칩
`지금 사면 이득` 이 그대로 찍혀 있다.

**추정(미검증)** — 위 커밋들이 `08`·`07`·`05`·`10` 의 **픽셀을 실제로 바꿨는지는 확인하지 않았다.**
확인하려면 그 4장을 다시 찍어 md5 를 대조해야 하는데, 지시서가 제외로 못 박았다.

**관찰 1건(판단 아님)** — 제출 1번(`06_landing`)은 `낙찰 이력 444건 (275건 대조)`,
3번(`02_accuracy`)은 `검증 표본 275건 / 누적 낙찰 히스토리 277건` 을 말한다.
두 장이 스토어에서 나란히 걸리는데 '낙찰 이력'으로 읽히는 수가 **444 와 277 로 갈린다.**
정의가 다른 값인 것은 템플릿에서 확인했다(`won` vs `bt.history_n`). 문구 문제인지 여부는
Steward·성장 담당 판단 영역이라 여기서 결론내지 않는다.

## 5. 기준 커밋

```
7b78b2e0b68b310d4a1dc76c5e941a6f6a1b9818
```

`screenshots/store/HEAD` 에 한 줄로 기록했다. 도구가 **전 장 성공했을 때만** 갱신한다
(실패한 회차의 커밋으로 덮어써 "이 커밋에서 찍었다"가 거짓이 되는 것을 막는다).

## 6. 도구에 무엇을 넣었나 — `tools/capture_store_shots.py` (함수명으로 가리킨다)

기존에는 `--slide5`·`--audit` 둘뿐이었고 **독스트링만 없는 `--all` 을 안내**하고 있었다.

| 이름 | 하는 일 |
|---|---|
| `shoot_landing()` | 1번 `06_landing.png` — `/landing`. `데이터로 먼저`·`전체 평균 오차` 가 프레임 안인지 확인 |
| `shoot_home()` | 2번 `01_home.png` — `/`. `전체 평균 오차`·`최저매각가`·`AI 예상낙찰가` 확인 |
| `shoot_accuracy()` | 3번 `02_accuracy.png` — `/accuracy`. 지표 타일 **4개 라벨 전부** 프레임 안인지 확인 |
| `labels_in_view()` | 좌표로 검사. 숨은 요소는 후보에서 제외(오프캔버스에 안 속음) · 같은 문구를 품은 조상 중 **가장 깊은 요소**만 남김 · 하단 고정 바에 가리면 탈락 |
| `no_forbidden()` | `준비 중`(미출시 기능 광고) + `다시 분석`·`운영 도구`·`분석 대기`(관리자 UI 누수) 탐지 |
| `guard_and_save()` | 임시로 찍고 **규격(1080×1920)·다른 장과의 md5 중복**을 본 뒤에만 제자리로. 걸리면 파일을 남기지 않음 |
| `polite_goto()` / `POLITE_SEC` | C.4 ②: 모든 외부 요청 전 7초 대기. 기존 `find_vehicle_with_cap()`·`shoot_slide5()` 의 이동도 전부 이걸 거치게 바꿨다 |
| `stamp_head()` | `screenshots/store/HEAD` 에 촬영 시점 커밋 기록(PANEL-45) |
| `SHOTS` 표 + `--all` | 독스트링이 안내하던 `--all` 을 **실제로 구현**. 개별 인자 `--landing`·`--home`·`--accuracy` 도 추가 |

⚠ 기대 문구에는 **숫자를 넣지 않았다.** `전체 평균 오차` 처럼 라벨로만 검사한다 —
값을 박으면 값이 움직이는 순간 촬영이 실패하는데, 그건 화면이 아니라 검사가 낡은 것이다.

### 회귀 테스트 추가 — `tests/test_store_shot_tool.py` (7건)

소스를 **실행하지 않고 `ast` 로 읽는다**(도구가 stdout 을 감싸고 playwright 를 끌어오므로).

1. 독스트링이 안내하는 인자가 전부 실제로 있는가 — **이번 사고 그 자체**
2. 네 장을 찍는 함수가 있고 `SHOTS` ↔ 인자가 연결돼 있는가
3. 제출 파일명 4개를 도구가 직접 가리키는가
4. 기대 문구에 움직이는 값(`±9.5`·`275건`)이 박혀 있지 않은가
5. `FORBIDDEN` 이 `준비 중` + 관리자 UI 를 함께 보는가
6. C.4 지연이 5~10초이고 `polite_goto()` 를 우회한 `pg.goto` 가 없는가
7. 기준 커밋을 남기는가

**변이 테스트로 헛것이 아님을 확인했다**: ⑴ `--all` 인자 제거 → 1번 FAIL,
⑵ 기대 문구에 `±9.5%`·`275건` 주입 → 4번 FAIL, ⑶ `polite_goto` 우회 → 6번 FAIL. 셋 다 잡혔다.

## 7. 테스트 게이트

```
python -m pytest -q  →  1205 passed  (97.85s)
```
작업 전 **1198 passed** → 작업 후 **1205 passed**(+7, 실패 0). 줄어든 테스트 없음.

## 8. 못 한 것 · 확신 없는 것

**확인된 사실**
- 재촬영 3장은 라이브 값(±9.5% · 94% · 61% · 275건)과 어휘(`전체 평균 오차`)가 화면에서 일치한다.
- 10장 md5 전부 상이. 재촬영 3장은 이전 판과도 다르다.
- 10장 전부 1080×1920.
- `09_report.png` 의 `실측 평균오차` 어휘는 현재 코드에 없다(`69e1c47` 에서 제거).
- 제출 8장에 판독 가능한 번호판이 없다.
- 하단 탭 바는 `position:fixed` 가 **아니다** — `base.html` 의 `<nav class="lg:hidden shrink-0 …">`
  는 flex 자식이라 스크롤 영역이 탭 위에서 끝난다. 그래서 도구의 "고정 바에 가리는가" 검사는
  이 앱에서는 **걸릴 것이 없어** 항상 통과한다(검사가 고장난 게 아니라 대상이 없는 것).

**못 한 것 / 미검증**
- `05`·`07`·`08`·`10` 의 픽셀이 실제로 낡았는지는 **재촬영해 대조하지 않았다** — 지시서 제외 항목.
  템플릿 커밋 이력으로 "바뀔 만한 이유가 있다"까지만 말할 수 있다.
- `05_calendar.png` 는 9/22 촬영이라 달력의 '오늘' 강조가 **22일**에 찍혀 있다(현재 24일).
  달(9월)은 맞다. 허위 수치는 아니나 제출 시점에 어긋나 보일 수 있다 — **판단은 Steward**.
  같은 장의 `지난달 낙찰 실적 2026년 8월 · 낙찰 100건 · 76%` 가 지금도 같은 값인지는 **미확인**.
- Play 콘솔에 이미 올라가 있는 이미지가 무엇인지는 **볼 수 없다**(오너 권한).
- `03_review.png`·`04_usepick.png` 는 제출 8장이 아니라 손대지 않았다. 낡았는지 **미확인**.
- C.4 준수: 모든 외부 요청 전 5~10초 대기. 403/429/CAPTCHA·비정상 응답 **0건**, 중단 사유 없었음.
  이번 작업의 외부 **이동**은 페이지 내비게이션 3회 + 사전 대조 curl 4회 = **7회**
  (브라우저가 그 페이지에서 끌어오는 CSS·JS·이미지 등 하위 리소스는 별개다 — 세지 않았다).
