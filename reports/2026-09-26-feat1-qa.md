# FEAT-1 가격대 필터 — QA 적대적 반증 검증 (2026-09-26-14 / qa-engineer)

- 기준 커밋: `bfc7708dc19a7fb7c60dd93640e9eaca3b8fd486` **dirty** — `M web/app.py · web/db.py · web/service.py · web/static/app.css · web/templates/vehicles.html`, `?? reports/2026-09-26-feat1-{backend,frontend}.md · tests/test_price_band_filter.py · tests/test_feat1_price_select.py`. 검증 도중 `?? reports/2026-09-26-feat1-design.md` 가 작업 트리에 새로 생겼다(내 산출물 아님, 읽지 않음).
- 코드 수정 없음. 신규 테스트 1파일(`tests/test_feat1_qa_adversarial.py`). 커밋·배포·라이브 요청 없음. 8765 무접촉(측정 전후 LISTENING 확인).
- 측정 조건: `data/auction.db` 를 `sqlite3.Connection.backup`(원본은 `mode=ro` URI 로 열어 읽기만)으로 scratchpad 에 복사 → **1,418행, 공개 목록 1,163건, `min_sale_price` NULL 0행, 전부 integer, 최저가 60.6만~119억**. 앱은 `db.DB_PATH` 를 사본으로 바꿔 `127.0.0.1:8797` 기동(`NC_NO_SCHEDULER=1 NC_NO_BACKGROUND=1`, 8765·8000·8811 회피). Playwright 1.60 `channel="chrome"`(Chrome 153) headless, DPR 1, ko-KR, 비로그인(비관리자). 검증 후 8797 종료.

## [판정: 배포 가능] — FEAT-1 범위에서 깨뜨린 것 0건

두 담당의 주장(패싯 규칙 · 경계 · 오염 파라미터 · 저장 검색 건수 · 쿼리 수 · CSS 산출물 · 좁은 폭)을 운영 사본 1,418행과 큰글씨 모드까지 넣어 흔들었지만 **하나도 어긋나지 않았다.** 어긋난 것은 전부 FEAT-1 이 손대지 않은 곳이었다 — 기존 틈 2건은 backend 보고 §5-3 이 미리 적어 둔 그대로 재현됐고, **새로 찾은 기존 결함 2건**(큰글씨 ≤360px 페이지네이션 넘침 · 큰글씨 320px 헤더 검색 아이콘 히트영역)은 `price=` 없이도 같은 값으로 재현되며 diff 의 +/- 줄이 그 영역을 한 줄도 건드리지 않았다. 아래 §3 에 분리해 적었다 — 별도 티켓 권고.

## 1. 깨뜨린 것 — 0건 (FEAT-1 범위)

없음. 검증 스크립트가 처음 낸 "빨간불" 3종은 전부 **내 술어의 오류**였고, 원인을 밝힌 뒤 테스트 파일에 주석으로 남겼다:

| 내가 낸 거짓 양성 | 실제 | 처리 |
|---|---|---|
| "가격 없음이면 `selected == ['']`" → 오염 20종 전부 BAD, 스위프 32건 BAD | `가격대 전체` 옵션에는 `selected` 속성이 **없다**(첫 옵션 기본값). 올바른 서명은 `selected == []` | 술어 수정. 대조군(`price=0%2D500` → 필터 걸림) 추가로 술어가 공허하지 않음을 증명 |
| "`(필터 적용)` 이 없다" 24건 | 전부 **0건 화면**(빈 결과 분기) — 결과 푸터 자체가 렌더되지 않는다. 24건 모두 `total == 0` 이고, 그 화면에서도 selected·해제 칩은 남는다(24/24) | 테스트 ① 로 고정 |
| "0건 화면에 `총 <b` 가 없어야" | 필터 카드 머리글 `총 0건` 이 그 앵커에 걸렸다. 푸터 앵커는 `/ 총 <b` | 앵커 교정 |

## 2. 못 깬 것 — 무엇을 어떤 조건에서 봤는가

### A. 패싯 일관성 — 운영 사본, 32 조합 × (없음 + 5구간) = 192 페이지 + 192 count API (확인된 사실)
스크립트 `claimA.py`(4 스레드). 조합: 없음 / maker=현대 / judgment=유찰 대기 / q=쏘나타 / upcoming=30 / cond=insp_expired / segment=suv / bucket=wait / bucket=review / usepick=1·now·cheap / picks=1 / promising=1 / all=1 / court=수원지방법원 / result=낙찰 / status=상세없음&all=1 / maker=(제조사 미상) / upcoming=-5 / court 2곳 콤마 / sort=expected·min_sale_price / 2~3개 조합 9종.

검사 5종 전부 통과(promising·upcoming 음수 2조합만 제외 — §3-1·2):
① 선택 구간 옵션 건수 == `NC_LIST_TOTAL` == `/api/vehicles/count` (150/150 구간 페이지)
② 옵션 5개 합 (+NULL 0) == 가격 없는 총수 (30/30 조합)
③ 가격 없는 화면의 옵션 b 건수 == `price=b` 화면 총수 (150/150)
④ 옵션 건수 벡터가 price 값과 무관 (150/150)
⑤ selected 정확히 하나 · 칩 정확히 하나 · 0건 화면 포함 (150/150)

대표값(사본): 없음 `1163 = [102, 256, 418, 176, 211]` · maker=현대 `377 = [30, 96, 146, 53, 52]` · segment=suv `265 = [17, 57, 98, 46, 47]` · bucket=wait `827` · **picks=1 `4 = [1, 3, 0, 0, 0]`**(backend 가 "공허 통과에 가깝다"고 적은 경로 — 사본에서는 실제 4행이 있고 6값 전부 일치) · usepick=1 `8 = [1, 4, 3, 0, 0]` · usepick=now `2` · bucket=review `7` · all=1 `1418 = [118, 274, 461, 207, 358]`.

⚠ 사본에 NULL 최저가 행이 **0** 이라 ② 의 NULL 항은 실데이터에서 공허하다 → 테스트 ④ 가 tmp DB 에 NULL 2행을 넣어 SQL 경로·파이썬 경로(segment) 모두에서 `sum + NULL == total` 을 고정했다.

### B. 경계 — 실제 화면 (확인된 사실)
사본에 정확히 5,000,000(5행) · 10,000,000(12) · 20,000,000(25) · 30,000,000(18) 이 있다. `/vehicles?all=1&q=<사건번호>&price=<구간>` 으로 아래 구간·위 구간·없음 3화면씩 렌더:

| 최저가 | 사건 | 아래 구간 | 위 구간 | 판정 |
|---|---|---|---|---|
| 5,000,000 | 2026타경4532 프라이드 | `0-500` 0건, 카드 없음 | `500-1000` 1건, 카드 있음, 옵션 `500~1,000만 (1)` | 위 구간에만 |
| 10,000,000 | 2026타경50194 그랜저 | `500-1000` 0건 | `1000-2000` 1건 | 위 구간에만 |
| 20,000,000 | 2026타경4046 카니발 | `1000-2000` 0건 | `2000-3000` 1건 | 위 구간에만 |
| 30,000,000 | 2026타경1223 팰리세이드 | `2000-3000` 0건 | `3000-` 1건 | 위 구간에만 |

캡처 `B-5000000-500-1000.png`(눈으로 확인: 최저매각가 `5,000,000` 카드가 `500~1,000만 (1)` 아래, 칩 `500~1,000만 ✕`, `총 1건 (필터 적용)`) 외 3장. 테스트 ③ 이 4경계를 렌더된 `href="/vehicle/<id>"` 로 고정.

### C. 파라미터 오염 — raw HTTP(`http.client`, 인코딩 정규화 없음) 20종 (확인된 사실)
`price=` · `0-500&price=1000-2000` · `1000-2000&price=1000-2000` · `%00` · `0-500;DROP` · `price[]=0-500` · **10,240B** · `0-500&price=` · `&price=0-500` · `PRICE=0-500` · `0-500%20` · `0-500%00` · `1000-2000;price=0-500` · `%EA%B0%80` · `3000-%0A` · 3중복 등 → **전부 200/200 · 총수 1,163 == 필터 없음 · 구간 selected 없음 · 칩 없음 · `(필터 적용)` 없음 · count API 1,163.**
대조군(필터가 **걸려야** 하는 값): `price=0%2D500`(=`0-500`) → 102건·selected·칩 · `price=0-500&price[]=x`(다른 키) → 102 · `price=0-500&PRICE=x` → 102. 테스트 ② 20종 + 대조군.

### D. 검색 저장 → 즐겨찾기 건수 (확인된 사실, Playwright 1440)
`/vehicles?maker=현대&price=3000-` (`NC_LIST_TOTAL` 52) → `검색 저장` 클릭 → alert `검색을 저장했습니다…` 수락 → localStorage `naechaget:searches[0]` = `{qs: "maker=%ED%98%84%EB%8C%80&price=3000-", label: "현대 · 3,000만~", baseline: 52}` → 다시 클릭 → `이미 저장된 검색입니다.` → `/watchlist` 카드 `현대 · 3,000만~ / 저장 시 52건 · 현재 52건`(캡처 `D-watchlist-1440.png`) → 그 qs 로 `/api/vehicles/count` 52 → 카드 링크로 되돌아가 총수 52 · 셀렉트 `3,000만~ (52)`. **52 = 52 = 52 = 52.**
오염값 저장(`price=abc&maker=기아`): 라벨 `기아`(구간 미포함), baseline 254 == 목록 254 == API 254 — 세 곳이 같은 "필터 없음"을 본다.

### E. 큰글씨(nc-large) — 320·360·390·430 × 2 URL × 접힘/펼침 (확인된 사실)
`localStorage naechaget:large=1` 을 `add_init_script` 로 주입(루트 20px 확인). 큰글씨에서는 `#listFilter` 가 **접힌 채** 뜬다(`open=false`, summary `검색 · 필터` 표시, 필터 하단 162px) — 펼쳐서 잰 값:

| 폭 | 가로 스크롤 | 뷰포트 밖(설계 외) 접힘/펼침 | 가격대 셀렉트 w×h · 선택 텍스트 폭 / 안쪽 폭 | 잘림 | 칩 (l,r,h) 화면 안 |
|---|---|---|---|---|---|
| 320 | 없음 | **2**/2 — 헤더 검색 아이콘 앵커(288–327) · 페이지네이션 nav(41–365) → §3-3·4 | 248×40 · 127 / 196 | 없음 | (36,181,32) 안 |
| 360 | 없음 | 1/1 — 페이지네이션 nav(URL A만) | 288×40 · 127 / 236 | 없음 | 안 |
| 390 | 없음 | 0/0 | 318×40 · 127 / 266 | 없음 | 안 |
| 430 | 없음 | 0/0 | 358×40 · 127 / 306 | 없음 | 안 |

`scrollWidth > clientWidth` 인 셀렉트 0. 셀렉트 4개 중 잘린 것 0(판정·제조사·정렬 포함). 칩 높이 32px(일반 26). 일반 모드 대조 4폭 전부 뷰포트 밖 0·잘림 0(frontend §3 과 같은 값: 262/302/332/372 × 36). 캡처 `E-large-{320,360,390,430}-{A,B}-{collapsed,expanded}.png` 16장 + 일반 8장 — `E-large-320-A-expanded.png` 를 눈으로 열어 `1,000~2,000만 (418)` 한 줄 온전·칩 `1,000~2,000만 ✕` 프레임 안 확인.

### F. 회귀 — 전체 스위트 (확인된 사실)
`NC_NO_SCHEDULER=1 NC_NO_BACKGROUND=1 python -m pytest -q -p no:cacheprovider`
- 1차(신규 QA 파일 추가 **전**): **1,381 passed, 0 failed**, 655s — 지시서 예상 1,381(1,312+49+20)과 일치.
- 신규 `tests/test_feat1_qa_adversarial.py` 단독: **33 passed, 2 xfailed**(xfail 은 §3-1·2 기존 틈 표식, `strict=True`), 8s.
- 2차(신규 파일 포함): 아래 §5 참조.

### G. CSS 산출물 — 규칙 단위 비교 (확인된 사실)
`git show HEAD:web/static/app.css` 와 작업 트리를 중괄호 깊이로 규칙 분해: **708 → 709 규칙, 추가 1 `.col-span-2{grid-column:span 2/span 2}`(`.z-50` 뒤·`.m-0` 앞), 제거 0, 순서 보존(추가분 제외 시 완전 동일), +38 바이트.** 입력(`web/static/tailwind_input.css` · `tailwind.config.js` · `package.json`) git 변경 없음.

### H. 쿼리 수 재측정 (확인된 사실 — 사본 DB, in-process TestClient, `db.connect` 프록시, 워밍업 후 2회째)
| 요청 | execute | 구간 COUNT(CASE…GROUP BY band) | backend 표 |
|---|---|---|---|
| `/vehicles` | 6 | 1 | 6/1 ✓ |
| `/vehicles?price=1000-2000` | 6 | 1 | 6/1 ✓ |
| `/vehicles?maker=현대&price=3000-` | 8 | 1 | 8/1 ✓ |
| `/vehicles?segment=commercial(&price=…)` | 5 | 0 | 5/0 ✓ |
| `/vehicles?usepick=1&price=…` · `bucket=wait&price=…` | 5 | 0 | (표에 없음) |
| `/vehicles?picks=1&price=…` | 6 | 0 | (표에 없음 — `promising_rows` 의 SELECT 1회 추가, 기존) |
| `/api/vehicles/count?…` 3종 | 1 | 0 | 1/0 ✓ |
테스트 ⑤ 가 "SQL 경로 COUNT 정확히 1 · 파이썬 경로 0 · price 유무로 execute 수 불변" 을 고정.

## 3. FEAT-1 무관 — 기존 틈·결함 (재현됨, 분리)

1. **`/api/vehicles/count` 가 `promising` 을 받지 않는다**(backend §5-3 기재). 사본: `/vehicles?promising=1` 372 vs API 1,163; 구간별 `[28,93,149,58,44]` vs API `[102,256,418,176,211]`; `promising=1&upcoming=30` 59 vs 144. `/vehicles` 쪽은 자기 안에서 일관(옵션==총수, 합 372). 저장 검색이 이 qs 를 가지면 즐겨찾기 건수가 틀린다 — FEAT-1 이전부터. xfail 표식 `test_known_gap_count_api_ignores_promising`.
2. **`upcoming` 음수 클램프 불일치**(backend §5-3 기재). `/vehicles?upcoming=-5` 1,163(0 으로 클램프) vs API **0**(`sale_date <= now-5day` 그대로). xfail 표식 `test_known_gap_count_api_negative_upcoming`.
3. **새로 찾음 — 큰글씨 + ≤360px: 페이지네이션 `nav` 가 카드 밖으로 넘친다.** `#listResults nav`(`flex items-center gap-1`, nowrap) 폭 324px > 부모 299px(320) / 339px(360) → `다음 ›`(307–364) 가 320·360 뷰포트 밖. **`price=` 없는 `/vehicles`(97쪽)에서도 같은 값(41–364)**, 390 부터 안(부모 369). 일반 모드 4폭 전부 안(33–293/310). diff 의 +/- 줄 중 `<nav`·`page={{`·`lg:hidden` 을 건드린 줄 **0**. 큰글씨는 저시력 모드라 "다음" 손잡이가 잘리는 건 사용성 결함 — **별도 티켓 권고**(P2 추정: 페이지 5개 이상 + 큰글씨 + 폭 360 이하 교집합).
4. **새로 찾음(경미) — 큰글씨 + 320px: 헤더 검색 아이콘 앵커의 패딩 박스가 뷰포트 밖 7px**(288–327; 글리프 자체는 295–319 로 보인다). `/` · `/vehicles` · `/watchlist` 전부 같은 값, 360 부터 안. `base.html` 헤더 규칙(`html.nc-large header …`) 영역, FEAT-1 무관. 히트영역만 살짝 잘리는 수준 — 3번 티켓에 묶어 볼 것.

## 4. 관찰 (결함 아님, 판단 참고)

- **성능(미검증 단독 시간)**: 4 스레드 동시 요청 중 `usepick=1` 20.4s · `picks=1` 15.9s · `usepick=now&price=0-500` 15.4s. 파이썬 경로가 전 행 갈래 판정을 하는 기존 구조이고 동시 부하 조건이라 단독 시간은 재지 않았다. FEAT-1 이 여기에 더한 것은 없다(H: 쿼리 +0).
- `all=1` 에서 구간 최대 461(3자리). frontend §6-4 의 4자리 포맷 질문은 사본에서도 아직 현실화되지 않았다.
- 큰글씨에서 필터 카드가 접혀 뜨므로 가격대 셀렉트는 `검색 · 필터` 를 펼쳐야 보인다 — 기존 큰글씨 설계(3회차 패널 결정)이고 셀렉트 3개도 같은 처지.
- **캡처 디렉터리 사고(내 쪽, 수습 완료)**: scratchpad `shots/` 에 이전 세션의 캡처 38장(2026-09-23 사고의 `before/after-390-top.png` md5 `56456b83…` 쌍 포함)이 남아 있었고, 와일드카드 복사로 `screenshots/feat1-qa/` 에 섞여 들어갔다. 이름 규칙(`B-*`·`D-*`·`E-*`·`HEAD.txt`)으로 걸러 삭제 → **최종 30장, md5 중복 0**, `HEAD.txt`(기준 커밋·dirty 목록·촬영 조건). 이 회차 판단에 쓴 캡처는 전부 이 30장 안이며 Read 로 연 5장(`E-large-320-A-collapsed/expanded`, `E-large-390-A-expanded`, `B-5000000-500-1000`, `D-watchlist-1440`)은 파일명과 내용이 일치했다.

## 5. 전체 스위트 2차 실행 (신규 파일 포함)

`NC_NO_SCHEDULER=1 NC_NO_BACKGROUND=1 python -m pytest -q -p no:cacheprovider` → **1,414 passed, 2 xfailed, 0 failed**, 115s.
1,381(1차) + 33(신규) = 1,414 ✓. xfailed 2 = §3-1·2 표식(`strict=True` 라 틈이 닫히면 빨간불로 알린다).
테스트 수: 변경 전 1,312 → backend 1,361 → frontend 1,381 → QA **1,416**(passed+xfailed) — 줄지 않았다.
⚠ 1차 655s vs 2차 115s — 같은 명령이다. 1차는 8797 서버·Playwright·스위프가 같은 PC 에서 동시에 돌던 시간대라 느렸을 것(**추정**, 측정 안 함). 결과 수에는 영향 없다.

## 6. 확인된 사실 / 추정

- **확인된 사실**: §2 A~H 의 모든 수치(스크립트 출력·pytest 출력·md5·git diff), §3 의 재현값과 "diff 가 그 영역을 건드리지 않음"(`git diff -U0 … | grep '^[+-]'` 0건), 운영 DB 쓰기 없음(`mode=ro` URI + backup API), 8765 무접촉.
- **추정**: §3-3 의 P2 우선순위, §4 성능 수치의 단독 시간, 큰글씨 폴드 영향(접힘 설계라 셀렉트가 폴드에 영향 없음 — 캡처로 봤지만 실기기 아님).

## 7. 산출물
- `reports/2026-09-26-feat1-qa.md`(이 파일)
- `tests/test_feat1_qa_adversarial.py` — 35건(33 passed + 2 xfailed strict). 앵커 문자열만, 오프셋 창 없음.
- `screenshots/feat1-qa/` 30장 + `HEAD.txt`(gitignore 대상)
- scratchpad(세션 한정): `claimA.py/.json/.txt` · `claimC.py` · `claimH.py` · `claimBDE.py/.json` · `serve.py` · `auction_copy.db`(md5 `4d29e5da…`) · `pytest_full.txt` · `pytest_full2.txt`
