# 주간 전문가 패널 — 7회차 (2026-09-23)

> ⚠ **이 문서는 원본이 아니라 실행 로그에서 재구성한 복구본이다.** 6회차와 같은 이유다 —
> 루틴이 4인 평가를 마치고 리포트를 쓰고 커밋(`af000c9`)까지 했으나 **푸시가 또 403**
> 으로 막혔고(`Claude doesn't have GitHub access … for your organization`),
> `persist_session: false` 라 샌드박스와 함께 원본이 사라졌다.
> 아래는 Steward 가 실행 로그(`cse_01FfqR3h…`)에서 직접 읽어 옮긴 것이고,
> 로그 표시 한도 밖이라 못 본 항목은 **"확인 못 함"** 으로 남겼다. 지어내 채우지 않았다.

## 한 줄 결론

**평균 77.0 (−1.5). 4인 전원이 같은 결함을 지적했다 — 예상낙찰가의 '실측 오차'가 화면마다
다른 숫자로 인쇄된다.** 6회차에서 지적된 뒤 일주일이 지나도록 미조치이고, 이번에는
**근본 원인까지 특정**됐다.

## 점수판

| 전문가 | 6회차 | **7회차** | 증감 |
|---|---|---|---|
| 중고차 | 81 | **79** | −2 |
| 앱 디자인 | 79 | **76** | −3 |
| 경매 | 78 | **77** | −1 |
| 앱 품질 | 76 | **76** | ±0 |
| **평균** | 78.5 | **77.0** | **−1.5** |

항목별로 로그에서 확인된 것만 적는다(나머지는 확인 못 함).

- **중고차 79** — 차량 상태 진단·가치 산정 23/30 · 맞춤 컨설팅 17/25 · 정보 투명성 25/30 ·
  사후 절차 14/15
- **앱 디자인 76** — 정보 설계 22/30
- **경매 77** — 가격 발견 21/30
- **앱 품질 76** — 데이터 무결성 23/30 · 응답·안정성 19/20

## ★ 합의 지적 — 4인 **전원**이 지적했다

같은 물건(`2026타경10616_1`)의 '실측 오차'가 화면마다 다른 숫자로 인쇄된다. 라이브 실측이다.

| 화면 | 표기 |
|---|---|
| 상세 | 이 유형(국산, 208건) 실측 오차 **±8.8%** |
| 상세 (같은 화면 안) | 실측 오차 **±9%** |
| 리포트 | 실측 평균오차 **±9.5%** |
| 리포트 (같은 문서 안) | 실측 평균오차 **±10%** |

**근본 원인이 이번에 특정됐다** — 같은 변수 `backtest.mae_pct` 를 **서로 다른 반올림 필터**로
찍고 있다.

- `report.html:892` — `expected.acc.mae` 또는 `backtest.mae_pct|round(1)`
- `report.html:900`·`1075` — 같은 변수, 다른 처리
- `detail.html:340` — `expected.mae|round|int` (정수 반올림 → `±9%`)

합격선(80)을 넘긴 전문가가 **한 명도 없고**, 네 명이 각자 독립적으로 같은 곳을 짚었다.
이 제품의 1순위 가치가 데이터 신뢰성인데, **정확도를 말하는 숫자 자체가 화면마다 다르다.**

## 역할별 주요 지적

### 경매 (77, −1)
- **`win_probability()` 가 '확률'이 아니라 '하한'임을 코드 주석이 스스로 인정한다.**
  `web/service.py:3616` · `web/db.py:117-137` · `service.py:2103-2138` —
  `record_sale_result` 가 `label == "낙찰"` 일 때만 기록하므로 **무입찰 유찰이 분모에서 빠진다.**
  그 편향이 사용자를 과다 입찰로 밀 수 있다는 것까지 주석에 적혀 있는데,
  **화면(`detail.html:478,494`)에는 그 단서가 없다.** **7회차 연속 지적, 백로그 티켓 0건.**
- `SOFT_CAP_RATIO = 1.10`(`service.py:3557`)이 하드코딩. 3회차부터 반복 지적, 티켓 0건.

### 앱 품질 (76, ±0)
- 데이터 무결성 23/30(−1) — 위 합의 지적이 **일주일째 그대로**여서 감점 유지.
- 응답·안정성 19/20 — 전 화면 200, 1.3~1.7초. 잘못된 ID·300자 ID·경로탐색·XSS·
  잘못된 `page`/`sort`·`admin`/`debug` 쿼리 등 **11종 이상값에 5xx 0건**.
- `Content-Security-Policy`·`Permissions-Policy` 헤더 여전히 부재.

### 앱 디자인 (76, −3)
- 위 합의 지적의 **근본 원인(반올림 필터 2종)을 특정**한 것이 이번 회차의 성과.
- MAE 합격선 3종 불일치도 **지난 회차와 같은 줄에 그대로** —
  `dashboard.html:437-438`(≤15%) · `accuracy.html:81`(±20%) · `accuracy.html:157`(>10%).
- **스크린샷 2회 연속 미확보** → 320~1440px 육안 검수 미수행, 레이아웃 판단은 '확인 필요'로 분리.

### 중고차 (79, −2)
- 합의 지적을 오늘 직접 재확인. 신뢰도 37/100·표본 12건인 물건일수록 오차 폭이
  입찰가 결정에 직결되는데 그 숫자가 화면마다 다르다.
- 표본 편차(±38%, "가격 흩어짐이 큼")가 **사용자가 먼저 보는 상세 화면엔 없고 리포트에만** 있다.
- 양호로 기록된 것: 취득세·이전·탁송·정비·충당을 더한 **총 취득원가** 제시,
  잔금 미납 시 보증금 전액 몰수·"경락잔금대출 사실상 없음" 경고까지 구체적(사후 절차 14/15).

## 실행 백로그

| | 항목 | 담당 | 근거 |
|---|---|---|---|
| **P0** | 실측 오차 표기 통일 — 같은 변수를 한 가지 필터로 | 프론트 | 4인 전원 · 근본 원인 특정됨 |
| **P0** | 패널 인프라 — GitHub 앱 설치(푸시 403), `playwright` 설치 | 인프라 | 리포트 2회 연속 유실 · 스크린샷 2회 연속 0장 |
| **P1** | MAE 합격선 3종 통일 | 프론트 | `dashboard.html:437` · `accuracy.html:81,157` |
| **P1** | `win_probability` 편향을 화면에 고지하거나 분모 확보 | 백엔드 | 7회차 연속, 티켓 0건 |
| **P2** | `SOFT_CAP_RATIO` 외부화 | 백엔드 | 3회차부터 반복 |
| **P2** | CSP·Permissions-Policy 헤더 | 인프라 | 헤더 실측 |

## 이 회차가 남긴 것

**지적이 반복되는데 티켓이 없다.** 경매 전문가는 같은 항목을 **7회 연속** 지적했고
백로그에는 한 줄도 없다. 패널을 도로 살려 놓아도 **지적을 받는 쪽이 티켓으로 바꾸지 않으면**
점수는 계속 내려간다 — 실제로 6회차 78.5 → 7회차 77.0 하락의 상당 부분이
'같은 결함이 그대로여서' 붙은 감점이다.
