# 백로그 (pm-orchestrator 관리)

> 티켓 단위 작업 관리. 한 번에 하나의 티켓만 doing. 완료 시 [changelog.md](changelog.md) 기록.
> 상태: `todo` · `doing` · `review` · `done` · `blocked`

## 디자인 자가개선 — ✅ 목표 85점 달성 (현재 86, PHASE 2 완료)

> Round1 64 → Round2 81 → Round3 최종 **86(합격)**. 상세 [critique/round_3.md]. 남은 항목은 87~88 상방(선택) 또는 유료화(PS).

## 진행 중 / 대기 (Round 2·3)

| ID | 제목 | 담당 | 상태 | 완료 기준(DoD) |
|---|---|---|---|---|
| IMP-R2-01 | 대시보드 유망물건·관심목록 모바일 카드화 | frontend | ✅ 완료 | 390px 카드형+예상낙찰가·게이지, 가로넘침 제거 |
| IMP-R2-02 | 목록/유망물건 미니 가격 게이지 | frontend | ✅ 완료 | `_macros.minigauge`(최저→예상마커→시세틱) 목록·대시보드 행 |
| IMP-R3-01 | 대시보드 상단 대표 예상낙찰가 밴드 | frontend/backend | ✅ 완료 | 선별 유망물건 예상낙찰가 범위·중앙값·평균신뢰도 상단 노출 |
| IMP-R3-02 | detail 모바일 헤더 세로붕괴 수정 | frontend | ✅ 완료 | 390px 제목 1줄·버튼 줄바꿈 |
| IMP-R2-03 | 빈 상태·로딩 강화 디자인 | frontend | ✅ 완료 | 목록 제로결과 카드(search_off+필터초기화 CTA)+스켈레톤 시머(필터적용/페이지이동) |
| IMP-R3-03 | 실제 시세 분포 히스토그램 | frontend/backend | ✅ 완료 | `service.price_distribution`→`_macros.histogram`(막대+최저/예상/시세 마커+±MAE 음영) 상세 히어로·리포트 05 |
| IMP-R2-04 | 관심목록 예상낙찰가·신뢰도 반영 | frontend/backend | todo | 상한가 편중 완화, 목록과 일관 |
| PS-13 | 감정 요항 구조화 + 상태·검사 밸류 반영 + 목록 주행거리 | backend/frontend | ✅ 완료 | `parse_appraisal`(색상·연료·검사유효기간·옵션·상태) → 상세 칩/배지, `condition_costs`로 상한가 차감(재분석 시). 목록 km 표기 |
| DATA-02 | 요항 파싱 결과 DB 영속화 → 목록 검사경과·손상 필터/정렬 | backend/frontend | ✅ 완료 | DB 컬럼 inspection_to·condition_level·condition_flags + 마이그레이션. 분석/재교정 시 저장 + 시작 시 backfill(691건). 목록 `cond` 필터(검사경과·외관손상 칩)·검사만료 임박순 정렬·행 인디케이터 |
| DATA-01 | 제조사 정규화 저장시점 반영 | backend | todo | 현재 `_canon_maker`는 조회시점만. 수집·저장(upsert)에 `maker_norm` 컬럼+마이그레이션으로 영속화하면 정렬·집계·시세매칭도 일관. 표기 늘어도 규칙만 보강 |
| PS-14 | 차량 사진 비전 분류(정면·측면·실내) + 사진없음 감가 | backend/frontend/vision | ✅ 완료(1차 20건) | `.claude/agents/photo-classifier.md` + `src/parse/photo_montage.py` + DB `photo_order` + `scripts/photo_classify.py prep\|apply`(증분). 실입찰 대상 20건 분류·검수 완료(정면 20/20). no_photos 감가 배선. **잔여 646건(완료/종결 위주) 일괄 분류는 PS-15** |
| PS-15 | 미분류 646건 사진 일괄 분류 | vision | ✅ 완료 | 162에이전트·0오류·646건 분류·apply 완료(총 666 분류, 미분류 0). 비차량 33건(선박·타워크레인·천공기·굴착기)은 front=0으로 정직 처리, 저확신 208건(순수측면 부재·다물건 혼재 포함). 스팟체크(idx 1~646 표본 15) 육안 확인. 신규 물건은 증분(`prep`)으로 소량 |
| DATA-03 | 다물건 사건 사진 물건별 분리 | backend/collect | todo | 한 사건 다물건 시 각 물건 폴더에 사건 전체 사진 유입(형제 파일셋 동일, 15사건·40물건). 현재 '사진 혼재?' 배지로 캐비엇만. 근본 해결=수집·저장 시 물건번호로 사진 분리(대법원 응답에 물건 귀속 정보 확인 필요, C.4-3 추측금지) |
| PS-16 | 목록 썸네일 확대(정면·측면·실내) | frontend | ✅ 완료 | 배경이미지 div stretch로 행높이 채움(행높이 유지), ≥1800px w-20 확대·max-h 상한. 1900/1536 육안검증 |

## 주간 전문가 패널 지적 — 티켓화 (2026-09-23 신설)

> **왜 이 절을 만들었나**: 7회차 리포트의 마지막 지적이 *"지적이 반복되는데 티켓이 없다"*
> 였다. 경매 전문가는 같은 항목을 **7회 연속** 지적했고 백로그에는 한 줄도 없었다.
> 패널을 살려 놔도 **받는 쪽이 티켓으로 바꾸지 않으면** 점수는 계속 내려간다 —
> 6회차 78.5 → 7회차 77.0 하락의 상당 부분이 '같은 결함이 그대로여서' 붙은 감점이다.
> 근거: [`docs/reviews/2026-09-23.md`](reviews/2026-09-23.md)

| ID | 제목 | 담당 | 상태 | 완료 기준(DoD) |
|---|---|---|---|---|
| PANEL-01 | 리포트가 특정 물건에 **전체 평균** MAE를 붙인다 | backend/frontend | **완료(배포 대기)** | `app.py:850` 의 `expected` 에 `acc` 가 없어 `report.html:892` 의 유형별 분기가 죽어 있다. 상세는 이 유형(±8.8%), 리포트는 전체(±9.5%)를 같은 라벨로 말한다. `accuracy_for` 독스트링이 "전체 MAE를 모든 물건에 붙이면 **과대주장**"이라 못 박은 바로 그 행위(3회차 지적으로 상세만 고쳤고 리포트가 남았다). **면책문(`:1451`)이 전체·유형 중 무엇을 말해야 하는지 함께 정한 뒤** 고친다. **2026-09-23 배포 후 라이브 재확인**(`/vehicle/2026타경70210_1`): 상세 `±8.8%`(유형별) · 리포트 `±9.5%`(전체평균) — **같은 화면 안의 불일치는 사라졌고 이 화면 사이 차이만 남았다.** 즉 이 티켓이 남은 전부다. **★ 2026-09-23 수정 완료 — 아직 배포 전이다(배포는 오너 승인 사항).** ⑴ 원인은 `app.py` 리포트 라우트의 `expected` 에 `acc` 키가 없어 템플릿의 `{% if expected.acc %}` 두 자리가 **한 번도 안 열리는 죽은 가지**였던 것. 상세(`:781-782`)와 같은 소스를 주도록 한 줄 추가. ⑵ **그것만 하면 개악이었다** — `{% elif backtest.mae_pct %}` 폴백이 열려 있고 `:1363·1380·1451` 은 여전히 전체평균이라 **한 문서에 두 값**이 인쇄된다. 폴백을 지우고 네 자리를 한 소스로 묶었다. `accuracy_for` 독스트링이 "표본이 부족하면 None — **전체 평균으로 대신하지 않는다**"고 못 박은 그대로다. ⑶ `:1075` §모델 검증 실적은 **전체평균이 맞는 자리**라 그대로 두되 보조문구를 `모델 전체 평균 · 이 물건 값 아님` 으로 바꿔 **스스로 모집단을 말하게** 했다. ⑷ `:1363` 의 `</small>` 이 조건문 **안**에 있어, 유형값이 None 이 될 수 있게 되는 순간 태그가 안 닫힌다 — 같은 커밋에서 밖으로 뺐다. ⑸ 음영 밴드를 그리는 `price_distribution` 인자도 유형별로 바꿨다(`app.py` 2곳) — 라벨만 바꾸면 **그린 폭과 말하는 %가 어긋난다**. ⑹ 검수 2인 지적을 반영해 **모수를 병기**한다: `이 유형(수입, 35건) 실측 오차 ±10.6%` — 상세 `detail.html:408` 이 이미 쓰던 형식을 그대로 따랐고(새 말을 발명하지 않는다), `accuracy_for` 가 이미 반환하던 `label`·`n` 이 템플릿에서 **사용 0회**였다. **검증**: `pytest` **1,086 통과**(신규 `tests/test_mae_stratum_parity.py` 7건) · **사보타주로 2 failed 확인**(`acc` 를 되돌리면 빨간불 — 공허 통과 아님) · 6폭 320~1440px **전 폭 0px** · 라이브에서 상세·리포트가 **글자까지 동일**한 `이 유형(수입, 35건) 실측 오차 ±10.6%` 를 말하고, 같은 문서의 모델 전체 타일은 `±8.9% 예측 오차 · 모델 전체 평균 · 이 물건 값 아님` 으로 스스로를 구분한다 · 추정치가 없는 물건은 면책문에서 **수치를 말하지 않는다**(전에는 22건 전부가 없는 추정치의 정밀도를 주장). **⚠ 오너 보고 필요** — 이 변경으로 **표시 오차가 대체로 작아진다**(전수 분포 8.3:510 · 10.6:315 · 8.8:220 · 12.8:118, 전체평균 8.9). 모델이 좋아진 게 아니라 **말하는 대상이 바뀐 것**이라 '더 정확해졌다'는 공지는 그 자체가 과대주장이다(검수 권고). 원인 중 하나는 `accuracy_for` 가 **가격대 층을 후보에서 제외**하는 것인데(`service.py` `cands` 2개만), 그 층에는 전체평균보다 나쁜 값(2,000만 초과 9.5)이 실재한다 — **의도인지 누락인지 코드에 근거가 없어 오너 결정 사안** |
| PANEL-02 | `win_probability` 분모에 **무입찰 유찰이 빠져** 낙찰확률이 하한값 | backend | **고지만 완료 · 분모는 PANEL-09** | `service.py:3616`·`db.py:117-137`·`service.py:2103`(`record_sale_result` 가 `label=="낙찰"` 일 때만 기록). 코드 주석이 스스로 편향을 인정하는데 화면(`detail.html:478,494`)엔 그 단서가 없다. **7회차 연속 지적.** 분모 확보가 어려우면 최소한 화면에 한계를 고지. **★ 2026-09-23 고지 적용**(`detail.html:478`) — 확률이 실제로 찍히는 분기 안에 *"입찰자가 없어 유찰된 기일은 이 분포에 들어 있지 않아, 실제 낙찰 가능성은 이 값보다 높습니다"* 를 넣었다. **편향의 존재만이 아니라 방향까지 말한다** — 어느 쪽으로 틀렸는지 모르면 사용자는 과다 입찰로 간다. 굵게 하지 않고 `<span class="block mt-1">` 로 **줄만 분리**했다(그 문단엔 이미 굵은 글씨가 셋이라 넷째는 행동을 제한하는 유일한 금액인 `입찰 상한선` 과 경쟁한다. 같은 파일 475행이 이미 쓰는 패턴). 390px 실측: span 의 left·width 가 문단과 같은 37px·316px — 자기 줄을 차지한다. `design-critic` 검수 "배포 가능". **⚠ 분모 자체는 그대로다** — `record_sale_result` 가 여전히 낙찰만 기록하므로 확률은 계속 하한값이고, 데이터 모델 개선은 **별도 과제로 남는다** |
| PANEL-03 | `SOFT_CAP_RATIO = 1.10` 하드코딩 | backend | **외부화만 완료 · 값 결정은 PANEL-08** | `service.py:3557`. 3회차부터 반복 지적. `config.yaml` 로 외부화하고 근거를 기록. **★ 2026-09-23 완료** — `soft_cap()` 안에서 `load_config().get("soft_cap_ratio")` 를 읽는다. 호출부 3곳(`:3599`·`:3654`·`:3676`)이 인자를 넘기지 않으므로 **호출부는 하나도 건드리지 않았다**. `load_config` 는 mtime 캐시(`:40-51`)라 행마다 불려도 파일 I/O가 반복되지 않는다. **값은 1.10 그대로** — 자리만 옮긴 변경이라 산정 결과는 동일하다. config 에는 "실측이 아니라 가정"이라는 근거와 올리고 내릴 때의 영향을 함께 적었고, 설정이 깨지면 코드 기본값으로 떨어져 **산정 자체가 멈추지는 않는다**. `tests/test_soft_cap_config.py` 11건 추가, 그중 **드리프트 감시 2건이 핵심**이다 — `config.yaml:57` 에 같은 종류의 사고 기록이 있다(승인값이 코드에만 반영되고 config 에는 낡은 300 이 남아 매일 300회에서 멈췄다). ⚠ **처음 쓴 배선 테스트는 아무것도 증명하지 못했다** — config 값과 코드 기본값이 둘 다 1.10 이라 배선이 끊겨도 통과한다. 코드 기본값만 9.99 로 바꿔치는 방식으로 고쳤고 **사보타주로 빨간불을 확인했다**(배선 절단 시 2 failed). 전체 **1,079 통과** |
| PANEL-04 | MAE **합격선이 화면마다 다르다** | frontend | **판정: 통일하지 않음** | `dashboard.html:437`(≤15%) · `accuracy.html:81`(±20%) · `accuracy.html:157`(>10%). 서로 참조가 없어 어느 것이 기준인지 알 수 없다. **★ 2026-09-23 조사 완료 — 패널 처방(통일)은 개악이다.** 세 숫자는 **비교 대상이 서로 다르다**: `dashboard.html:437` 은 `backtest.mae_pct`(백테스트 **전체 스칼라 1개**)를 보고 '목표 달성/개선 필요'를 찍는 **제품 KPI 배지**이고, `accuracy.html:81` 은 `p.err_pct`(**개별 낙찰 1건**의 LOO 오차)에 걸리는 산점도 **색칠 기준**이며, `accuracy.html:157` 은 `r.mae`(**층별 평균**)에 걸리는 경고색이다. 질문 자체가 '목표에 도달했나 / 이 한 건이 적중인가 / 이 유형이 나쁜가'로 다르다. 하나로 합치면 `within10_pct`·`within20_pct` 두 공표 지표의 정의가 깨지고, 층 경고선을 15로 늦추면 **PANEL-01 이 지키려는 보수적 층 선택이 화면에서 무력화된다** — 두 티켓이 정면으로 충돌한다. **★ 백로그가 센 자리 수도 틀렸다**: `accuracy.html:190`(최근 검증 사례 표)이 같은 ±20% 를 쓰므로 **3자리가 아니라 4자리**이고, `report.html:1062`(표본 편차 CV 10/20)·`detail.html:805`(관리자 오차 색 10/20)까지 세면 10·20 리터럴이 **코드에 네 벌** 있다. 그 CV 10/20 은 예측오차와 **다른 양**이라 '숫자 통일'로 처리하면 여기까지 쓸려 들어간다. **남는 진짜 결함 2건은 별도 티켓으로 분리**: ⑴ 같은 모수(개별 1건)에 ±20%(산점도·사례표)와 ±10%(적중률 타일) **두 잣대**가 동시에 걸려 있고 어느 쪽도 '무엇 대비'인지 안 밝힌다 ⑵ 세 임계값 중 `config.yaml` 에 있는 것이 **하나도 없고** 고정하는 테스트도 **0건**이다(`ACCURACY_STRATUM_MIN_N=8` 이 이미 상수→화면 노출 패턴을 보여준다). 권고는 **통일이 아니라 모수 병기** |
| PANEL-05 | `Content-Security-Policy`·`Permissions-Policy` 헤더 부재 | 인프라 | todo | 품질 전문가 6·7회차 지적. 다른 보안 헤더(HSTS·nosniff·X-Frame·Referrer-Policy)는 이미 있다 |
| PANEL-06 | **리포트가 모든 모바일 폭에서 가로로 스크롤된다** | frontend | todo | 2026-09-23 Steward 실측(`/vehicle/…/report`): 문서 넘침 320px **20** · 360px **91** · 390px **21** · 430px **20** · 768px **11px**. 범인은 히스토그램 마커 라벨 `<span>"▎예상 2500만"`(+21px)·`"최저 2500만"`(+12px) — `overflow` 상자 **밖**이라 문서를 민다. 표(`table`)는 `div.tscroll(auto)` 안이라 무관하다. ⚠ 360px의 91px 중 **70px는 아직 원인 미상** — 고치기 전에 그것부터 특정할 것. **★ 이 결함은 패널 6·7회차가 못 잡았다. 스크린샷을 못 찍었기 때문이다** — 코드 열람만으로는 보이지 않는 종류이고, 그래서 캡처 복구(PANEL 인프라)가 품질 지표에 직접 걸린다. **권고(design-critic 2026-09-23)**: 라벨을 줄이는 것(`2500만`→`25백만`)은 **가독성만 깎고 넘침은 남는다.** 캡션에 이미 `▎예상 · ┆시세` 범례가 있으므로 **금액을 그 범례로 옮기고 차트에는 마커만** 두면 정보 손실 없이 해결된다. **★ 2026-09-23 정정 — 공개 사용자에게는 해당하지 않는다.** 그 마커는 `app.py:889`·`report.html:1204` 로 **관리자 전용**이다. 공개 세션으로 운영 리포트 8건을 받아 보니 마커 라벨 **0/8**. 즉 **내 측정이 관리자 세션이었다**(로컬 서버는 XFF 가 없어 `is_admin` 이 참이 된다) — 내가 늘 강조한 *"무엇을 어떤 조건에서 쟀는가"*(CLAUDE.md §2.4)를 내가 어겼다. 우선순위를 낮춘다: 공개 화면 결함이 아니라 **관리자 화면 결함**이다. 360px 의 원인 미상 70px 도 같은 이유로 공개 화면 기준 재측정이 먼저다. **★ 2026-09-23 공개 기준 재측정 완료** — 비로그인 세션으로 목록·상세·리포트를 320·360·390·430·768·1440px 에서 다시 쟀더니 **전 폭 0px**. 360px 의 '원인 미상 70px' 도 공개 화면에는 없다. 이 티켓은 **관리자 화면 전용**으로 확정하고 우선순위를 최하로 둔다 |
| PANEL-07 | **오차를 모르면 `±30%` 라는 지어낸 숫자를 찍는다** | frontend | **완료** | `report.html`(음영 범례·참고 정확도·면책문)·`vehicles.html`(표 머리글)에 `{{ … if … else 30 }}` 폴백이 있다. 값이 없을 때 **실측이 아닌 30%가 정확도인 양** 표시된다 — 이 회사가 금지한 *"데이터를 추정치로 채우기"* 다(표본이 없으면 '미산출'이 옳다). design-critic 2026-09-23 지적, *"이번에 고친 '같은 값이 화면마다 다른 숫자'와 같은 계열의 위험"*. **먼저 이 분기가 실제로 렌더되는 조건인지 재현**한 뒤, 맞으면 숫자 대신 '미산출'로 바꾼다. **★ 2026-09-23 재현 완료 — 실재하고, 공개 화면에서 찍힌다.** 공개 세션으로 운영 리포트 8건을 받아 `±30%` 가 **7/8건**에 인쇄되는 것을 확인했다(`2026타경70210_1`·`2025타경73940_1`·`2026타경10616_1`·`2026타경61769_1`·`2026타경30644_1`·`2026타경30485_1`·`2025타경34553_1`). 조사 담당은 "운영 미렌더"로 판정했으나 **틀렸다** — 그는 `backtest.mae_pct`(전체 MAE, 표본 140건이라 값이 있다)만 봤고, `else 30` 을 때리는 것은 **`report.mae`** 다(시세 없는 물건에서 `report_data` 가 `None` 을 반환). **판정을 `출시 전 진행` 으로 뒤집는다** — 표본이 없으면 '미산출'이 옳다는 원칙의 정면 위반이고, 지어낸 30% 를 정확도로 읽히게 두는 것은 이 제품이 파는 '정직'을 직접 깎는다. **★ 2026-09-23 수정 완료** — 네 자리에서 `else 30` 제거(`report.html` 음영 범례·참고 정확도·면책문, `vehicles.html` 머리글은 **`title` 속성과 화면 글자 두 군데** 다 물려 있었다). 음영 범례의 else 는 **죽은 가지라 삭제**했다: `dist.band` 는 `if exp and mae` 일 때만 생기고(`service.py:4528`) 그 `mae` 와 `report.mae` 는 **같은 `bt["mae_pct"]`**(`:4671` · `app.py:882-885`) — band 가 있으면 mae 도 반드시 있다. 게다가 `dist` 는 `app.py:889` 로 관리자 전용이다. 머리글은 값이 없으면 **오차 표기를 생략**한다(`whitespace-nowrap w-[158px]` 이라 긴 말이 안 들어간다). `tests/test_mae_parity.py` 에 **소스 금지 테스트**를 추가하고 결함 주입으로 빨간불을 확인했다 — 그 분기는 시세가 없는 물건에서만 열려 렌더 검사로는 못 잡는다. **⚠ 위의 '7/8건' 은 못 믿을 수치다** — `동급 (연식±1·주행±30%)` 라는 매칭 허용범위를 오차로 집는 함정(`test_landing_parity.py:93-94` 가 경고하는 바로 그것)에 걸렸을 수 있다. **면책문 자리만 정확히 다시 셌다**(비로그인·로컬·기일 순 40건): 괄호 없음 **21건** · 실측값 19건 — 고치기 전 **40건 중 21건(52.5%)** 이 지어낸 `±30%` 를 인쇄하고 있었다. 같은 40건 중 **17건**에 '주행 ±30%' 가 들어 있어 naive 계수는 실제로 오염된다 |
| PANEL-08 | **소프트캡 배율 `1.10` 이라는 값 자체** — 실측 IQR 상단 0.86과 30%p 괴리 | backend | **오너 승인 대기** | ★ **PANEL-03 이 이 지적을 덮을 뻔했다.** 패널이 **7회 연속**(1~7회차 전부) 요구한 것은 자리가 아니라 **값**이다(0.86~1.00 권고). PANEL-03 은 `config.yaml` 외부화만 닫았고 본문에 "값은 1.10 그대로"라고 내 손으로 적었다 — 그대로 두면 7회 연속 지적이 티켓 없이 사라진다. 1회차가 이미 `P0-4 ⏳미결 — 산정 정책이라 사용자 결정 필요`로 분류했다. 근거: `docs/reviews/2026-09-12.md:43-44,102` · `-r2.md:240` · `-r3.md:136` · `-r4.md:117` · `-r5.md:147` · `2026-09-22.md:72,94` · `2026-09-23.md:63,92`. 코드: `service.py` `SOFT_CAP_RATIO`·`soft_cap()`·`config.yaml soft_cap_ratio`. **Steward 권한 밖**(CLAUDE.md 승인 필수 — 판정 기준·산정 정책). 예상낙찰가 상한을 시세의 110%까지 여는 값이라 **사용자 입찰가에 직접 반영**된다. **★ 2026-09-23 근거 숫자 확보 — 오너가 숫자로 결정할 수 있게 됐다**(`reports/panel08-softcap-accuracy-strata.md`, `insight` + Steward 원문 대조). **㉠ 패널이 옳았다.** `backtest_stats()` 의 discount 정의(낙찰가 ÷ 시세중앙값)를 `sale_results` 135건(이상치 3건 제외)에 그대로 적용하니 **median 0.746 · p25 0.622 · p75 0.859** — 패널이 7회 연속 인용한 **"실측 IQR 상단 0.86"이 재현**됐다. 같은 데이터에서 낙찰가가 시세중앙값을 넘은 건 15/135(11.1%), **110%를 넘은 건 5/135(3.7%)뿐**이다. 패널이 원 사례로 든 6건(105~110%)은 **캡을 100% 이하로만 내려도 전부 해소**된다. **㉡ 배율별 비용(두 방향)** — 모집단 A: `expected_for` 주경로 562건 / 모집단 B: 낙찰 실적 135건. `0.86`: 캡이 낮추는 건 295(52.5%) · 법원 하한 복사본 **201(35.8%)** · 실낙찰이 캡 초과 **32(23.7%)** / `0.95`: 229(40.7%) · 152(27.0%) · 21(15.6%) / `1.00`: 198(35.2%) · 125(22.2%) · **15(11.1%)** / `1.10`(현재): 153(27.2%) · 77(13.7%) · 6(4.4%). **㉢ Steward 권고: `1.00`** — 패널 요구 범위(0.86~1.00)의 **상단**이다. 패널이 든 원 사례를 전부 해소하면서, "사용자가 우리 캡을 믿고 낮게 써서 실제로 졌을" 비용이 0.86 대비 **절반 이하**다. 1.00→0.86 구간은 이득(+17.3%p)보다 그 비용(+12.6%p)과 **'최저가 복사본' 증가(22.2%→35.8%)** 가 함께 커지는데, 그 복사본 문제는 **2회차 패널이 이미 지적한 별개 결함**이라 다시 키우면 안 된다. 규칙도 한 문장으로 설명된다 — *"시세 중앙값보다 비싸게 낙찰될 것이라고는 예측하지 않는다."* ⚠ **담당이 신뢰도를 스스로 '중간'으로 낮췄다** — 통계는 확실하나 최종 배율은 세 비용 사이의 가치 판단이다. 오너가 패널 문구 그대로 `0.86` 을 택한다면 **실낙찰 초과 23.7% · 최저가 복사본 35.8% 를 감수한다는 뜻**임을 함께 적는다. **★★ 선결 조건: `PANEL-29` 를 먼저 고쳐야 한다** — 백테스트 LOO 가 배율을 하드코딩하고 있어, 값만 바꾸면 예상가와 정확도 배지가 서로 다른 배율로 계산된다. **★★ 2026-09-23 선결 조건 해소(`cf5b6d1`) — 이제 오너가 값 하나만 정하면 된다.** `soft_cap_ratio()` 단일 원천으로 배선했고, **값을 바꾸면 예상낙찰가와 정확도 배지가 함께 움직임**을 회귀 테스트로 고정했다(배율 1.10·1.50 두 값에서 두 경로를 같은 테스트 안에서 잰다). 값은 **1.10 그대로** 두었다. **Steward 권고는 `1.00`** — 패널이 7회 연속 요구한 0.86~1.00 의 **상단**이고, 패널이 든 원 사례 6건(105~110%)을 전부 해소하면서 "사용자가 우리 캡을 믿고 낮게 써서 진" 비용이 0.86 대비 절반 이하다(11.1% vs 23.7%). ⚠ **0.86 을 택하면** 실낙찰 초과 23.7% · 최저가 복사본 35.8% 를 감수한다는 뜻이다(2회차 패널이 이미 지적한 별개 결함을 다시 키운다). **오너 결정만 남았다** |
| PANEL-10 | **오차 라벨이 모집단을 말해 주지 않는다** — 같은 이름이 전체평균과 유형별을 동시에 가리킨다 | frontend | **완료 · 색 예외 2건 판정됨 · 반영 진행 중** | `design-critic` 교차검수 2026-09-23 지적, **Steward 가 코드로 전수 재현함**(ORG §2.3). ⑴ `참고 정확도` — `report.html:1380` 은 `expected.acc.mae`(**유형별**), `vehicles.html:241` 은 `mae`(`app.py:539 _bt.get("mae_pct")`→`:615`, **전체평균**). ⑵ `실측 오차` — `detail.html:897` 은 `expected.mae`(`accuracy_for`, **유형별**), `dashboard.html:459` 는 `backtest.mae_pct`(**전체**). ⑶ 하필 **'평균'이 붙은 쪽이 더 좁은 모집단**이다 — 유형별에 `실측 평균오차`(`report.html:892·900·1363`)를 쓰고, 전체평균은 `평균 오차`(`dashboard.html:47`·`landing.html:97`·`accuracy.html:26`)·`예측 오차`(`report.html:1075`)·`예상 오차`(`landing.html:62`)로 부른다. 한 단어 차이로 반대 모집단을 가리켜 **거꾸로 읽힌다**. ⑷ 유보 어휘도 갈린다 — `참고 정확도 미산출`(`report.html:1380`) vs `검증 표본 부족`(`detail.html:408`) vs `표본 부족 — 미산출`(`accuracy.html:160`). 게다가 이 제품에서 `미산출` 은 **값 자체가 없다**는 뜻으로 학습돼 있어(육각형 축·목록 예상낙찰가), 금액이 찍힌 줄 옆에서 오독 위험이 있다. **★ 이 동음이의의 일부는 PANEL-01 수정이 만들었다** — `:1380` 을 유형별로 바꾸면서 목록의 같은 라벨은 전체평균으로 남겼다. 화면 **안**의 불일치를 고치며 화면 **사이**에 새로 만든 것이라, PANEL-01 과 한 묶음으로 닫아야 한다. **권고(design-critic)**: 라벨을 **두 체계로 고정**하고 모수를 병기한다 — 유형별은 `이 유형(수입, 35건) 실측오차`(`detail.html:408` 이 이미 쓰는 가장 정확한 형태), 전체는 `모델 전체 평균오차`. 최다 트래픽 화면인 목록(`vehicles.html:241`)이 **최우선** — 거기서 전체평균을 행 단위 예상낙찰가 옆에 붙이는 것이 `accuracy_for` 독스트링이 금지한 '표본에 없는 유형에 정확도 전이' 그 자체다. **★ 2026-09-23 절반 반영** — PANEL-01 과 같은 커밋에서 ⑴ 리포트 네 자리를 `이 유형(라벨, N건) 실측 오차` 로 통일(`참고 정확도` 라는 이름을 리포트에서 없앴다) ⑵ 모델 전체 타일은 `모델 전체 평균 · 이 물건 값 아님` ⑶ **목록 머리글에서 전체평균 숫자를 뺐다** — 행마다 다른 유형의 물건 위에 한 값을 얹는 것이 금지된 전이이고, `whitespace-nowrap w-[158px]` 이라 설명을 덧붙이면 폭이 깨진다(PANEL-07 교훈). 근거는 `title` 에 *"모델 전체 평균오차 ±8.9%. 이 값은 전체 평균이라 개별 물건의 정확도가 아닙니다"* 로 남겼다. 1440px 실측 머리글 **137×41 한 줄**(±를 빼 150→137px). **남은 것(출시 후)**: `dashboard.html:459` 의 `실측 오차`(전체평균인데 상세의 유형별 라벨과 **글자가 같다**) · `landing.html:62,97` · `accuracy.html:26` 의 `평균 오차`·`예상 오차` 계열 · 유보 어휘 통일(`미산출` vs `검증 표본 부족` vs `표본 부족 — 미산출`). **★ 2026-09-23 이름 결정 — `모델 전체 평균 오차` → `전체 평균 오차` 로 바꾼다(아직 미적용).** `design-critic` 지적: *"`현재 모델 전체 평균 오차` 는 **'현재 모델'** 로 붙어 읽힌다. **차 앱에서 `모델` 의 1차 의미는 예측모델이 아니라 차종**이다. 60·70대 독자에게 '현재 모델(=지금 이 차) 오차 ±9.1%' 로 읽히면 이번 개편이 없애려던 그 오해가 랜딩에서 그대로 재발한다."* **맞는 지적이고 내가 고른 말이다.** `이 유형(수입, 35건) 실측 오차` 와의 대비도 `전체 ↔ 이 유형` 으로 더 선명해진다. **적용 대상 7곳**: `dashboard.html:47`·`:463` · `landing.html:62`·`:97` · `accuracy.html:26`·`:83` · `vehicles.html:241`. ⚠ **전역 결정이라 한 번에 바꾼다** — 지금은 `vehicles.html` 을 다른 담당이 잡고 있어 대기. 같은 묶음으로 처리할 것 2건: ⑴ `landing.html:62` 의 `(정직 공개)` **삭제** — *"신뢰 제품이 스스로 '정직'이라 선언하면 근거보다 약해진다"*(`design-critic`), 지우면 고아 조각 `공개)` 가 사라져 타일 비대칭도 함께 해소 ⑵ `dashboard.html:463` 의 `<b>` 범위를 `예상낙찰가` 로 **축소** — 이름이 길어지며 굵은 덩어리가 1.5줄을 차지해 강조가 제 역할을 못 한다. **★ 2026-09-23 칩 색 부분 완료** — `vehicles.html` 의 `{% elif _tone == 'wait' %}bg-amber-500 text-white shadow-sm` **오버라이드를 제거**해 `{{ _tone\|tcls }}` 로 되돌렸다(`_nm` sky·`stop` 로즈 분기는 유지). ★ **담당이 내 지시의 전제를 두 번 뒤집었다**: ⑴ 내가 "`유찰 대기` 가 다수일 것"이라 썼으나 실측은 **0건**이고 최다는 `지난 기일 — 다음 기일 공고 대기` **713건** ⑵ 더 중요한 것 — **wait 은 예외가 아니라 목록의 91.7%(1,067/1,163)** 다(stop 72·6.2% / caution 16·1.4% / ok 8·0.7%). 즉 앰버 솔리드는 소수 상태 강조가 아니라 **기본값을 경고색으로 칠한 것**이었고, 같은 사진 안 `외관손상`(앰버)과 정면 충돌했다. **A/B 육안**(wait 12/12 인 페이지에 옛 규칙을 되칠해 비교): 바꾼 뒤 각 사진에 **앰버는 `외관손상` 하나만** 남고 빨강 `이번 회차 입찰 부적합`(72건)이 비로소 먼저 읽힌다. 대비 7.4:1. **Steward 재현**(390px 계산색): 앰버 `rgb(245,158,11)` **0건** — 빨강 6·회색 2·연앰버 1·sky 2·연초록 1 로 의도대로 갈렸다. 6폭 0px, 테스트 6파일 **106 passed**. ⚠ **남은 것**: ⑴ **라벨 전역 개명 7곳**(위 결정) ⑵ `(정직 공개)` 삭제 ⑶ `dashboard.html:463` 굵기 축소 ⑷ **범례가 칩과 어긋난다** — `vehicles.html:152` `유찰 대기` 는 `text-amber-700`, `:153` `신뢰도 낮음` 은 `text-sky-700` 인데 실제 칩은 이제 둘 다 회색이다(이 불일치는 **이번 변경 이전부터 있었다**). ⑸ `_nm`(동급 시세 없음, 5건)만 wait 계열 중 색이 다르다 — 의도된 예외이나 재검토 여지. **★ 2026-09-23 잔여 전부 완료 — Steward 소스 재현 확인.** ⑴ **라벨 개명 7곳**(`dashboard.html:47`·`:463` · `landing.html:62`·`:97` · `accuracy.html:26`·`:83` aria-label · `vehicles.html:248` title) — `모델 전체 평균 오차` **잔존 0건**, 전부 `전체 평균 오차`. ⑵ `(정직 공개)` 삭제 — 390px 실측으로 **타일 비대칭이 실제로 해소**됐다(2×2 라벨 전부 1줄, 네 박스 높이 60px, y좌표 633.2/633.2/717.2/717.2 정렬). ⑶ `dashboard.html:463` 굵기를 `예상낙찰가` 한 낱말로 축소하되 괄호는 **색은 딥네이비 유지**(돈·숫자 토큰이라 앰버 본문색으로 떨어뜨리지 않음). ⑷ 범례 색 정합 — `유찰 대기`·`신뢰도 낮음` 을 `text-slate-600` 으로. **클래스가 아니라 계산된 색으로 대조**했다(칩·범례 모두 `rgb(71,85,105)`). ⑸ 범례에 `기일 경과` 신설. **검증**: 4화면 × 6폭 **24개 조합 전부 0px** · pytest **1099 passed** · DB 프로브 불변 · 390px 육안 3종. ★ **담당이 내 지시를 한 군데 바꿔 실행했고 그게 옳다** — 범례 문구를 전문(`기일 경과 — 결과 확인 전`)이 아니라 짧은 `기일 경과` 로 넣었다. 전문을 넣었으면 `tests/test_dday_after_bid_time.py:70` 의 `assert "기일 경과 — 결과 확인 전" in html` 가 **범례 때문에 영구 통과**해, 출시 블로커였던 "끝난 경매에 빨강 D-DAY" 재발을 못 잡는 **공허 통과 가드**가 됐을 것이다. 렌더 실측으로 격리 확인(전문 0회, 범례발 짧은 말 3회). **⚠ Steward 가 추가로 확인한 부작용 2건(둘 다 비차단)**: ㉠ `:78` 의 공존 단언 `not ("D-DAY 입찰" in html and "기일 경과" in html)` 은 **짧은 말**을 쓰므로 범례 탓에 두 번째 항이 상시 참이 되어 사실상 `"D-DAY 입찰" not in html` 로 줄었다 — 다만 이 테스트는 **끝난 물건만 렌더**하고 대조군(`:82`)은 별도 단언을 쓰므로 거짓 실패는 없고 회귀 탐지력도 유지된다(표현력만 약화) ㉡ `_badges()`(`:58`)가 `html.find("기일 경과")` 로 실패 발췌를 뜨는데 **범례가 먼저 걸릴 수 있어**, 실패 시 문제의 카드가 아니라 범례를 보여줄 수 있다(진단 품질 저하). **남은 색 판단 2건**: `_nm`(동급 시세 없음) 카드 칩이 사진 위 특수 처리로 sky(`rgb(186,230,253)`)인데 표 칩은 회색 · 범례 `건설기계·선박 등`(`vehicles.html:163`)은 `text-sky-700` 이라 **표 칩과 카드 칩 중 어디에 맞출지**가 색 규칙 결정이다 — 담당이 임의로 정하지 않고 남겼다. **★ 2026-09-23 색 예외 2건 판정**(`design-critic` 교차검수 + Steward 코드 재현) — **sky 는 빠뜨린 게 아니라 의도된 구분이다.** `vehicles.html:409-412` 주석이 색 규칙을 이미 명문화해 뒀다: **일정=보라 / 상태경고=앰버 / 해당없음=sky**. `_nm`(동급 시세 없음)은 경고가 아니라 '해당 없음'이고, sky 가 카드 칩·`detail.html:40`·`:913-914` 네 자리에서 반복된다. 범례를 회색으로 통일하면 오히려 상세·카드와 어긋난다 → **범례 sky 유지.** **진짜 어긋난 자리는 `vehicles.html:312`(데스크톱 표 칩)** 다 — `_nm` 분기가 없어 `\|tcls` 회색으로만 나가므로 1024px 이상에서 **범례 sky ↔ 표 칩 회색**이 된다. 고칠 대상은 표 칩이다. **★ 같은 뿌리 3곳 추가 접수**(Steward 가 `grep -rn "bcls" web/templates/` 로 재현, 1440·320 캡처로 육안 확인): `detail.html:41` · `watchlist.html:66` · `watchlist.html:88` 이 아직 레거시 `{{ v.judgment\|bcls }}` 를 쓴다. `_bcls`(`app.py:334-342`)는 **judgment 문자열**로 색을 고르는 옛 방식이라 `유찰 대기`→앰버, `시세 신뢰도 낮음`→sky 인데, 목록은 `bid_state` 톤(`_TONE_CLS`, `app.py:320-331`)을 써서 셋 다 `wait`→회색이다. **즉 `유찰 대기` 라는 같은 글자가 목록은 회색, 즐겨찾기는 앰버다** — 변경 논거였던 "같은 상태를 두 색으로 말하지 않는다"가 제거된 게 아니라 **화면 밖으로 옮겨졌다.** ⚠ `_bcls` 삭제 여부는 잔존 사용처를 센 뒤 별도 판단(필터 등록 `app.py:394` 포함). 지시서 2026-09-23-23 으로 `frontend-engineer` 에 배치 |
| PANEL-09 | **낙찰확률의 분모가 없다** — `sale_results` 가 낙찰만 기록해 유찰이 분모에서 빠진다 | backend | todo | ★ **PANEL-02 가 이 지적을 덮을 뻔했다.** PANEL-02 는 '고지 완료'로 닫으면서 본문 끝에 "분모 자체는 그대로 — 데이터 모델 개선은 **별도 과제로 남는다**"라고 적었는데 그 별도 과제에 티켓이 없었다. 화면 고지는 증상 완화이고 원인은 그대로다. 코드: `service.py:2103-2138` `record_sale_result`(`label=="낙찰"` 일 때만 기록) · `db.py:117-137` · `service.py:3616` `win_probability()`. 근거: `docs/reviews/2026-09-22.md:70-71,93`(6회차 연속 미해결) · `2026-09-12-r5.md:130,151`(경매 점수 −16 의 단일 원인으로 지목) · `2026-09-23.md:58-62,91`. **편향 방향이 과다 입찰** 쪽이라 금전 손실로 기운다. 수집 시 유찰 기일도 기록하는 스키마 변경이 필요 |
| PANEL-11 | ★ **테스트가 운영 DB에 쓴다** — 경고인 줄 알았던 것의 실체 | qa | **구조 차단 완료 · 잔여 2건** | 2026-09-23 PANEL-01 작업 중 발견. `pytest` 전체 실행 출력에 `PytestUnhandledThreadExceptionWarning: Exception in thread Thread-366 (backfill_sale_results)` → `service.py:3741` → `db.record_sale_result` → `sqlite3.OperationalError: no such table: sale_results` 가 찍힌다. **실패는 아니다**(1,086 passed). ⚠ **원인을 단정하지 못했다** — 파일 단독 실행 0회, 신규 파일+기존 파일 쌍 실행 0회, 신규 파일을 제외한 전체 0회인데 **전체 실행에서만** 나타나고 경고 총수도 277↔276 으로 흔들린다(비결정적). 스레드 번호가 366 인 것으로 보아 후반부 경합으로 보이나 **추정이다.** 위험: 백그라운드 스레드의 예외가 **삼켜지므로**, 운영에서 같은 경로가 조용히 실패해도 아무도 모른다. `backfill_sale_results` 는 PANEL-09(낙찰확률 분모)가 손댈 바로 그 테이블을 쓴다 — 그 티켓과 함께 보는 것이 효율적이다. **★ 2026-09-23 원인 규명 — 경고보다 나쁘다.** `qa-engineer` 조사 + **Steward 재현**: `web/app.py:364` 가 띄우는 **데몬 스레드를 lifespan 종료 때 아무도 join 하지 않는다.** 테스트가 끝난 뒤에도 살아남아, `monkeypatch` 가 풀려 `DB_PATH` 가 **실제 `data/auction.db` 로 복원된 뒤** INSERT 한다. 운영 DB 엔 테이블이 있으니 **경고조차 없이 조용히 성공**한다. **실측(읽기 전용 조회)**: `sale_results` 140행 중 **2행이 테스트 픽스처 행** — id 길이 2자 1행(`W1`)·5자 1행(`DONE1`), 나머지 138행은 10~14자 실제 사건키. `recorded_at` 이 실행마다 갱신됐고(`DONE1` 15:08:38 → `W1` **15:09:12**), **조사자가 마지막으로 본 시각 이후에도 또 쓰였다 — 지금도 진행 중이다.** `W1` 의 기일이 2026-08-01 이라 **'지난달 낙찰 실적' 101행 중 1행**이 거짓이다(수치 영향은 아직 반올림 아래 — 학습표본 137→135, `discount_median` 0.746 동일). ⚠ **부수 발견**: 같은 경합이 경고가 아니라 **테스트 ERROR** 로도 터진다(`database is locked`, 8회 중 1회) — **"항상 초록"이 아니다.** 그리고 백필 4종 전부 `try/except` 가 없어 **데몬 스레드의 예외가 삼켜진다** — 운영에서 같은 경로가 조용히 실패해도 아무도 모른다. **조치 순서**: ⑴ 원인 차단(진행 중, 테스트 5개 파일의 lifespan 미가동) ⑵ 검증은 `recorded_at` 불변으로 ⑶ 그 뒤 `W1`·`DONE1` 삭제(백업 후 Steward 가 직접) ⑷ 별도 티켓으로 `NC_NO_BACKGROUND` 가드와 예외 로깅 래퍼. **★ 차단 전까지 전체 pytest 를 돌리지 않는다** — 매 실행이 운영 DB 에 쓴다. **★ 2026-09-23 차단 완료(A-1)** — `with TestClient(app)`(lifespan 가동)을 쓰던 **테스트 5개 파일**(`test_landing_parity` · `test_mae_parity` · `test_mae_stratum_parity` · `test_seo_entrances` · `test_share_preview`)을 `yield TestClient(app)` 로 바꾸고 각 파일에 이유 주석을 남겼다. 프로덕션(`app.py`·`service.py`·`db.py`·`conftest.py`)은 **무접촉**. **검증**: `recorded_at` 이 전체 pytest 1회 + 5파일 3회 반복 내내 **불변**(`DONE1` 15:08:38 · `W1` 15:09:12 고정, 총 140행 유지), `sale_results` 경고 0건, ERROR 0건, **1086 passed**. 검증 논리도 확인했다 — `db.py:912` 의 upsert 가 `ON CONFLICT DO UPDATE SET` 으로 모든 컬럼을 덮으므로 **재오염이 있었다면 `recorded_at` 이 반드시 움직인다.** ⚠ **남은 것 셋**: ⑴ `W1`·`DONE1` 삭제 — `vehicles` 에 둘 다 없어 실제 물건이 아님을 확인했고(지우면 학습표본 140→138, 2026-08 표본 101→100), **다른 담당이 그 두 행으로 오염을 검증 중이라 끝난 뒤 백업하고 지운다** ⑵ **A-2 가드**(`NC_NO_BACKGROUND`)가 없어 누군가 `with` 를 되돌리면 **재발한다** — 주석은 막지 못한다 ⑶ **`database is locked` 는 미해결** — 이번 4회에서는 재현되지 않았으나 원 관측이 간헐적이라 사라졌다고 볼 수 없다. ⚠ **조사 중 새 의문**: 조사자는 "138+테스트2=140 이라 정상 백필"로 추정했으나 Steward 실측으로 **`vehicles` 낙찰+낙찰가 보유가 257행** 이라 맞지 않는다 — **낙찰 119건이 표에 없다.** 별도 조사 중. **★ 2026-09-23 정리 완료** — 원인 차단이 검증된 뒤(다른 담당이 `W1`·`DONE1` 로 오염 여부를 검증 중이어서 그때까지 기다렸다) Steward 가 직접 처리했다. **백업 먼저**: `data/auction.db.bak-20260923-152702`(4,661,248 bytes)를 뜨고 **읽어서 검증**(`sale_results` 140행 · `vehicles` 1,418행 일치)한 **뒤에** 삭제했다. 결과: `sale_results` **140 → 138행**, 짧은 id 잔존 **0건**, 2026-08 표본 **101 → 100행**. ⚠ **남은 것 셋은 그대로다**: A-2 가드(`NC_NO_BACKGROUND`) 부재로 누가 `with TestClient(app)` 를 되돌리면 **재발** · `database is locked` 미해결 · 백필 4종에 `try/except` 가 없어 **데몬 스레드 예외가 삼켜진다**. **★ 2026-09-23 — 차단은 절반이었다. 진짜 출혈은 따로 있다.** 테스트를 막은 뒤에도 `recorded_at` 이 15:10 → **15:27 로 134행 갱신**됐다(DB mtime 15:27:52 일치). 원인: **`app.py:386-399` 의 `@app.on_event("startup")` 이 데몬 스레드 4종을 띄우고**, 그중 `backfill_sale_results`(`service.py:3736`)가 `db.list_vehicles(result="낙찰")` 전량을 `ON CONFLICT DO UPDATE`(`db.py:901`)로 **재upsert** 한다. 즉 **테스트가 아니라 '앱을 켜는 행위' 자체가 운영 낙찰 이력의 기록 시각을 덮어쓴다** — 로컬 검증용 기동도 예외가 아니다. ★ **Steward 가 설계한 검증 프로브가 이것을 못 잡았다** — `where length(id)<=6` 은 테스트 픽스처 행만 보므로 "쓰레기 행이 다시 안 생겼다"만 증명했지 **쓰기가 없었다는 증명이 아니었다.** 프로브를 `select count(*), max(recorded_at)` 로 바꾼다. **조치(진행 중)**: `NC_NO_SCHEDULER` 와 같은 관례의 기동 백필 가드 + 백필 4종 예외 로깅 래퍼(`try/except` 가 하나도 없어 데몬 스레드에서 죽으면 흔적이 0이다). **★ 2026-09-23 구조 차단 완료 — Steward 전 항목 재현 확인.** ⑴ **가드** `NC_NO_BACKGROUND`(기존 `NC_NO_SCHEDULER` 관례에 맞춤)를 `app.py:_startup()` 의 `init_db()`·`clear_orphaned_runs()`·`start_scheduler()` **뒤**, 스레드 기동 **앞**에 두었다. 주석에 실측 근거(138행 중 134행이 15:10→15:27 로 갱신)를 박았다. ⑵ **예외 래퍼** `service.background_task`(`:2034`) 신설 — 예외를 **다시 던지지 않되 반드시 기록**한다(한 백필이 죽어도 나머지 3종은 돌아야 한다). ★ 구현 중 한 번 고쳤다: 첫 판은 **로거 이름이 안 찍혔다** — uvicorn 이 root 로거에 핸들러를 달지 않아 기본 `lastResort` 가 `%(message)s` 만 찍는다(실측). 메시지 본문에 `[naechaget.background]` 태그를 넣어 journald 에서 grep 되게 했다. ⑶ **`conftest.py:26-27` 에서 두 값을 모듈 import 시점에 강제 대입** — 픽스처가 아니라 lifespan 보다 먼저 서므로, 누가 `yield TestClient(app)` 를 `with` 로 되돌려도 **구조로** 막힌다(A-1 의 주석 방어를 대체). ⑷ 신규 `tests/test_startup_backfill_guard.py` 6건 — 가드 ON 차단 / **가드 OFF 시 4종이 모두 뜸**(운영 백필 회귀 방지) / conftest 상시 적용 / 래퍼 로깅. **검증 A/B**: 가드 ON·운영 DB → `(138, 15:27:52)` **불변** · 가드 OFF·**사본** → `15:27:52 → 15:40:16` **쓰기 발생**. ★ **담당이 1차 사보타주가 무효임을 스스로 잡았다** — 포트가 다른 담당 서버에 선점돼 **서버가 뜨지도 못한 채** "쓰기 없음"이 나와 가드가 일한 것처럼 보였다. 바인드 선점 검사와 `served=False` 무효 처리를 넣고 다시 돌렸다. 사보타주를 **사본에서** 한 판단도 옳다(가드를 끈 서버가 운영 DB 를 덮어쓰게 둘 수 없다). **운영 무영향 확인**: `Procfile`·`docs/DEPLOY.md`·`.env.example`·`deploy/` 어디에도 이 변수가 없어 **미설정 = 백필 정상 수행**. 테스트 7파일 **47 passed**. **★ 프로브 교체 완료** — 앞으로 `where length(id)<=6`(픽스처 행만 봄) 대신 **`select count(*), max(recorded_at) from sale_results`** 를 쓴다. 기준값 **138행 / 2026-09-23 15:27:52**. ⚠ **잔여 2건**: `database is locked` 간헐 ERROR **미해결**(이번엔 재현 안 됐으나 사라진 증거 아님) · 스크래치에 운영 DB 사본이 권한 문제로 남아 있다(`scratchpad/dbcopy/auction.db`, 세션 격리 임시 디렉터리) |
| PANEL-12 | ★ **출시 블로커** — 끝난 경매에 빨강 "D-DAY 입찰" 배지가 붙는다 | backend | **수정 완료 · 검수 조건 3건 중 1건 해결 · 2건 남음** | `app-qa-auditor` 감사 + **Steward 가 운영에서 직접 재현**(`https://naechaget.co.kr/vehicles?date=2026-09-23`, 15:11:07 KST): `D-DAY 입찰` **12회** · `기일 경과` 24회 · 카드 단위로 **둘을 동시에 말하는 카드 12장** · `D-DAY` 만 있는 카드 **0장**. 즉 **배지가 붙은 카드 100% 가 모순 상태**다. 해당 물건 입찰 시각은 **09:55~10:30** 으로 조회 시점보다 5시간 전에 끝났다. **빨강은 이 앱에서 가장 센 신호**이고, 그 빨강이 "오늘 입찰"이라 말하는 차를 보고 법원에 가면 이미 끝난 경매다. 한 카드가 반대되는 두 말을 하니 사용자는 어느 쪽도 못 믿는다. **판정 엔진은 멀쩡하다** — `service.py:3804` 가 `sale_time_passed()` 로 시각을 올바르게 본다. 문제는 **그 값을 안 쓰는 두 곳**: `app.py:596-600`(D-day 를 `sale_date - today` 로만 계산) · `app.py:229-241` `_display_judgment()`(`sale_date < today` 만 보고 `sale_time` 미독). 상세 상단 배지(`detail.html:41` 의 `v.judgment`)도 같은 뿌리라 `2025타경33329_1` 은 상단 "유찰 대기" + 히어로 칩 "기일 경과"가 동시에 뜨고, `2026타경30666_1` 은 예상낙찰가 미산출이라 **"기일 경과" 가 0회**이고 상태 표시가 "유찰 대기" 하나뿐이다. **DoD**: 두 곳이 `sale_time_passed()` 를 함께 보고, 시각이 지나면 빨강 배지를 끄고 판정 칩 하나만 남긴다. **시각을 모르는 물건은 현행대로 보수적으로 둔다**(운영 표본 기준 절반 가까이). `app-qa-auditor` 루브릭 **77/100**, 합격선 미달 사유가 이 한 항목이다. **★ 2026-09-23 수정 완료 · 교차검수 2건 회신 — 둘 다 '조건부'.** 표시 계층이 판정 엔진과 **같은 함수**(`sale_time_passed`)를 보게 했다(`app.py:233` `ELAPSED_LABEL` + `:236-249` `_bidding_over` + `:639-640` D-day `None` + `_display_judgment` 한 줄). **판정 엔진은 손대지 않았다.** `service.py:3805` 라벨과 한 글자도 다르지 않게 상수로 묶고 드리프트 가드 테스트를 붙였다. **Steward 재현**: 운영 `D-DAY 입찰` 12회·동시 표출 12/12장 → 로컬 **0회·0장**, `bg-rose-500` 12→0, `기일 경과` 24→31(데스크톱 표 판정 셀 7행 갱신). 오늘 기일인데 시각을 모르는 물건 **0건**(보수 분기 유지). 관련 테스트 11파일 **203 passed**. **★ 검수 조건 — 두 자리가 색에서 갈렸고 Steward 가 코드로 가렸다**: `design-critic` 은 **회색**(근거 `service.py` 주석 *"톤은 wait(회색) — 경고가 아니라 **해당 없음**이다. 빨강·앰버를 쓰면 같은 화면의 실제 경고가 희석된다"*), `app-design-expert` 는 **앰버**(목록과 통일)를 냈다. **확인 결과 `design-critic` 이 맞다** — `_TONE_CLS["wait"]` 가 `bg-slate-100` 이므로 상세 회색은 **규칙대로**이고(히어로 칩은 `_tcls(tone)` 경로), 규칙을 어긴 쪽은 `vehicles.html:410` 의 `{% elif _tone == 'wait' %}bg-amber-500` **오버라이드**다. 같은 카드에 `외관손상`(`:417`)이 이미 앰버라 **겹치기까지 한다.** → 별건으로 수정 중. **남은 조건 3건**: ⑴ **즐겨찾기·대시보드에 같은 게이트가 없다**(`design-critic` P0) → **PANEL-16** ⑵ 목록 칩이 `whitespace-nowrap` 이라 히어로가 쓰는 `ui.judge_label`(' — ' 뒤 접기)을 안 써서 **320px·큰글씨 잘림 미확인** ⑶ `app-design-expert`: 목록 상단 범례 6종에 **이 상태만 없다**(지금 가장 많이 보이는 칩인데 설명이 없는 유일한 칩) · 카드 날짜 줄이 `2026-09-23` 뿐이라 *"오늘 기일이네, 지금 가면 되겠다"* 로 오독될 수 있어 **시각 병기**(`오늘 10:00 마감`) 제안 · 라벨을 `입찰 마감 — 낙찰 결과 확인 전` 으로 바꾸자는 제안은 **`app.py:233`·`service.py:3805`·드리프트 가드 테스트를 함께 고쳐야 한다**. **★ 2026-09-23 진행** — ⑶의 **범례 누락은 해결**했다(`vehicles.html` 에 `기일 경과` 항목 신설). ⚠ 범례에는 **짧은 `기일 경과`** 만 넣었다. 칩 전문(`기일 경과 — 결과 확인 전`)을 항상 렌더되는 범례에 넣으면 `tests/test_dday_after_bid_time.py:70` 의 칩 검사가 **영원히 통과**한다(가짜 합격) — 하위 담당이 이 함정을 먼저 잡았고 그대로 유지한다. **남은 2건**: ㉠ 카드 날짜 줄에 **시각 병기**(`2026-09-23 (오늘 10:00 마감)`) — 날짜만 있으면 *"오늘 기일이네, 지금 가면 되겠다"* 로 오독된다 ㉡ 목록 칩이 `ui.judge_label`(' — ' 뒤 접기)을 안 써서 **320px 큰글씨 잘림 미확인**. **둘 다 지시서 2026-09-23-23 범위에 넣지 않았다 — 다음 회차 배치 대상이다**(부분 해결을 '완료'로 쓰지 않는다, 운영 규칙 7) |
| PANEL-13 | 스토어 등록 문구의 정확도 수치가 **라이브와 다르다** | 성장/오너 | todo | `compliance-officer` 지적 + **Steward 가 라이브 직접 조회**(`/accuracy`, 2026-09-23): 라이브는 **평균 오차 ±9.5% · 검증 표본 275건 · ±20% 적중 94% · ±10% 적중 61%** 인데 `docs/STORE_LISTING.md` 는 **±9.3% · 249건 · 95%** 다. 근거: Play Misrepresentation 정책(메타데이터 정확성, 삭제·정지 사유) · 표시광고법 제3조·제5조(실증책임, 요청 시 15일). **실증자료는 있으니 숫자 교체와 기준일 병기로 끝나고 콘솔에서 재빌드 없이 즉시 반영된다.** ⚠ **구조적 문제가 더 크다** — 이 수치는 계속 움직인다(오늘 하루에도 로컬 9.1 · 주간리포트 9.3 · 라이브 9.5). **고정 숫자를 문서에 박는 방식 자체**를 바꾸거나, 출시 직전 재확인을 절차로 박아야 한다. **미확인**: Play Console 에 실제 저장된 문구는 오너만 볼 수 있다 |
| PANEL-14 | `/privacy` — §2 이용 목적이 **없는 기능을 현재형**으로 적고, 개인정보 보호책임자가 없다 | frontend/compliance | **§2 수정 완료 · 나머지 오너 승인 대기** | ⑴ **§2 이용 목적**(`privacy.html:57-58`)이 `서비스 제공·본인 식별·등급(무료/유료) 관리 및 결제 처리` · `매각기일 알림 등 이용자가 설정한 기능 제공` 을 **현재형**으로 적는다. 로그인·결제가 없는데 현재 목적으로 서술한 것이고, 바로 `:49` 가 Play 에 **"수집 없음"으로 신고했다**고 명시해 **문서 안에서 앞뒤가 맞지 않는다.** §1 표(`:46-48`)·§3(`:65`)·§5(`:85`)는 `(예정)` 이 붙어 있어 **§2만 빠졌다.** ★ 3회차 지적(광고 개인화 거부·회원 탈퇴 허위 서술)은 **이미 해소됐다** — 이건 그때 지적과 **다른, 새로 발견된 건**이다(`growth`·`compliance` 가 "해소"라 본 것은 그 옛 지적을 말한 것이고 둘 다 맞다). ⑵ `compliance-officer`: 개인정보 보호법 제30조 제1항 제6호가 **보호책임자 성명·부서·연락처**를 방침 필수 기재사항으로 규정한다. 제31조상 소상공인은 별도 지정을 안 해도 되나 그 경우 **대표자가 보호책임자**가 되므로 '없음'이 아니라 '대표자'로 적어야 한다는 뜻으로 읽힌다. **법 해석은 변호사 확인 사항으로 남긴다**. **★ 2026-09-23 ⑴ 수정 완료** — `privacy.html:57-59`. 담당이 지시보다 한 걸음 더 나가 **"매각기일 알림"의 실체를 코드로 가렸다**: 화면 안 D-3 임박 카드는 **실재**하고(`service.py:4110 alert_items` · `dashboard.html:298,301,304` · `base.html:461`), **푸시는 없다**(`web/static/sw.js` 에 `push`·`Notification`·`showNotification`·`subscribe`·`pushManager` **전부 0건**). 게다가 **"이용자가 설정한"도 틀렸다** — `alert_items` 주석이 *"즐겨찾기는 기기 로컬 저장이라 서버가 알 수 없음"* 이라 D-3 카드는 전 이용자 공통이고 설정 대상이 아니다. 그래서 **있는 것은 살리고(화면 내 표시)·없는 것은 명시적으로 부정하고(푸시 안 보냄)·설정 항목은 로컬 저장이라는 §1 기재와 일치**시켜 3줄로 분리했다. 표기법은 바로 아래 `:61` 의 `(예정 — 설명)` 과 동일하게 맞췄다(새 표기 발명 없음). **Steward 가 전부 재현 확인.** 검증: 390px 육안·가로 넘침 0 · `test_render_smoke`+`test_no_visitor_tracking` **63 passed** · `recorded_at` 불변 3회 · `npm run build:css` 후 **app.css md5 동일**(새 클래스 없음). **★ 오너 결정 2건**: ⑴ **시행일** — `app.py:497` 의 `"updated": "2026-09-11"` 이 템플릿 `:37`·`:105` 에 렌더된다. **데이터 처리 실태는 바뀌지 않았고**(여전히 수집 없음) 기술이 실태에 맞게 정정된 것뿐이라 **시행일을 올리면 '정책이 바뀌었다'는 잘못된 신호**가 된다. 그렇다고 두면 "2026-09-11 시행 문구"와 실제 문구가 다르다. **권고: 시행일은 유지하고 '최종 수정일'을 따로 둔다.** 공개 법적 문서의 구조 변경이라 Steward 가 단독 결정하지 않는다 ⑵ **개인정보 보호책임자 기재** — 성명·연락처는 오너만 정할 수 있다 |
| PANEL-15 | ★ **1440px 목록 표에서 차량명이 한 글자씩 세로로 쪼개진다** | frontend | **완료** | **Steward 실측**(로컬 8018, 1440×900, 비로그인): `차량·사건` 칸이 **204x490**(행0)·**204x755**(행1)인데 안쪽 텍스트 노드의 **줄상자가 24개·34개**다(Range 로 실측). 약 28자가 24줄 — **한 줄에 한 글자**다. 같은 행의 `판정` 칸(182px)은 줄상자 1개로 정상이므로 **이 열에 한정**된다. 바깥 `td` 는 204px 인데 **안쪽 내용 상자가 한 글자 폭으로 눌린** 형태다. 행 높이 490~755px 이라 **두세 행이 화면을 꽉 채운다.** **재현**: `design-critic`(2026-09-23, `모/하/비`·`2/0/2/5/타` 로 보고) · `frontend-engineer`(`?bucket=nomarket` 과 대조군 `/vehicles` **양쪽 모두**) · Steward(위 수치). **★ 이 델타(app.css 재빌드) 탓이 아니다** — 새로 컴파일된 5규칙 중 2개는 매칭 DOM 0개이고 나머지 3개는 배너 div·홈 알약·상세 페이지만 건드린다. 미커밋 템플릿 수정분(오차 라벨 문구 교체)과도 무관하다. **커밋된 기존 결함**이다. ⚠ **Steward 가 이 지적을 한 번 기각했다** — 머리글이 41px 한 줄인 것만 보고 "캡처 왜곡"으로 종결했고, 490px 행은 "썸네일 든 카드형 행이라 정상"이라고 설명해 넘겼다. **셀 크기만 재고 안쪽 줄바꿈을 세지 않았다.** 독립된 두 보고를 얕은 측정 하나로 덮은 것이라, 원인 규명보다 **이 기각 과정이 더 비싼 교훈**이다. **DoD**: 원인 특정(내용 상자를 누르는 것이 무엇인가 — 플렉스/그리드 자식의 `min-width`·`width:min-content`·`word-break` 중 무엇인지 계산값으로) · 1440·1920px 에서 행 높이가 통상 범위로 복귀 · 320~1440 6폭 재측정 0px 유지 · **Range 기반 줄상자 수**로 검증(`getClientRects().length` 는 블록 요소에서 항상 1이라 거짓 안심이 된다). **★ 2026-09-23 원인 특정·수정·검증 완료.** **범인은 `min-w-0` + 썸네일 strip 의 `flex-shrink:0` 조합**이었다 — `word-break`·`width:min-content`·그리드 `min-width:auto` 는 전부 무죄(`wordBreak: keep-all` 정상). 1440px 계산값: `td` 204px → 안폭 **180px**, strip `width:176px`·`flex-shrink:0`, 텍스트 칸 `flex-shrink:1`·`min-width:0px`·**`width:0px`**. 176+gap 12 = **188 > 180** 인데 축소 가능한 자식이 텍스트 칸 하나뿐이고 `min-w-0` 이 최소폭 바닥을 없애 **문자 그대로 0px** 로 눌렸다. 폭 0 상자에서 한글은 한 글자씩 접힌다. ★ **진짜 설계 결함은 게이트 축**이다 — strip 을 `xl:`(뷰포트 1280px)로 켜는데 자리를 정하는 건 **열 폭**이고, 열 폭은 뷰포트를 따라가지 않는다(다른 열이 최소폭에 걸려 1480px 까지 210px 고정). **1920px 은 열이 638px 이라 원래 멀쩡했다 — 1440 전용 결함**이었다. **수정**: 게이트를 `min-[1720px]` 로(텍스트가 1920px 과 같아지는 첫 지점을 스위프로 찾음, 추정 아님) + `min-w-[160px]` 붕괴 방지 바닥 + `flex-wrap` 안전판. 컨테이너 쿼리는 **일부러 쓰지 않았다**(`container-type:inline-size` 가 내용 기반 폭 계산을 끊어 auto 테이블 배분이 뒤집힌다). **Steward 재현**(`NC_NO_BACKGROUND=1`, 1440px): 줄상자 **28·44·50 → 2·2·2**, 행높이 **490·755·875 → 103·103·103**, 칸 폭 0 → 200px, 6폭 전부 0px, 운영 DB 프로브 불변. `app.css` 델타 정확히 3줄(`xl:flex` 제거, `min-[1720px]:flex`·`min-w-[160px]` 추가). ⚠ **1440px 에서 썸네일은 사라진다** — 열이 204px 이라 176px strip 을 넣을 자리가 물리적으로 없다. 1장만 남기는 안도 계산했으나 텍스트가 112px 로 눌려 더 나빴다. **썸네일은 장식이고 금액 열이 제품**이라 숨기는 쪽을 택했다(lg~xl 구간의 기존 동작과 동일). **★ 2026-09-23 노출 범위 정정 — 수정은 유효하되, 이 표를 보는 사람이 우리가 생각한 사람이 아니었다.** `design-critic` 이 재판단했고 Steward 가 코드로 재현했다(PANEL-23 과 같은 뿌리). `base.html:6` 이 폰 프레임 스크립트를 `{% if not is_admin(request) %}` 로 감싸고, 그 스크립트가 데스크톱 브라우저를 `/static/frame.html` 로 보내며 거기 iframe 폭은 `min(420px, 94vw)`(`frame.html:20`)다. 데스크톱 표는 `vehicles.html:239` `hidden lg:block`(lg=1024px)이라 **420px iframe 에서는 절대 렌더되지 않는다.** 즉 이 결함을 실제로 본 사람은 ㉠ 관리자 ㉡ 데스크톱에 PWA 를 설치한 사용자뿐이고, 일반 데스크톱 브라우저 사용자와 안드로이드 TWA(폰 폭 390) 사용자는 **이 표를 본 적이 없다.** ⚠ **그렇다고 무의미한 수정이 아니다** — `frame.html:39` 가 *"PC에 **설치**하면 넓은 데스크톱 화면으로 열립니다"* 라며 **설치를 권유한다.** 노출 대상이 0명인 게 아니라 **우리가 늘리고 있는 집단**이다(`design-critic` 의 지적). **★ 이 티켓에서 가져갈 교훈은 수정 내용이 아니라 측정 조건이다** — 당시 "1440px 실측"은 전부 관리자 세션이었다(로컬 서버는 XFF 가 없어 `is_admin` 이 참). PANEL-06 에서 똑같이 당하고 이 문서에 적어 둔 함정을 반복했다. 앞으로 데스크톱 표 수치에는 **관리자/공개 조건을 반드시 병기**한다(PANEL-23 DoD) |
| PANEL-16 | D-day를 **날짜만으로** 계산하는 자리가 세 곳 더 남았다 | backend/frontend | **데이터층 완료(00763f1) · 화면 실측 통과 · 알림 경로는 운영 데이터로 미검증** | PANEL-12(출시 블로커)를 고치면서 **목록만** 닫았다. `backend-engineer` 가 범위 밖이라 남긴 것을 **Steward 가 코드로 확인**했다. ⑴ **`app.py:1206-1208`(즐겨찾기)** — `v["dday"] = (date.fromisoformat(v["sale_date"]) - today).days` 로 시각을 보지 않는다. ★ 여기는 단순 표시가 아니라 **`dday` 가 정렬 키를 겸한다**(`app.py:1171`). 목록과 같은 방식으로 `None` 을 넣으면 **정렬 순서가 바뀐다** — 그래서 구현자가 판단을 넘겼다. `None` 을 정렬에서 어디에 둘지(맨 뒤? 지난 것끼리 묶기?)를 **먼저 정하고** 고쳐야 한다. ⑵·⑶ **`dashboard.html:77`·`:318`**(홈 캐러셀·임박 알림 카드) — 같은 날짜 전용 계산. 홈은 첫 화면이라 **끝난 경매가 '임박'으로 떠 있으면 블로커와 같은 성격**이다. **DoD**: 세 곳이 `service.sale_time_passed()` 를 함께 보고, 즐겨찾기 정렬 규칙을 명시적으로 정한 뒤 회귀 테스트로 고정. 시각 미상은 **보수적으로 그대로 둔다**(PANEL-12 와 동일 원칙). **★ 2026-09-23 DoD 원문 대조 후 기록**(운영 규칙 7) — ⑴ 즐겨찾기 `app.py:1243-1251` 완료, **정렬 규칙을 명시적으로 결정**했다(`_dday_sort_key`, `:252-277`): 표시용 `dday`(경과 시 `None`)와 정렬용 값을 나누고 ① 아직 입찰 가능(기일순) ② 끝난 것(최근 끝난 순) ③ 기일 미상(맨 끝). 옛 규칙은 ②와 ③을 둘 다 `9999` 로 뭉개 순서가 **클라이언트 localStorage ID 순서**에 좌우됐다. ⑵⑶ **템플릿은 자기 날짜 계산을 하지 않는다** — Steward 가 코드로 확인: `dashboard.html:77` 은 `v.dday is not none` 을 이미 가드하고, `:318` 은 가드가 없어 `alert_items` 가 `None` 을 실어 보내지 않고 **항목 자체를 뺀다**(계약을 코드 주석과 테스트에 박았다). 백로그가 쓴 "같은 날짜 전용 계산"은 템플릿이 아니라 **그 줄에 먹이는 데이터**였다. 고친 자리는 `service.alert_items`·`alert_count`·`_pick_dict`·`get_daily_picks` 넷. 회귀 테스트 13건(`tests/test_dday_data_layer.py`), pytest 1112 passed. **화면 실측**(qa, 3화면 × 6폭): 끝난 경매의 빨강 D-DAY 잔존 **0**, 본문 넘침 0, 가로 스크롤 0, 정렬 관측 순서가 raw SQL 기대값과 일치. 라벨 12자는 320·360 에서 **넘치지 않는다**(여유 +33px·+25px). 담당이 **양성 대조**를 먼저 설계했다 — 사본 서버에서 실제 rose D-DAY 칩을 탐지시켜 "0건"이 탐지 실패가 아님을 증명. **startup 백필 가드가 실제 서버 기동에서 동작함을 처음으로 실증**(프로브 4회 전부 `(138, 2026-09-23 15:27:52)`). ⚠ **커밋 메시지 정정**: `알림 4→2 · 벨 4→2` 는 **사본 기준**이다. 운영 DB 에는 D-3 이내 물건이 0건(다음 기일 09-28=D-5)이라 `alert_items`/`alert_count` 경로가 **한 번도 실행되지 않았다**(운영 벨 0 · 홈 0). **D-3 물건이 생기는 시점에 벨==홈 재확인 필요.** **남은 것**: ㉠ 1440 즐겨찾기 표에서 라벨이 3줄로 접혀 행 높이 ~100px(정상 ~78px) ㉡ 320 카드에서 12자 nowrap 라벨이 제목을 자른다(`토레스 EVX 20…`, 390 이상은 정상) — 둘 다 지시서 2026-09-23-23 으로 `frontend-engineer` 에 배치 |
| PANEL-17 | ★ **낙찰가율이 '낙찰 전수'가 아니라 '시세 매칭에 성공한 낙찰'로만 계산된다** | backend/frontend | **⑴ 완료 · ⑵ 원인 계량 완료 · ⑶ 오너 승인 대기** | `insight` 조사 + **Steward 전수 재현**(읽기 전용, 2026-09-23). `sale_results` 포함 조건은 단 하나 — `winning_price and median_price`(`service.py:3736` · `db.py:901`). `_result_anomaly()` 는 이 저장 경로를 막지 않는다(독스트링과 실제 동작이 다르다 — 학습표본 선별에서만 쓰인다). **실측**: 낙찰+낙찰가 보유 **257건 중 `median_price` NULL 이 123건(48%)**, `sale_results` 실질 **138행**. ★ **사용자에게 보이는 결과**: '지난달 낙찰 실적' 카드(`service.py:4135 last_month_sale_stats`)가 **2026-08 낙찰 193건 중 100건(51.8%)** 으로만 낙찰가율을 계산한다. **빠진 쪽이 무작위가 아니다** — 결측 차종은 `maker/model` **공백 23건(파싱 실패)** · 토레스 3 · 콘크리트믹서 3 · 지게차 2 · 덤프 2 · 다마스밴 2 · 포터Ⅱ 2 로 **경상용차·건설기계·특장에 편중**되고, 그 사이에 **G80 · K7 하이브리드 · XM3 · 레이 · 카니발** 처럼 매칭 실패할 이유가 없어 보이는 흔한 승용차가 섞여 있다. 건설기계·특장 배제는 `no_market_reason()` 에 명문화된 **의도**지만 나머지는 근거가 코드 어디에도 없다. ⚠ **구조적으로 복구가 막혀 있다** — `service.py:4889` 의 `if v.get("status") in ("종결","상세없음") or v.get("auction_result") in ("낙찰","종결"):` 로 `requery_missing_market()` 이 낙찰·종결 물건을 재시도에서 **영구 제외**한다. 즉 **0표본 시세가 낙찰과 함께 박제**되고, 09-10·09-13 매핑 개선도 기존 0표본 물건엔 적용된 적이 없다(`service.py:4875-4879` 주석이 스스로 증언). **★ PANEL-09(낙찰확률 분모) 착수 전에 이 선택편향을 먼저 인지해야 한다** — 유찰이 분모에서 빠진 것만 고치면, 낙찰 쪽이 **52% 비무작위 표본**이라는 사실이 그대로 남는다. **조치 분리**: ⑴ **출시 전(값쌈, 진행 중)** — 화면이 이 수치의 **모수를 말하게** 한다 ⑵ 출시 후 — `no_market_reason()` 키워드 보강 · `maker/model` 파싱 실패 23건 원인 규명 ⑶ **오너 승인 필요** — 낙찰·종결 물건의 시세 소량 재조회를 허용할지(C.4 요청상한과 얽힌다). **★ 2026-09-23 ⑴ 완료** — `calendar.html:60,110-111`. 기존 문구 `실제 낙찰 {{ ss.n }}건 집계` 는 **그달 낙찰이 100건이라는 거짓 진술**이었다. 바꾼 뒤: 헤더 `{{ ss.label }} · 시세 확인 {{ ss.n }}건`, 각주 *"{{ ss.label }} 낙찰된 차 가운데 **시세를 구할 수 있었던 {{ ss.n }}건**만 넣어 계산했습니다. 시세를 구하지 못한 차는 빠져 있어, 그달 낙찰 전체와는 다릅니다."* ★ **담당이 두 번 옳게 판단했다**: ⑴ 분모(193)가 **템플릿 컨텍스트에 오지 않아**(`last_month_sale_stats` 반환 키에 없다) 지시서 예시 문구를 따르는 대신 **지어내지 않고 분모 없이** 썼다 ⑵ 빠진 차종(경상용차·건설기계·특장)을 **일부러 열거하지 않았다** — 8월 실측엔 맞지만 이 카드는 매달 렌더되어 다음 달엔 틀릴 문장이 되기 때문이다. `ss.label` 을 써서 **90일 폴백을 탈 때도 문장이 자동으로 맞는다.** 대비도 `white/45`(3.9:1, 기준 미달) → `white/70`(5.7:1)로 올렸다. **검증**: 6폭 전 폭 0px · 390px 육안(헤더 한 줄, 각주 3줄, 잘림 없음) · 관련 테스트 **94 passed** · `sale_results` **138행·`max(recorded_at)` 불변**(Steward 재확인). ⚠ **`service.py` 에 `sold_total` 한 키만 추가하면 "193건 중 100건" 이 된다** — ⑵ 에서 함께 본다. **★ 같은 뿌리 3곳 추가 접수**(표본을 전수인 양 말하는 자리): `accuracy.html:19`(`현재 누적 낙찰 표본 {{ bt.history_n }}건`) · `accuracy.html:46`(`누적 낙찰 히스토리`) · `dashboard.html:47`(`실제 낙찰 {{ backtest.pred_n }}건과 대조`). **★ 기존 문제 하나 더**: 표본이 얇아 90일 폴백을 타면 각주는 `ss.label` 덕에 정확하지만 **제목 알약은 여전히 "지난달 낙찰 실적"** 이라 값과 어긋난다. **★ 2026-09-23 ⑵ 원인 계량 완료**(`reports/panel17-nomarket-breakdown.md`, `insight` + Steward 상수 대조) — **㉠ 모집단부터 통일해야 한다.** 같은 "시세 없음"이 **684**(무필터) / **431**(목록에 실제로 뜨는 것, `hide_incomplete=True`) / **257**(낙찰+낙찰가 보유) / **123**(낙찰 & median NULL) 으로 갈린다. 앞으로 이 티켓의 모든 수치는 **노출 기준 431** 을 주 모집단으로 쓴다(전체 노출 1,163건의 37.1%). **㉡ `no_market_reason()` 이 431건의 80.3%(346건)를 설명하지 못한다** — 건설기계·특장 80건(18.6%), 선박 5건(1.2%), 나머지는 전부 기본값. 미커버 346건은 두 성격이 섞여 있다: 화물 표기 누락형(`봉고III 플러스냉동차`·`한성10.4톤 카고` — 상수에 `카고트럭` 4글자만 있고 `카고`·`냉동` 단독이 없다, **키워드 보강으로 회복 가능**)과 흔한 승용·수입차 매칭 실패형(`K7`·`XM3`·`레이`·`카니발`·`G80`·`BMW`·`MINI` — **키워드로는 못 고친다, 매칭 로직 별도 과제**). **㉢ "파싱 실패 23건"은 틀리지 않았으나 낙찰 한정 모집단에서만 맞다** — 노출 기준으로는 **18건**이고, 차이 6건은 `case_no='(병합)'` 이라 목록에서 항상 숨겨지는 중복 레코드다(설계대로). 18건 중 17건이 `year` NULL · `displacement_cc=0` · 디젤코드 · **연안·수산 관할 법원**(통영·포항·영덕·진주·해남·순천·홍성·군산)으로 지문이 같고, 그중 5건은 `spec_remark` 로 선박 확정, 12건은 `spec_remark` 가 비어 **선박 추정(직접 증거 없음)**. **㉣ 겹침**: 선박 버킷 5건은 파싱 실패와 **100% 겹치고**, 건설기계 80건은 **정의상 0% 겹친다**(`if m and any(...)` 라 `model` 이 비면 매칭 불가). 즉 `no_market_reason()` 을 고쳐도 파싱 실패 몫은 못 잡는다 — **법원 상세 파싱 단계** 조사가 따로 필요하다(원인 코드 미특정, 미착수). **㉤ ⑶ 되살릴 몫**: 낙찰·종결 **118건**(requery 영구 제외 → **오너 승인 필요**) / 아닌 것 313건. ⚠ **313 을 그대로 보고하면 기대치를 부풀린다** — `year` 없음 5, `auto_map()` 실패 57 을 빼면 251건이고, `daily_update` 기본 30일 창을 지금 통과하는 건 48건, 매핑까지 성공하는 건 **41건**이다. **★ Steward 재현 검증**: 담당이 상수를 정적 복사했다고 한계를 명시해 `service.py:1299`·`:1356`·`:1360-1369`·`:4948` 원문과 글자 단위로 대조 — **전부 일치**. 다만 담당이 적용하지 않은 게이트가 하나 더 있다(`:4952` `analyzed_at`/`collected_at` 백오프) → **41 은 상한이다. 오너 보고에는 "최대 41건"으로 쓴다** |
| PANEL-18 | **`market_ref_date` 가 1,418행 중 3행뿐인데 이름이 "시세 기준일"이다** — 운영 지표로 읽혀 오진을 만들었다 | backend | todo | **★ 이 티켓은 Steward 의 오진에서 나왔다.** 2026-09-23 나는 `max(market_ref_date)=2026-09-05` 를 읽고 **"시세 기준일이 18일 전"** 이라 판단해 **오너에게 배포 보류를 권고했다.** `app-qa-auditor` 가 반증했다(`reports/ops-collection-stall-2026-09-23.md`). **실측**: `market_ref_date` NOT NULL **3행 / 1,418행**, 시세(`median_price`)는 있는데 이 컬럼이 NULL 인 행 **731건**. 화면이 실제로 쓰는 값은 `service.py:3547` 의 `as_of = str(v.get("market_ref_date") or v.get("analyzed_at"))[:10]` 라 거의 모든 물건이 `analyzed_at` 으로 떨어진다. **3행짜리 컬럼의 최댓값을 운영 지표로 읽은 것이 오진의 직접 원인이다.** **DoD**: 셋 중 **하나로 확정**하고 빈 채로 두지 않는다 — ① 운영 점검 쿼리를 화면과 같은 식으로 고정 ② 분석 시 이 컬럼을 항상 채움 ③ 쓰지 않으면 스키마·지표 문서에서 제거. **곁들임**: 로컬 개발 서버 기동 시 화면에 **"개발 사본 · 데이터 기준일 YYYY-MM-DD"** 표식을 붙인다 — 같은 오진이 반복되지 않게. 근거: `runs` #61 `status=error` 원문 "시세 분석 27/80 · G80 (개발 서버 기동으로 의도치 않게 시작 — 수동 중단)", 로컬 `daily_enabled=0`·`last_run_date=2026-09-17`, `encar_health_code=200`(차단 아님) |
| PANEL-19 | **홈 '시세 갱신 지연' 배너가 전역 핑 1개만 보고 물건별 낡음을 축소해 말한다** | backend/frontend | todo | `service.encar_health_status()`(`:1683-1697`)의 `stale_days` 는 전역 설정 `encar_health_ok_at`(마지막 헬스 핑) **하나**에서 나오고, `dashboard.html:12-13` 이 그 값만 렌더한다. 물건별 `as_of` 분포는 반영되지 않는다. **로컬 실측**(시세 보유 + 매각기일 오늘 이후, 화면과 같은 식): 6일 12건 · 9일 3건 · 14일 8건 · 18일 54건 · 31일 38건 → **115건 중 92건(80%)이 18일 이상인데 배너는 "6일"이라고 말한다.** 이 앱의 원칙은 "낡은 시세를 조용히 보여주지 않는다"(`:1684` 독스트링)이다. ⚠ **완화 장치는 이미 있다** — `prov.stale`(21일 이상)이 상세·리포트에서 물건별로 고지하고 `grade` 를 weak 로 내린다(`:3555`·`:3562`). 즉 **물건별 낡음은 고지되고 전역 배너만 낙관적이다.** **운영 기준으로는 지금 배포를 막는 사안이 아니다**(오늘 엔카 정상 → `degraded=False`, 배너 미표시가 정상 동작). **DoD**: 배너 문구를 물건별 분포 기반으로(예 "분석 완료 물건 중 N건이 21일 이상 갱신되지 않았습니다"), 회귀 테스트로 고정 |
| PANEL-20 | ★ **리포트 §01 한 줄에 '상한'이 두 개 붙어 금전 숫자가 오독된다** | frontend | todo | `app-qa-auditor` 가 운영 리포트 원문에서 발견(2026-09-23). 마크업 원문: `880 ~ 1040 만원 · 중심값 9,400,000원 · 재판매 상한가 5,014,400원 — 예상 경쟁가가 상한선을 500,000원 넘습니다.` **500,000 은 손익분기 8,900,000 대비 차이**(9,400,000−8,900,000)인데, 바로 앞 명사는 `재판매 상한가 5,014,400원` 이고 그 차이는 **4,385,600원**이다. 같은 줄·같은 빨강·분리 요소 없음 → 사용자가 "재판매 상한가를 50만원 넘는구나"로 읽으면 **438만원을 오판한다.** 이 제품에서 가장 비싼 종류의 오독이다(숫자 표기 = 금전 손해). **DoD**: 빨강 문구에 기준 명사를 박거나("**손익분기(890만원)를** 500,000원 넘습니다") 재판매 상한가와 줄을 분리한다. 고친 뒤 `app-design-expert`·`design-critic` **교차검수**(CLAUDE.md — 숫자 표기 변경). ⚠ **Steward 미검증** — 담당이 인용한 마크업이고 내가 리포트 페이지를 직접 받아 보지는 않았다. 착수 전 재현부터 할 것. **★ 2026-09-23 Steward 재현 완료 — 미검증 해제.** 운영 리포트를 직접 받아 확인했다(`GET /vehicle/2026%ED%83%80%EA%B2%BD70210_1/report` → HTTP 200 · 125,259B. 한글 경로는 percent-encoding 필수 — 빠뜨리면 `app.py:920-921` 이 물건 조회 실패로 보고 303 을 준다). 태그를 걷어낸 본문 원문: `재판매 상한가 5,014,400원 — 예상 경쟁가가 상한선을 500,000원 넘습니다.` **같은 페이지의 다른 자리가 스스로 답을 말한다** — `예상 경쟁가 940만원은 실사용 손익분기 890만원을 넘습니다`. 즉 **500,000 = 9,400,000 − 8,900,000(손익분기)** 가 확정이고, 바로 앞 명사인 재판매 상한가와의 차이는 **4,385,600원**이다. **★ 범위가 최초 접수보다 넓다** — 한 페이지에 `상한선` 이 **3회** 나오고 가리키는 대상이 최소 셋이다: ㉠ `재판매 상한가 5,014,400원` ㉡ `실사용 손익분기 890만원` ㉢ `입찰 상한선`(바로 다음 문장 `현 최저매각가 8,400,000원 — 예상 경쟁가가 입찰 상한선을 넘어 이번 회차는 권하지 않습니다`). 세 '상한'이 인접 문장에 색 구분 없이 섞여 있다. **DoD 보강**: 한 군데를 고치는 것이 아니라 **`상한선` 이 나오는 모든 자리에 기준 명사를 박는다**(예 "손익분기(890만원)를 500,000원 넘습니다"). 고친 뒤 같은 물건으로 재수신해 세 문장을 나란히 읽어 확인하고, `app-design-expert`·`design-critic` 교차검수 |
| PANEL-21 | **상세 판정 칩에서 색과 글자가 서로 다른 말을 한다** — 색 불일치를 고치다 의미 불일치가 생겼다 | frontend | todo | **★ PANEL-10 ④ 수정이 만든 새 결함이고, 담당이 스스로 신고했다**(2026-09-23). `detail.html:46` 이 레거시 `\|bcls`(judgment 문자열로 색 선택)에서 톤 기반 `{{ (bidst.tone if bidst else '')\|tcls }}` 로 옮겨지면서 **색은 `bid_state` 의 톤, 글자는 `v.judgment`** 가 됐다. 둘의 출처가 다르다. **담당 실측**(`2025타경13405_1`): 칩이 **rose 인데 글자는 "유찰 대기"** 이고, 같은 화면 히어로는 `이번 회차 입찰 부적합` 이라 말한다. 즉 한 칩 안에서 색(중단)과 글자(대기)가 반대를 가리킨다. 목록↔상세의 색 불일치는 없앴지만 **한 칩 안의 불일치로 옮겨 간 것**이다(PANEL-01 때 "화면 안 불일치를 고치며 화면 사이에 새로 만들었다"와 같은 패턴). **DoD**: 라벨도 `bidst.label` 로 옮겨 **색과 글자를 한 원천에서** 낸다. `bidst` 가 없는 경로(즐겨찾기)는 PANEL-22 와 함께 본다. 판정 **표시** 변경이므로 `app-design-expert`·`design-critic` **교차검수** 필수(CLAUDE.md). ⚠ 판정 **기준**은 손대지 않는다. **★ 2026-09-23 판정 확정 — 되돌리지 않고 라벨을 이관한다.** 교차검수 두 자리가 **서로 모른 채 같은 결론**을 냈고 둘 다 **배포 차단** 등급을 매겼다(`reports/xcheck-batch-2026-09-23.md`). `app-design-expert` 가 **라이브에서 실물 재현**했다(`2026타경30644_1`): 목록 카드 칩 `이번 회차 입찰 부적합` / 상세 상단 칩 `유찰 대기` / 같은 화면 히어로 `이번 회차는 입찰하지 마세요`. **근거가 저장소 안에 이미 있다** — `detail.html:39` 주석이 *"판정은 bid_state 가 단일 진실원천"* 이라 선언했고 **바로 윗줄 `:44`(nomarket)는 이미 `bidst.label` 을 쓴다**(`:45` 만 예외인 자기모순). 그리고 `app.py:659-662` 가 목록이 같은 길을 간 이유를 적어 뒀다 — *"상세에서 '이번 회차 입찰 부적합'인 차가 목록에서는 앰버 '유찰 대기'로 보였다(4회차 디자인·경매 P0)"* — **지금은 그 문장의 좌우가 뒤바뀐 상태다.** 되돌리면 그 결함이 되살아난다. ⚠ **그냥 바꾸면 깨진다**: `bidst.label` 은 `지난 기일 — 다음 기일 공고 대기` 처럼 길어 320px h1 옆 `flex-wrap` 칩에서 줄이 갈린다 → `:318` 이 이미 쓰는 **`ui.judge_label()`** 을 함께 적용한다. 테스트는 막지 않는다(확인: 상세 칩 텍스트를 무는 단언 0건). **닫기 조건**: 렌더로 `유찰 대기` 글자에 로즈가 붙는 조합 **0건** 확인. 지시서 2026-09-23-27 로 배치 |
| PANEL-24 | **판정 배너 아이콘이 톤 체계를 거꾸로 쓴다** — `wait`→앰버, `caution`→sky | frontend | todo | `design-critic` 지적, **Steward 원문 재현**(2026-09-23). `detail.html:307` 원문: `'text-amber-300' if _vt=='wait' else 'text-sky-300' if _vt=='caution'`. 그런데 `_TONE_CLS`(`app.py:321-326`)는 **wait→slate, caution→amber** 다 — **정확히 반대**다. 결과: 판정 배너 아이콘과 **바로 아래 히어로 칩**(`detail.html:318`, `\|tcls`)이 **같은 물건에 반대 색**을 쓴다. "색은 bid_state 한 곳에서만 정한다"(`service.py:3798` 독스트링)가 아직 사실이 아닌 증거다. **DoD**: 톤 체계에 맞춘다(wait=회색 계열, caution=앰버). 어두운 히어로 배경이므로 명도를 고르고 **대비값을 계산해 남긴다**. ⚠ 판정 **기준**은 손대지 않는다 — 색만 |
| PANEL-25 | **목록 행 좌측 악센트가 아직 judgment 문자열 기반이다 — 레거시 네 번째 자리** | frontend | todo | `design-critic` 지적, **Steward 원문 재현**. `vehicles.html:256-260`: `border-emerald-500 if v.judgment=='입찰 검토 가능' / border-amber-500 if '유찰 대기' / border-sky-500 if '신뢰도' in / border-rose-500 if '입찰 보류'`. 지난 배치가 **같은 표의 칩**을 톤 기반으로 옮겼으므로 지금 한 행에서 **칩은 slate, 악센트는 앰버**가 된다. 지시서 23의 "레거시 `\|bcls` 3곳" 목록에 **없던 네 번째 자리**다 — 레거시 색 경로를 셀 때 `\|bcls` 만 세고 **인라인 문자열 분기를 안 셌다.** **DoD**: 톤 기반 이관 또는 제거. 고르는 근거를 남긴다. 그리고 **judgment 문자열로 색을 고르는 자리가 더 없는지 전수로 센다**(`\|bcls` 가 아니라 `v.judgment==` 패턴으로) |
| PANEL-26 | **리포트 검증 카드 제목이 `모델 검증 실적` — 개명이 두 줄 위를 놓쳤다** | frontend | todo | `design-critic` 지적, **Steward 재현**. `report.html:1075` 부제는 `전체 평균 · 이 물건 값 아님` 으로 고쳤는데 **`:1073` 제목은 `모델 검증 실적`** 이다. `모델` 을 쓰지 않기로 한 이유가 바로 그 편집의 근거였다(차 앱에서 `모델` 의 1차 의미는 **차종**). `report.html` 에 `모델` 3회(`:1073`·`:1083`·`:1401`), 홈의 같은 블록은 `dashboard.html:425` **`실측 검증`**. **★ 그리고 같은 숫자(`backtest.mae_pct`)를 부르는 라벨이 아직 3종이다** — `전체 평균 오차`(8곳) · `예측 오차`(`report.html:1075`) · `예상낙찰가 오차`(`dashboard.html:436`). **DoD**: 제목 통일 + 라벨 3종을 1종으로. `:1083`·`:1401` 의 `모델` 은 문맥을 읽고 판단한다(예측모델을 가리키는 산문이면 유지 가능, 차종 오독 자리면 교체) |
| PANEL-27 | **모바일 고지가 스스로 단 주석의 "한 줄" 약속을 어긴다** | frontend | todo | `app-design-expert` 지적, **Steward 재현 — 보고보다 나쁘다.** `vehicles.html:372` 주석 원문: *"범례(110px)가 첫 카드를 폴드 밖으로 밀어낸 전례가 있다 — **한 줄로 유지한다**"*. 그런데 `:373` 소스는 **110자**(표시 약 78자)다. **주석을 단 그 편집이 그 약속을 어겼다.** 계산: 390px 안폭 ≈325px · 11px 한글 → 라인당 ~29자 → **3줄 ≈65px**. 큰글씨(`app.css:249` `html.nc-large{font-size:125%}`, `:253` 이 `text-[11px]`→`.8125rem`)에서 **4~5줄 ≈96~120px** — 주석이 기록한 **바로 그 110px 급**이다(계산값, 렌더 미확인). **★ 강조도 거꾸로다**: 굵은 것은 안심시키는 `±9.3%` 이고 한계("개별 물건의 정확도는 아닙니다")가 회색이다 — **고지에서 굵을 것은 한계다.** **DoD**: 문장 축약 또는 뒤를 `ui.gloss` 로 접기 + 굵기 반전 + **320·390 × 큰글씨 ON/OFF 4장**으로 첫 카드 예상낙찰가가 폴드 안에 남는지 확인 |
| PANEL-28 | **즐겨찾기가 이미 아는 처방을 적용받지 못한 자리 셋** | frontend/backend | todo | `app-design-expert` 지적(2026-09-23). 즐겨찾기는 사용자가 **직접 담아 둔** 화면이라 더 오래·더 믿고 본다(`app.py:1248` 이 스스로 적은 말이다). ⑴ **맨 `—`** — 예상낙찰가가 `—` 로 뜬다. 목록은 이미 `미산출` 로 바꿨고 이유가 `vehicles.html:460-465` 에 있다(*"맨 '—' 는 '로딩 실패·버그'로 읽힌다"*). 같은 결함·이미 아는 처방인데 이번에 이 파일을 열고도 적용하지 않았다. ⑵ **정확도 프레임 0** — `app.py:1268` 이 `mae` 를 넘기는데 `watchlist.html` 에 `오차`·`mae`·`검증` 문자열이 **0건**이다. 목록에는 고지가 생겼는데 **예상낙찰가를 나란히 놓고 비교하는 화면**에는 없다. ⑶ **QA 스모크 커버리지 0** — `/watchlist` 가 스모크 8개 페이지에 없어 캡처가 안 찍힌다. **DoD**: ⑴⑵ 는 템플릿, ⑶ 은 스모크 목록에 추가. ⑴ 은 PANEL-22(즐겨찾기 라우트에 `bid_state` 없음)와 별개로 지금 고칠 수 있다 |
| PANEL-29 | ★★ **소프트캡 외부화가 절반만 됐다 — 백테스트가 배율을 하드코딩한다** (PANEL-08 의 **선결 조건**) | backend | todo | `insight` 가 발견, **Steward 원문 대조 확인**(2026-09-23). `service.py:3263` 원문: `pred = min(pred, r["median_price"] * 1.10)` 이고 바로 옆 주석이 **`# expected_for와 동일 소프트캡`** 이다 — **"동일"이라고 적어 두고 `load_config()` 를 읽지 않는다.** 리터럴 `1.10` 은 코드 전체에서 `:3263` 과 `:3585`(기본값) **두 곳뿐**임을 전수로 확인했다. **왜 중요한가**: 이 LOO 결과가 `pred_pool` → `accuracy_strata` → `accuracy_for`(화면의 "이 유형 실측 오차 ±X%" 배지)와 적중률 전체를 만든다. 즉 `config.yaml` 의 `soft_cap_ratio` 를 바꾸면 **예상낙찰가는 새 배율로, 정확도 검증은 옛 1.10 기준으로** 계산된다. **PANEL-03 이 '외부화 완료'로 닫힌 것이 절반만 사실이었다** — 운영 규칙 7 이 말하는 "자리는 옮기고 값은 안 옮긴" 것과 같은 계열이고, 이번엔 **자리조차 한 곳이 빠졌다.** **DoD**: `:3263` 이 `soft_cap()`(또는 같은 config 값)을 쓰게 하고, **배율을 바꿨을 때 예상가와 정확도 배지가 같은 배율을 쓰는지 회귀 테스트로 고정**한다. ⚠ 값을 바꾸는 것은 PANEL-08(오너 승인)이고, **이 티켓은 값이 아니라 배선만 고친다** — 지금 값 1.10 을 그대로 두고 고쳐도 결과가 변하지 않아야 한다(그게 이 수정이 옳다는 증거다). **★ 2026-09-23 완료(`cf5b6d1`)** — `soft_cap_ratio()` 를 신설해 배율 읽기를 한 곳으로 모으고 `soft_cap()`(`:3627`)과 백테스트 LOO(`:3271`)가 **둘 다 그 함수만** 부르게 했다. 리터럴 `1.10` 은 이제 기본값 `:3593` 한 곳뿐이다. **값은 바꾸지 않았다**(`config.yaml` 1.10 그대로). **닫기 증거**: `mae_pct` 9.2→9.2 · `within10_pct` 61→61 · `pred_n` 135→135 · `within20` 95→95 · 층 9종 n/mae/within10 **전부 동일**. **반올림은 뺐다** — `soft_cap()` 이 10만원 반올림하는 이유는 화면 금액이 밴드와 1원까지 맞아야 해서인데(그 독스트링), LOO 의 `pred` 는 인쇄되는 금액이 아니라 오차 지표의 입력이고 base(min×prem)도 반올림하지 않는다. 캡만 반올림하면 한 식 안에서 규칙이 갈리고, 실측으로도 캡 binding 6건의 MAE 가 9.199882→9.197875 로 움직인다(표기 ±9.2%엔 안 보이지만 "자리만 옮긴다"는 약속과 어긋난다). **★ Steward 가 새 테스트의 실효성을 직접 확인했다** — `cfg({"soft_cap_ratio": ratio})` 로 배율을 1.10·1.50 으로 바꾼 뒤 `preds == {round(MED*ratio)}` 를 단언하므로 리터럴이 남아 있으면 **실패한다**. 같은 테스트에서 `soft_cap()` 도 함께 재서 "한쪽만 움직이는" 그 버그를 잡고, `_bt_cache` 를 명시적으로 비워 캐시로 인한 공허 통과까지 막았다 |
| PANEL-30 | **`accuracy_for` 가 가격대 층을 후보에서 빼 죽은 계산을 만든다** — 판정: **넣는다** | backend | **판정 완료 · 구현 대기** | 근거 산출 완료(`reports/panel08-softcap-accuracy-strata.md`). **★ 이 건은 오너 결정 목록에서 뺀다.** CLAUDE.md 의 승인 필수는 판정 기준 **완화**인데 이 변경은 오차를 **크게** 만드는 쪽(조이는 방향)이고 되돌리기 쉽다 — Steward 자율로 실행하고 오너께 통보한다. **실측**: `accuracy_strata`(`:4086-4094`)가 `price_band` 를 계산해 두는데 `accuracy_for` 의 후보는 `cands = 제조사 · 유찰횟수` **둘뿐**이다(원문 확인) — **죽은 계산**이다. **㉠ 회복 건수 0건** — 현재 `accuracy_for` 가 `None` 을 돌려주는 물건이 **한 건도 없다.** 제조사(국산100/수입35)와 유찰횟수(59/59/17) 층이 **전부 이미 n≥8** 이라 후보가 항상 찬다. 즉 *"표본 부족으로 미산출"* 이라는 원래 전제가 현재 데이터(LOO n=135)에서는 **더 이상 성립하지 않는다.** 구간을 3가지(기존 / 데이터 4분위 / 중앙값 2분할)로 바꿔도 세 방식 모두 0건 — 민감도 통과. **㉡ 넣으면 표시 오차가 커지는 건수**(median_price 보유 734건 기준): 기존 구간 396건(27.9%) 평균 **+0.77%p** 최대 +1.0%p. **㉢ 판정 근거**: '더 나쁜 쪽' 규칙 때문에 **더 낙관적으로 바뀌는 경우가 전혀 없다** — 독스트링이 금지한 **과대주장**과 방향이 같다. 영향도 작다("±9%"가 "±10%"로 반올림 경계를 넘는 정도). **⚠ 구현 시 필수 가드**: `price_band`(`:4087`)가 `med = p.get("median_price") or p.get("actual") or 0` 이라 **가격을 모르는 물건을 `500만 이하` 에 강제로 넣는다.** 지금은 그 층이 n=2(<8)라 자동 제외돼 무해하지만 표본이 8을 넘으면 **가격 미상 물건이 엉뚱한 오차율을 배정받는다.** → **"가격이 없으면 가격대 층 자체를 후보에서 제외"** 가드를 같은 커밋에 넣는다. **DoD**: 후보에 가격대 층 추가 + 위 가드 + 회귀 테스트(가격 미상 물건이 가격대 층을 배정받지 않음, '더 나쁜 쪽' 규칙 유지). **★ 2026-09-23 완료(`cf5b6d1`)** — 후보에 가격대 층을 넣고 **가드를 두 겹**으로 걸었다: ⑴ `accuracy_for` 가 가격을 모르면 후보를 만들지 않고 ⑵ `accuracy_strata` 도 층을 만드는 쪽에서 라벨 `None` 표본을 건너뛴다. **후보에서만 빼면 `500만 이하` 층 값 자체가 이미 오염**되기 때문이다 — `or 0` 함정을 원천에서 제거했다. **실측**(운영 1,418대 전수, `effective_median` 기준, 같은 프로세스 내 전후 대조): 표시 오차 증가 **400건**(평균 +0.77%p, 최대 +1.0%p) · 변화 없음 1,018건 · **감소(더 낙관적) 0건** · `accuracy_for` 가 `None` 인 물건 **0건**. `insight` 리포트의 396건과 4건 차이는 **모집단이 다르다**(리포트는 `median_price` 보유 734건, 구현은 블렌드 포함 전수 1,418대) — 방향·평균·최대는 일치. 신규 테스트 9건(`tests/test_accuracy_price_band.py`), 깨진 테스트 0건. **★ 2026-09-23 교차검수 ②/②(`design-critic`) — 계산은 유지, 표기 3건 선결(PANEL-40).** *"되돌릴 게 아니라, 이 커밋이 화면의 다른 문장 2곳을 거짓으로 만들었고 새 라벨의 경계 어휘가 틀렸으며 같은 개념의 가격대 어휘가 앱 안에 두 벌이 됐다."* **라벨 원천 자체는 합격** — `expected.acc.label` 을 렌더하는 6자리(`detail:419` · `report:892/900` · `report:1369/1386` · `accuracy:154`) 어디에도 문자열 가공이 없고 `_price_band` 하나가 `accuracy_strata` 의 버킷 키와 `accuracy_for` 의 조회 키를 **동시에** 만든다. 길이도 기존 최장(`유찰 3회 이상`)보다 오히려 **약 3px 짧아** 레이아웃 리스크를 새로 만들지 않았다(계산값 — 담당 세션에 셸이 없어 실측 0건·캡처 0장, **배포 전 실측 필요**) |
| PANEL-40 | ★ **가격대 라벨 표기 3건 — 라벨이 경계에서 사실과 다르고, 내가 넣은 고지를 내가 깼다** | backend/frontend | todo | `design-critic` 지적, **Steward 원문 전수 재현**(2026-09-23). ⑴ **★ 라벨이 사실과 다르다 — 그리고 테스트가 그 오기를 고정했다.** `_price_band`(`service.py:4093-4108`)가 `if med < 20_000_000` 다음 `return "2,000만 초과"` 라, **시세가 정확히 20,000,000원인 물건에 `2,000만 초과`** 라고 쓴다(초과가 아니라 같은 값이다). `500만 이하`(`< 5_000_000`)와 `1,000~2,000만`(상한 배타)은 정상이다. **더 나쁜 것은 `tests/test_accuracy_price_band.py:129` 가 `(20_000_000, "2,000만 초과")` 로 그 오기를 단언으로 굳혔다는 점**이다 — 다음 사람이 고치려 하면 **테스트가 "그게 맞다"고 막는다.** 문이 잘못된 것을 지키는 상태다. → `2,000만 이상` 으로 고치고 **단언도 같은 커밋에서** 바꾼다. ⑵ **같은 개념의 가격대 어휘가 앱 안에 두 벌이다.** 새 라벨은 `500~1,000만`·`1,000~2,000만`·`2,000만 초과`(쉼표 O)인데, 공개 `/calendar` 의 '지난달 낙찰 실적'(`service.py:4258-4259` → `calendar.html:79`)은 `500~1000만`·`1000~2000만`·`2000만 이상`(쉼표 X)이다. **두 축이 각각 반대로 맞다** — 쉼표는 `_man` 필터(`app.py:305-318` `f"{n:,}만"`)가 앱 규칙이라 **새 쪽이 맞고**, 경계 낱말은 **달력 쪽(`이상`)이 맞다.** 둘을 한 어휘로 모은다. ⑶ **★ 내가 직전 배치에서 넣은 고지가 이제 거짓이다.** `vehicles.html:248`(표 헤더 title)·`:381`(모바일 1회성 고지)이 *"물건별 오차는 상세·리포트에서 **유형(제조사·유찰횟수)별로** 표시합니다"* 라고 층을 **열거**한다. 층이 셋이 됐으니 목록에서 "두 가지"로 읽은 사용자가 상세에서 `이 유형(2,000만 초과, 52건)` 을 본다 — **화면이 스스로의 고지를 반증한다.** 하필 `:381` 은 폰 사용자의 **유일한 고지**다(PANEL-28 로 막 만든 자리). **DoD**: 층을 열거하지 말고 **"유형별"로 줄인다** — 세 번째 낱말을 추가하면 다음 층 추가 때 또 깨진다. **열거를 없애는 것이 근본 수정이다.** ⑷ 곁들임: `service.py:4168` 독스트링이 "734건 중 396건"인데 구현 실측은 "전수 1,418대 중 400건"이다(**모집단이 다르다** — 둘 다 맞다). 코드에 박힌 쪽이 앞으로 읽히므로 **독스트링에 모집단을 명시**한다. `dashboard.html:464-465` 주석의 줄 포인터 3개(`detail.html:408` → 실제 `419`, `report.html:1363·1380` → 실제 `1369·1386`)도 낡았다. ⑸ 판단 필요: 히어로(`detail.html:351`/`357`)만 `이 유형 ±9.6%` 로 **라벨·표본수를 감춘다.** 제조사·유찰 시절엔 무해했으나 이제 그 '유형'이 가격대일 수 있고, 정체는 70px 아래 `:419` 에서야 밝혀진다. 인라인 6자리에 그룹명을 붙일지(`이 유형(가격대 1,000~2,000만, 59건)`)와 함께 정한다 — ⚠ `detail.html:419` 는 **바로 옆에 실제 금액**(`최저매각가 1,234만`)이 와서 라벨의 만원 숫자가 계산값으로 오독된다. **★★ 2026-09-23 교차검수 ①/②(`app-design-expert`) — 아무도 재지 않은 것을 찾았다: 표본수 `N` 이 함께 떨어진다.** 지시서도 구현 보고도 **오차만** 쟀다(+0.77%p). 그런데 `N` 은 라벨 안에 같이 인쇄되는 **신뢰 신호**다(`이 유형({{ label }}, {{ n }}건)`). **Steward 전수 측정**(운영 DB, 읽기 전용, 프로브 불변): **감소 314건 · 불변 1,104건 · 증가 0건**, 감소폭 **중앙값 −41 · 최대 −48**, 그 물건들의 오차 증가 평균 **+0.93%p**. 로컬 층 표본은 국산 100 / 수입 35 / 유찰 0~1회 59 / 2회 59 / 3회+ 17 / 가격대 59·52·22·2 이므로 국산→`1,000~2,000만` 이면 **−41%**다. 담당이 라이브에서 확인한 `국산 208건 ±8.8%` → `2,000만 초과 98건 ±9.2%`(**−53%**)와 **같은 구조**다(라이브는 표본이 커 절대값만 다르다). → **314대에서 오차는 오르고 근거 표본은 떨어진다. 사용자 눈에는 신호가 둘 다 나빠진다.** **★ 두 검수자가 서로 모른 채 같은 결론에 닿았다**: *라벨이 자기 축을 못 밝힌다.* `수입`·`유찰 3회 이상` 은 축이 자명하지만 `2,000만 초과` 는 **무엇의 2,000만인지 문자열 안에 없다**(실제 기준은 `service.py:4175` 의 `effective_median`, 즉 엔카+케이카 블렌드 시세중앙값인데 화면 어디에도 없다). 그리고 **선택 규칙이 화면에 단 한 번도 없다** — `service.py:4173` 주석 *"더 불리한(오차가 큰) 층을 택한다 — 낙관적인 쪽을 고르지 않는다"* 가 **코드에만** 있다(템플릿 전수 grep 0건). 이 한 줄만 화면에 있으면 "오차↑·표본↓"이 **"일부러 불리한 쪽을 보여준다"** 로 뒤집혀 읽힌다. **⚠ Steward 지시서의 자리 지목이 틀렸다(정정)**: `detail.html:351` 에는 라벨이 **안 들어간다**(`이 유형 ±{{ expected.mae }}%` — 숫자만). 라벨 자리는 **`:419`** 다. `report.html:1386` 은 `{% else %}`(`:1383`) **폴백**이고 주경로는 **`:1369`** 다. 최장 라벨도 내가 고른 `2,000만 초과`(10자)가 아니라 **`1,000~2,000만`(12자)** 이고 그 층이 더 자주 이긴다. **⚠⚠ CSS 오버플로 위험(실측 필요)**: `report.html:140` 의 `word-break: keep-all` 은 상속되는데 `overflow-wrap: break-word` 는 `:148` 에서 **`table, thead, tbody, tr, th, td` 에만** 걸린다 — `.logic .ld small` 에는 없다. `.logic{grid-template-columns:24px 1fr auto}`(`:560`)의 `1fr` 은 min-content 아래로 못 줄어들므로, 최장 비분할 토큰이 `실측 오차 ±8.8%`(10자)에서 `유형(1,000~2,000만,`(17자)로 커지면 **그리드가 컨테이너를 넘을 수 있다.** `base.html:271` 이 기록한 `nc-kv` 사고(트랙 298px vs 카드 안쪽 218px)와 **같은 형태**다. ★ 여기에 축 접두(`시세 …`)를 붙이면 **라벨이 더 길어져 위험이 커진다** — 접두와 길이는 **함께 판단**해야 한다. **배포 전 실측 필수**: 320·360·390 × 큰글씨 ON/OFF, 대상은 **가격대 층이 실제로 이기는 물건**(국산·유찰 0~1회·시세 1,000~2,000만)이어야 한다 — 아무 물건이나 찍으면 라벨이 `국산` 으로 떨어져 **공허 통과**한다. **★ 2026-09-23 완료(`bbcb8c1`)** — ⑴ 경계 라벨 `2,000만 초과`→**`2,000만 이상`**(경계 **값**은 그대로, 낱말만). **오기를 굳혔던 단언을 뒤집고 재발 방지 단언을 새로 넣었다**: `assert "초과" not in (service._price_band(20_000_000) or "")`. **Steward 재현**: 19,999,999→`1,000~2,000만` / 20,000,000→`2,000만 이상` / 20,000,001→`2,000만 이상` / 가격 미상→`None`. ⑵ 고지에서 **열거 자체를 삭제**(`유형(제조사·유찰횟수)별`→`유형별`) — 세 번째 낱말을 더하지 않았으므로 다음 층 추가 때 또 깨지지 않는다. ⑶ 달력 어휘 통일(쉼표는 새 쪽, 경계 낱말은 달력 쪽). 무는 테스트 없음을 담당이 **먼저 확인**했다(`test_sale_stats.py` 는 라벨이 아니라 건수만 문다). ⑷ 축 접두를 `_acc_label()` **한 곳**에서만 만든다(`group == "가격대"` 일 때만 `시세 `), 선택 규칙은 `_macros.acc_why()` 한 곳. ⑸ **경고받은 CSS 위험을 구조로 닫았다** — `.logic` 의 `1fr`→`minmax(0,1fr)`, `.ld` 에 `min-width:0; overflow-wrap:anywhere`. 실측 320·360·390·430 × 큰글씨 ON/OFF(공개) + 768·1440(관리자) **전 조합 넘침 0건·가로 스크롤 0건**. ★ **공허 통과가 아님을 Steward 가 확인**: 측정 대상 `2025타경741_1` 의 시세 13,100,000 이 실제로 `1,000~2,000만` 층에 배정된다. ⑹ **★ 담당이 자기가 만든 회귀를 스스로 잡았다** — 달력 라벨이 2자 길어지자 320px 큰글씨에서 **숫자 한가운데서** `1,000~2,0 / 00만` 으로 갈렸다. before 캡처를 열어보니 쉼표 이전에도 `1000~200 / 0만` 이라 **원래 있던 결함**이었다(악화시킨 것이지 만든 것은 아니다). px→em(`w-[7em]`)으로 고치니 이번엔 막대가 6px 로 뭉개져 그 폭에서만 라벨을 윗줄로 올리는 규칙을 더했다 — **측정 → 수정 → 재측정을 스스로 돌았다.** ⑺ **분모가 다른 두 수를 추적했다**: `+0.77%p`(400건 전체 평균)와 `+0.93%p`(N이 떨어진 314건만) **둘 다 맞다.** 독스트링에 분모와 함께 적고 "섞어 쓰지 말 것"을 명시. 게이트 pytest **1124 passed**, 프로브 6회 전부 동일, 캡처 before/after **md5 상이**(SOP 2번 충족) |
| PANEL-41 | **인쇄되는 리포트에는 '가장 불리한 층을 고른다'는 규칙이 없다** | frontend | todo | 2026-09-23 `frontend-engineer` 가 **범위 밖이라 손대지 않고 신고**했다. 선택 규칙 문장을 `_macros.acc_why()` 툴팁으로 넣었는데, **리포트 산식 2자리(`report.html:1376`·`:1393`)에는 붙이지 않았다.** 이유 둘: ㉠ `report.html:40` 이 **인쇄 시 `.pop`·`.q` 를 숨긴다** — 툴팁은 인쇄본에서 사라진다 ㉡ 그 자리가 **폭이 가장 빠듯한 CSS 위험 지점**이다. 같은 문서 위쪽 히어로(`:899`·`:907`)에는 규칙이 있으므로 화면 독자는 보지만, **인쇄해서 법원에 들고 가는 사용자**는 "오차 ±9.6%"만 보고 그게 왜 그 값인지 모른다. **DoD**: 인쇄본에도 규칙을 넣으려면 툴팁이 아니라 **본문 문장**이어야 한다. 길이 위험이 가장 큰 자리라 ㉠ 한 줄을 어디에 둘지 ㉡ 320px 큰글씨에서 `.logic` 을 넘지 않는지 **먼저 측정하고** 결정한다. 넣지 않기로 한다면 **그 판단 근거를 여기 적는다**(부분 해결을 완료로 쓰지 않는다) |
| PANEL-42 | **포인터를 적는 커밋이 같은 파일에 줄을 더하면 적자마자 낡는다** | 절차 | todo | 2026-09-23 `frontend-engineer` 가 겪고 기록했다. `dashboard.html` 주석의 줄 포인터를 갱신하는 작업이었는데, **자기 주석 7줄 때문에** 대상 줄이 `892·900·1369·1386` → `899·907·1376·1393` 으로 밀려 **같은 파일을 두 번 고쳤다.** ★ **그리고 지금도 하나 낡아 있다** — Steward 확인: `dashboard.html:461` 주석이 여전히 *"이 물건 유형**(제조사·유찰횟수)**의 오차"* 라고 적어 층을 **둘로** 말한다(층은 셋이다). 포인터만 고치고 **본문 표현은 못 고쳤다.** **DoD**: ⑴ `dashboard.html:461` 표현 정정 ⑵ **줄 번호 포인터를 주석에 적는 관행 자체를 재검토한다** — 대안은 앵커 문자열·함수명·티켓 번호로 가리키는 것이다. 줄 번호는 **적는 순간부터 썩는다.** ⑶ 정한 방식을 `CLAUDE.md` 나 `ORG.md` 에 한 줄로 남긴다(조직 문서면 오너 승인 필요) |
| PANEL-43 | **잔여 3건 — 어포던스 비대칭 · 달력 임계값 · 미확인 화면** | frontend | todo | 2026-09-23 `frontend-engineer` 가 스스로 신고한 것들(전부 범위 밖이라 미착수). ⑴ **어포던스 비대칭**: 리포트 히어로 한 줄에서 `분위수 밴드` 는 `?` 배지가 있고 `이 유형` 은 없다 — 한 줄에 `?` 두 개가 붙는 잡음을 피하려 `q=false` 로 둔 **의도된 선택**이지만, 규칙 설명이 있는 쪽에 단서가 없는 셈이라 검수자 눈으로 재확인할 가치가 있다. ⑵ **달력 쌓임 임계값 22.5em**: 320·360 큰글씨는 라벨을 윗줄로 올리고 390 큰글씨는 인라인으로 둔다(막대 **76px**). 76px 이 짧다고 보면 임계값만 올리면 된다 — **판단 필요.** ⑶ **`/accuracy` 관리자 표는 문자열로만 확인했고 눈으로 안 봤다**(라벨 4종 정상·`시세` 접두 미유출은 확인). 다음 캡처 회차에 포함한다. ⚠ 담당이 남긴 프로브 교훈도 함께 적는다 — 첫 검증 정규식 `이 유형\(` 이 매크로가 `이 유형</span></span>(` 를 뱉으면서 **0건**을 돌려줬다. **프로브가 자기 주장보다 좁았던 사례**라 태그 제거 후 비교로 바꿨다(Steward 가 오늘 반복한 것과 같은 계열) |
| PANEL-31 | **목록 첫 카드의 예상낙찰가가 폴드 밖이다** — 320px 일반·큰글씨, 390px 큰글씨 | frontend | todo | 2026-09-23 `frontend-engineer` 가 PANEL-27(고지 축약) 작업 중 **폴드 검사 4장을 찍어 2장 실패**로 스스로 신고했다. 첫 카드 예상낙찰가 하단 y좌표 vs 폴드: **320 일반 681 vs 568 ✗ · 320 큰글씨 1092 vs 568 ✗ · 390 일반 681 vs 844 ✓ · 390 큰글씨 971 vs 844 ✗**. ★ **원인은 고지가 아니다** — 고지 축약으로 16~73px 줄었을 뿐이고(320 큰글씨 143→70px), 높이를 먹는 것은 **위쪽 검색·필터 카드 + 차종 칩줄 + 범례**다. 즉 PANEL-27 은 제 몫을 했고 이건 별개 문제다. **왜 중요한가**: 목록은 최다 트래픽 화면이고 예상낙찰가가 이 제품의 핵심 숫자다. 첫 화면에서 그 숫자를 못 보면 스크롤 전에 이탈한다. **DoD**: 320·390 × 큰글씨 ON/OFF 네 조합에서 첫 카드 예상낙찰가가 폴드 안에 들어오게 한다. 후보 — 필터 카드를 접힌 상태로 시작 · 차종 칩줄을 가로 스크롤 한 줄로 · 범례를 `details` 안으로. **어느 것을 줄일지는 측정 먼저**(각 블록의 실제 높이를 네 조합에서 재고 시작) |
| PANEL-32 | **즐겨찾기의 `여유`·`시세중앙값` 칸이 아직 맨 `—` 다** | frontend | todo | 2026-09-23 PANEL-28 ⑴ 작업에서 **예상낙찰가 칸만** `미산출` 로 바꿨다(`watchlist.html:80`·`:107`). 같은 표·같은 카드의 `여유`(`:82`·`:110`)와 `시세중앙값`(`:85`)은 그대로 맨 `—` 다. 근거는 같다 — `vehicles.html:460-465`: *"맨 '—' 는 '로딩 실패·버그'로 읽힌다."* 담당이 지시 범위 밖이라 손대지 않고 **스스로 신고했다**(옳은 행동). **DoD**: 세 칸의 빈값 표기를 한 규칙으로 모은다. ⚠ 다만 `여유`는 '계산할 수 없음'과 '여유가 0'이 다른 뜻이라 **같은 말로 뭉치면 안 된다** — `margin_room` 이 `None`(데이터 없음)일 때와 `0`(여유 없음)일 때를 구분해 문구를 정하고, 그 구분을 회귀 테스트로 고정할 것 |
| PANEL-33 | **히어로에 AA 미달 글자가 2곳 더 있다** (`text-white/45`) | frontend | todo | 2026-09-23 PANEL-27 ⑤ 작업(`text-white/40`→`/55`) 중 담당이 **범위 밖이라 손대지 않고 신고**했다. `detail.html:365`(감정가 대비)·`:373`(신뢰도 기준선)이 `text-white/45` 로, 히어로 그라디언트(`linear-gradient(150deg,#16305e,#0f2247,#0b1730)`)의 **밝은 스톱 기준 3.88~4.24:1** — 본문 크기 AA 4.5:1 미달이다. ★ **여기서 배운 것을 같이 적는다**: 내가 지시서에 `/50` 으로 쓴 것을 담당이 **거부하고 `/55`** 를 썼는데, 근거가 옳았다 — 그라디언트는 **가장 밝은 스톱 기준으로 재야** 하고 `/50` 은 `#16305e` 에서 4.43:1 로 여전히 미달이다. **DoD**: 두 곳을 AA 통과 값으로 올리되, 전부 같은 불투명도가 되면 위계가 사라지므로 **굵기로 위계를 살린다**(`:351` 이 이미 쓴 방법 — 새 색을 만들지 않는다). 대비값을 계산해 남길 것 |
| PANEL-34 | **`divide-y` 특이도가 행별 보더 색을 2행부터 덮는다 — 다른 표 전수 미점검** | frontend | todo | 2026-09-23 `frontend-engineer` 가 PANEL-25(행 악센트) 작업 중 발견했다. `tbody` 의 `divide-y divide-line` 이 만드는 `.divide-line > :not([hidden]) ~ :not([hidden])`(**클래스 2개**)가 행별 `.border-rose-500`(**클래스 1개**)을 특이도로 이겨, **첫 행에만 색이 칠해지고 2행부터 전부 회색**이 된다. 1440px 실측 8행: 1행 `rgb(244,63,94)`, **2~8행 전부 `rgb(227,232,238)`** — 판정이 같은 두 행이 다른 레일을 달고 있었다. 해당 표는 악센트를 **제거**해 해소했지만(경고색 희석 회피 근거 포함), **다른 `divide-y` 표에 같은 패턴이 있는지는 점검하지 않았다.** **DoD**: `divide-` 를 쓰는 표·목록을 전수로 찾아 행별 보더/배경 색이 함께 걸린 곳이 있는지 센다. 있으면 같은 판단(특이도로 이길지, 제거할지)을 적용하고 근거를 남긴다. ⚠ **이 함정은 눈으로만 보면 "첫 행만 색이 있네" 로 보여 의도된 강조로 오독된다** — 계산된 색값을 행마다 찍어야 잡힌다. **★ 2026-09-23 전수 점검 완료 — 함정 0건. 닫는다.** `design-critic` 이 `divide-` 를 쓰는 10곳(`watchlist:64·91` · `vehicles:254·370` · `detail:617·835` · `dashboard:357·383` · `accuracy:147·184`)을 전수로 확인했다: **직계 자식에 색 테두리 클래스를 거는 곳 0곳.** 유사 후보였던 `watchlist.html:110` `border-t border-line` 은 `a > div` 구조라 **손자**여서 `.divide-line > *` 직계 선택자에 걸리지 않는다(안전). 모바일 카드형 3곳(`vehicles:398`·`watchlist:94`·`dashboard:387`)도 직계 자식이 전부 `a.block` 이고 테두리 클래스가 없다. ⚠ **특이도 숫자 정정**: 담당은 "클래스 2개 vs 1개"라 적었으나 정확히는 `.divide-line`(클래스 1) + `:not([hidden])` 둘이 각각 속성 특이도를 더해 **(0,3,0) vs (0,1,0)** 이다 — 결론은 같고 주석에 남길 숫자는 **3:1**. **남기는 것**: `vehicles.html:256-264` 주석이 지금 이 함정의 **유일한 방어**이므로 지우지 않는다. 가능하면 테스트로 박는다(`divide-` 컨테이너의 직계 자식에 `border-*-500` 금지) |
| PANEL-35 | **판정 배너 아이콘이 칩과 다른 원천을 본다 — `lowconf` 에서 색이 어긋난다** (톤 정정이 5/6만 달성) | frontend/backend | todo | `design-critic` 지적, **Steward 원문 재현**(2026-09-23). 칩은 `bidst.tone`, 아이콘은 `verdict.tone` 을 쓴다. 여섯 상태 중 다섯은 일치하지만 **`lowconf` 하나가 어긋난다**: `service.py:3857`·`:3895` 가 `out("lowconf", "… 판정 보류", "wait")` 로 **칩을 회색**으로 내는데 `:3935` 가 `{"tone": "caution"}` 으로 **아이콘을 앰버**로 낸다. 즉 `시세 신뢰도 낮음 — 판정 보류` · `손익분기 산출 불가 — 판정 보류` 물건에서 **앰버 주의 아이콘 + 회색 판정 보류 칩**이 한 카드에 같이 뜬다. 특히 `:3895` 는 med·exp 가 모두 있는 경로라 히어로가 **반드시** 렌더된다. **★ 그리고 주석이 사실보다 강하게 단정돼 있다** — `detail.html:310` 이 *"아이콘 색은 **판정 톤 체계**(app.py `_TONE_CLS`)를 그대로 따른다"* 고 적었는데 `:314` 는 `_TONE_CLS` 가 아니라 **`verdict.tone`(`_vt`)** 을 쓴다. 이번 배치에서 단 주석이 바로 이 불일치를 덮고 있어 다음 사람이 이 줄을 믿고 검증을 건너뛴다. **DoD**: 아이콘 색 원천을 칩과 같은 `bidst.tone` 으로 통일하고 문장의 '주의' 성격은 **글자로** 말한다. 주석을 사실대로 고친다. 여섯 상태 전부 칩·아이콘 톤 일치를 회귀 테스트로 고정. ⚠ 담당 캡처 3종(stop·wait·caution)은 **일치하는 상태만** 담겨 이 불일치를 보여주지 못했다 — 고친 뒤 **lowconf 캡처를 반드시 찍을 것** |
| PANEL-36 | **템플릿에서 없앤 `예측 오차` 가 서버 문자열로 화면에 남아 있다** | backend | todo | `design-critic` 지적, **Steward 원문 재현**. `service.py:3992` `"within_error": " 다만 절감 폭이 이 유형의 예측 오차 범위 안이라 …"` · `:3993` `"no_error_stat": " 다만 이 유형은 예측 오차를 낼 검증 표본이 부족해 …"` — **히어로 배너 문장으로 렌더되는 문자열**이다. 가리키는 값은 유형별이라 모집단은 틀리지 않지만 **이름이 리포트가 버린 `예측 오차`** 다. 반대편 6자리(`detail:419·908`, `report:892·900·1369·1386`, `accuracy:143`)는 전부 `이 유형(라벨, N건) 실측 오차` 로 통일돼 있다. **즉 이번 어휘 통일이 템플릿만 훑고 서버 문자열을 빠뜨렸다** — `report.html:1078` 주석이 *"같은 숫자를 부르는 이름은 앱 전역에서 하나"* 라고 선언한 원칙이 유형별 어휘에는 적용되지 않았다. **DoD**: 두 문자열을 `실측 오차` 로 바꾸고, **어휘 통일 시 `web/service.py` 의 사용자 노출 문자열도 함께 센다**는 절차를 남긴다(`:3222` 는 주석이라 대상 아님). **★ 2026-09-23 완료(`cf5b6d1`)** — `within_error`·`no_error_stat` 두 문자열의 `예측 오차` 를 **`실측 오차`** 로 바꿨다. `:3222` 는 지시대로 주석이라 손대지 않았다. **무는 테스트가 있다 — 그러나 통과한다**: `tests/test_terms.py:61-78` 이 이 문장을 `"오차" in out["text"]` 와 `"오차 범위 안" not in out2["text"]` 로 무는데, 두 조건 다 바뀐 문자열(`실측 오차 범위 안이라…`)에서 그대로 성립한다. 템플릿·다른 테스트에 이 두 문장을 **리터럴로 비교하는 자리는 없다**(전수 grep). **★ 이 티켓이 남긴 절차 교훈**: 어휘를 통일할 때 **템플릿만 훑으면 절반이다.** 사용자에게 보이는 문자열이 `web/service.py` 에도 있고, 이번 어휘 통일이 정확히 그 절반을 빠뜨렸다 — 다음 어휘 작업 지시서에는 **"`web/service.py` 의 사용자 노출 문자열도 함께 센다"** 를 범위에 명시한다 |
| PANEL-37 | **히어로 안에 톤·색·문구가 어긋나는 자리 셋** | frontend | todo | `app-design-expert` 지적 3건, **Steward 원문 재현**(2026-09-23). ⑴ **캡션 색 불일치** — `detail.html:351` 은 `text-white/55 font-medium` 인데 `:357`(상한선 없는 분기)만 `text-amber-300` 이라 **같은 낱말 'AI 예상낙찰가'가 분기에 따라 앰버(이 앱에서 '조건부 경고')와 흰색으로 갈린다.** 대비를 통일한 바로 그 자리에 색 불일치가 남았다. ⑵ **판정 문장 3회 반복** — `:325` 칩과 `:411` 게이지 라벨이 같은 문자열 **전문**을 찍는다(`_gl = bidst.label` 이고 `weak` 일 때만 접두를 뗀다). 한 화면에 `이번 회차 입찰 부적합` 이 두 번, 로즈 요소가 넷이 된다 — 이 저장소가 반복해 막아 온 **경고색 희석**의 문턱이다. `:409` 주석이 *"판정은 바로 위 칩이 이미 말했다"* 고 스스로 적어 둔 규칙을 `weak` 외 톤에도 적용한다. ⑶ **caution 물건의 대표 숫자가 로즈** — `:341` `_capcls` 가 `bidst.state in ('blocked','over_market')` 로 **state 기반**이라 tone=caution 인 `over_market` 에서 **칩은 앰버인데 숫자는 로즈**다(칩은 '조건부', 숫자는 '금지'라고 말한다). `_capcls` 를 **state 가 아니라 tone** 에서 파생하고 `_over`(최저가>상한선) 예외만 로즈로 남긴다. ⚠ **착수 전 확인**: 검수자는 `tests/test_terms.py:121-122`·`test_panel_r2_fixes.py:136` 이 이 식을 문다고 했으나 Steward 의 grep 에는 `tests/test_panel_r3_market_label.py:60` 만 잡혔다. **인용이 엇갈리므로 전수로 다시 센 뒤 착수할 것** |
| PANEL-38 | ★ **캡처 위생 — before/after 가 같은 파일인 채로 근거가 될 뻔했다** | 측정·절차 | todo | 2026-09-23 `app-design-expert` 가 *"두 캡처를 나란히 열었는데 차이를 찾지 못했다"* 고 신고했고 **Steward 가 해시로 확정**했다: `before-390-top.png` 와 `after-390-top.png` 가 md5 **`56456b8342502781b2ada26684a5dd39`** 로 **완전히 동일**하고 크기도 577941 로 같다. CLAUDE.md §2.4 가 기록한 *"md5까지 같은 실패 캡처를 '페이지가 짧다'의 근거로 사용"* 한 사고와 **같은 계열**이다. ⚠ **이번엔 결론이 오염되지 않았다** — 고지 판정은 다른 캡처(`shots2/list-390-notice.png` 등)로 했고 검수자가 먼저 잡았다. 그러나 **막은 것은 절차가 아니라 운이다.** 같은 회차에 `shots2/hero-390-wait.png` 가 파일명과 달리 히어로가 아닌 페이지 최상단을 담고 있던 것도 함께 신고됐다. **DoD**: 캡처를 근거로 쓰는 작업 지시서에 **"before/after 는 해시가 달라야 한다"** 를 검증 항목으로 넣는다. 가능하면 캡처 스크립트가 **같은 해시 2장을 만들면 실패**하도록 만든다. 파일명과 실제 화면이 다른 것도 같은 수단으로 잡는다(찍은 뒤 대상이 프레임 안에 있었는지 확인). **★ 2026-09-23 절차화 완료(`af7a330`)** — CLAUDE.md 검수 SOP 2번(눈으로 확인)에 해시 비교·파일명 대조·프레임 확인 셋을 박았다. 스크립트 차원의 자동 실패는 미착수 |
| PANEL-39 | ★ **테스트 픽스처가 공허 통과 상태다 — 층 배선을 끊어도 안 울린다** | qa | todo | 2026-09-23 `backend-engineer` 가 PANEL-30 구현 중 **내가 묻지 않은 것을 스스로 신고했다.** `tests/test_personal_use.py`·`tests/test_panel_r2_followup.py` 의 `pred_pool` 은 **40행 전부 `median_price=20,000,000`** 이라 가격대 층의 mae 가 제조사·유찰 층과 **값이 같아지고**(10.0), `tests/test_mae_stratum_parity.py` 도 **8행 전부 30,000,000** 이라 12.8 로 같다. 그래서 **가격대 층 배선을 끊어도 테스트가 안 울린다** — PANEL-30 구현에서 "깨진 테스트 0건"이 나온 것을 "기존 테스트가 보호한다"로 읽으면 안 되는 이유다. ★ **이 함정은 이 저장소가 이미 문서화해 뒀다** — `test_mae_stratum_parity.py:18-22` 가 *"층값과 전체평균을 **일부러 다르게** 둔다 — 같으면 이 테스트는 아무것도 증명하지 못한다"* 고 적었는데, **다른 파일들이 그 교훈을 못 받았다.** 신규 파일(`test_accuracy_price_band.py`)은 층값을 일부러 다르게(12.0/4.0/8.0) 만들어 이 함정을 피했다. **DoD**: ⑴ 위 세 파일의 `pred_pool` 픽스처에 **층별로 다른 값**을 넣어 각 층이 실제로 구분되게 한다 ⑵ 고친 뒤 **가격대 층 배선을 일부러 끊어 테스트가 실패하는지** 확인한다(반증 없이 '고쳤다'고 쓰지 않는다) ⑶ 같은 계열의 공허 픽스처가 더 있는지 `pred_pool` 을 쓰는 테스트 전수로 센다 |
| PANEL-22 | **즐겨찾기 라우트가 `bid_state` 를 호출하지 않아 색 통일이 거기서 멈춘다** | backend | todo | 2026-09-23 `frontend-engineer` 가 PANEL-10 ④ 를 작업하다 발견하고 **없는 값으로 색칠하지 않으려 이관을 멈췄다**(옳은 판단). `web/app.py:1234-1268` 즐겨찾기 라우트는 `service.bid_state` 를 호출하지 않아 `bidst` 가 없고, 그래서 `watchlist.html:72`·`:98` 이 아직 레거시 `\|bcls` 를 쓴다. 결과: **`신뢰도 낮음` 이 즐겨찾기=sky / 목록=slate** 로 여전히 다르다(1440 캡처 육안 확인). `_bcls`(`app.py:334-342`)와 필터 등록(`:394`)도 이 두 곳 때문에 **삭제 불가**다. **DoD**: 즐겨찾기 라우트에 `bid_state` 를 태우고 두 곳을 톤 기반으로 옮긴 뒤 `_bcls` 삭제 가능 여부를 다시 센다. ⚠ 판정 경로에 호출을 추가하는 변경이라 **성능·판정 동일성**을 먼저 확인한다 — 목록과 같은 입력에 같은 판정이 나오는지 회귀 테스트로 고정 |
| PANEL-23 | ★ **우리가 재 온 "1440 데스크톱 표"는 공개 사용자가 보는 화면이 아니다** — 측정 타당성 문제 | 측정·우선순위 | todo | 2026-09-23 `frontend-engineer` 가 측정 중 발견, **Steward 가 코드로 재현**. `base.html:6` 이 폰 프레임 스크립트를 `{% if not is_admin(request) %}` 로 감싸고, 그 스크립트는 `standalone 아님 + iframe 아님 + (min-width:900px and pointer:fine and hover:hover) + screen.width>=900` 이면 `/static/frame.html` 로 `location.replace` 한다. `web/static/frame.html:20` 의 iframe 폭은 **`min(420px, 94vw)`**. 즉 **공개 사용자의 데스크톱 브라우저는 420px 카드 화면을 본다.** 데스크톱 표(`vehicles.html:239` `hidden lg:block`)를 실제로 보는 사람은 ㉠ 관리자 ㉡ 설치앱(standalone)을 데스크톱 폭으로 연 사용자뿐이다. TWA 는 폰이라 카드다. **★ 그런데 로컬 개발 서버는 XFF 가 없어 `is_admin` 이 참이 된다** — 따라서 오늘 찍은 1440 캡처와 PANEL-15(1440 표 붕괴)·PANEL-10(표 칩 색) 측정이 **전부 관리자 세션**이었다. PANEL-06 에서 똑같이 당하고 *"내 측정이 관리자 세션이었다"* 라고 이 문서에 적어 둔 함정을 같은 날 다시 밟았다. **DoD**: ⑴ 데스크톱 표 관련 티켓의 우선순위를 **공개 사용자 노출 기준**으로 재평가한다(PANEL-15·PANEL-10 표 칩·PANEL-21 포함) ⑵ 측정 절차에 **"비관리자 세션으로도 잰다"** 를 넣고, 관리자 전용 화면을 잰 경우 수치에 그 조건을 반드시 병기한다 ⑶ 설치앱 데스크톱 사용 비중을 모른다 — 모르면 **모른다고 적고** 우선순위를 그에 맞춘다. ⚠ 이 티켓은 기능 결함이 아니라 **우리가 무엇을 재고 있었는지의 문제**다 |

## 패널 지적 중 **티켓이 없던 것** — 2026-09-23 전수 대조

> 7회차 리포트가 스스로 진단한 하락 원인이다: **"지적이 반복되는데 티켓이 없다.
> 경매 전문가는 같은 항목을 7회 연속 지적했고 백로그에는 한 줄도 없다.
> 6회차 78.5 → 7회차 77.0 하락의 상당 부분이 '같은 결함이 그대로여서' 붙은 감점이다."**
>
> 회차 문서 **10개**(전문가 1~7회차 + 페르소나 1~3회차)에서 지적 **371건**을 뽑아
> 이 백로그와 대조했다. 티켓이 없는 것이 **51건**이고, 그중 **24건이 3회 이상 반복**이다.
> 별도 반증 담당이 "티켓 없음" 판정을 전부 재확인했다(철회 0건).
> 상위 2건은 **PANEL-08·PANEL-09 로 승격**했다(위 표) — 둘 다 오늘 내가 닫은 티켓이 덮을 뻔했다.
>
> ⚠ 이 표는 **접수 목록이지 착수 목록이 아니다.** 출시 전에 다 할 수 없다.
> 다만 **티켓 없이 사라지는 것**만은 막는다 — 그게 점수를 깎아 온 원인이다.
> 페르소나 트랙 지적이 백로그에 **0건**이었던 것도 여기서 드러났다
> (`docs/reviews/README.md:40` 은 "백로그 티켓으로 옮긴다"고 규정한다).

| # | 우선 | 반복 | 제목 | 근거(회차 문서) | 코드 |
|---|---|---|---|---|---|
| 1 | P1 | 7회 | 소프트캡 값 자체 → **PANEL-08 로 승격** | `2026-09-12.md:43-44,102` 외 6회차 | `service.py SOFT_CAP_RATIO` |
| 2 | P1 | 7회 | 측정·캡처 스크립트가 반복해서 **거짓 합격**을 낸다 — 매번 '넘침 0건'이라는 가장 안심되는 형태로 | `-r5.md:344-345,96-103` · `-r4.md:76` · `-r3.md:121` · `-r2.md:256-260` | `capture_weekly_screens.py` · 레이아웃 측정 스크립트 |
| 3 | P1 | 5회 | 리포트가 **큰글씨 모드 미지원**, 큰글씨·320px 잘림·넘침 | `-r5.md:145,129` · `-r4.md:123` · `-r3.md:133` | `report.html` · 육각형 `min-width:641px` |
| 4 | P1 | 4회 | 카드 **숫자 위계가 중요도와 정반대** — 정작 행동 기준인 입찰 상한이 제일 작다 | `persona/-r3.md:77,90-91` · `persona/-r2.md:76,94-96` | `vehicles.html` 카드 마크업 |
| 5 | P1 | 4회 | **체납·과태료가 취득원가에 없다** — 인수부담이 원가 밖인데 상한선은 '여기까지만'으로 확정적 | `-r5.md:149,131` · `-r4.md:125` | 총 취득원가 §06 · 상한선 산식 |
| 6 | P2 | 4회 | 테스트가 분기를 렌더하지 않아 **가드가 공허해진다** | `-r5.md:342-343,334-336` · `-r4.md:86-92` | `test_exposure.py` 픽스처 외 |
| 7 | P2 | 4회 | 320×568에서 첫 카드 노출 **27%** · 320px 금액 줄바꿈·법원명 절단 | `-r5.md:232-233,287` · `-r4.md:122` | 목록 필터 카드 · 페이지네이션 |
| 8 | **P0** | 3회 | **패널 실행 인프라** — GitHub 푸시 403(리포트 2회 유실)·playwright 미설치(스크린샷 2회 0장)·pip 권한 거부 | `2026-09-23.md:89,4-6,75` · `2026-09-22.md:90,51-56` | 클라우드 루틴 `persist_session:false` |
| 9 | ~~P0~~ **해결됨** | 3회 | ~~낡은 최저가 45건으로 예상낙찰가 43~61% 과대~~ → **가드가 이미 있다.** 4회차 리포트 커밋(`ccb6663` 20:20:11) **4분 뒤** `d39b6c0`(20:24:36)으로 `stale_floor()` 투입, 11일째 회귀 없음 | `-r4.md:120,143` · 재현: `service.py:3580,3602` · Steward 실측 2026-09-23 | `expected_for` 가 `stale_floor` 면 `None` 반환 |
| 10 | P1 | 3회 | 낙찰확률 분모 → **PANEL-09 로 승격** | `2026-09-22.md:70-71,93` 외 | `record_sale_result` |
| 11 | P1 | 3회 | **사고·정비 감가가 금액을 무시** — 감정서 수리금액 62건 보유하고도 건수만 사용 | `-r4.md:118` · `-r3.md:129` | `accident_hits` · `condition_costs` |
| 12 | P1 | 3회 | 기일 **시각**이 지난 물건이 현재형('유찰 대기')으로 남는다 | `persona/-r2.md:61,83,105-107` | `bid_state` · DB `sale_time` |
| 13 | P1 | 3회 | **입찰표 오기 경고가 없다** | `-r5.md:148,131` · `-r4.md:125` · `-r3.md:137` | 리포트 §12 |
| 14 | P1 | 3회 | 차량 **보관장소**가 주소뿐 — 확인하러 가라면서 갈 곳을 못 알려준다 | `persona/-r3.md:81,53` · `persona/-r2.md:46,142` | 상세·리포트 보관장소 |
| 15 | P2 | 3회 | **금액 단위**가 만원/원/무단위로 섞임 | `persona/-r3.md:80,95,136` | 전 화면 |
| 16~24 | P2 | 3회 | (중복·병합 `?all=1` 도달 · 터치 타깃 18/30px · 어려운 말 · 상용화물 정보 공백 · 리포트 공유 링크 · 벨 배지 · 숫자 기준 미공개 · 리포트 분량 · 항목별 점수 유실) | `-r5.md` · `persona/-r2.md`·`-r3.md` | 각 항목 참조 |
| 25~40 | P1~P3 | 2회 | (감정서 공유 46건 · 홈↔리포트 톤 역전 · 추천 정렬 · 밴드 폭 불일치 · 초록 남용 · 쿠키 HttpOnly · 목록 진입로 · 물건번호·입찰시각 · 하한 걸린 예상가 · 상세 결론 부재 · 접근성 · 캡처 대표물건 · 신뢰도 오독 · 즐겨찾기 동기화 · 페르소나 지시문 · 평일 법정) | 각 회차 문서 | 각 항목 참조 |
| 41~51 | P1~P3 | 1회 | (**표본 편차가 상세에 없음** · §07 최저가 미만을 '우량' 판정 · `wait` 판정이 손익분기 무시 · `/privacy` 허위 기능 서술 · `/static` Cache-Control 부재 · 2회차 열린 결정 3건 소멸 · 성능점검기록부 부재 미고지 · 새 데이터 필요 · 사례 24건 3일 편중 · 홈 3G 14.4초 · 법원 정식 명칭) | 각 회차 문서 | 각 항목 참조 |

**최초 집계**: P0 2건 · P1 16건 · P2 27건 · P3 6건. 전체 목록 원본은 조사 산출물에 있고,
여기에는 **반복 3회 이상 + P0/P1** 을 펼쳐 적었다. 나머지는 묶어 두되 **지우지 않는다.**

> ### ★ 2026-09-23 정정 — **"51건"은 미처리 부채가 아니다**
>
> Steward 가 오너에게 51건을 **밀린 일**처럼 보고했다. **틀렸다.** 검증해 보니 상당수가
> **이미 고쳐졌고 티켓 기록만 없었다.** 지금까지 코드로 확인된 것만:
>
> | 항목 | 실제 상태 | 근거(직접 재현) |
> |---|---|---|
> | 9번 낡은 최저가 (P0·3회) | **해결** | `service.py:3602` · 누출 0/65건 |
> | 12번 기일 시각 지난 물건 (P1·3회) | **해결** | `service.py:3804` `기일 경과 — 결과 확인 전` |
> | `wait` 판정이 손익분기 무시 | **해결** | `service.py:3840,3845` → `blocked` |
> | §07 최저가 미만을 '우량' 판정 | **해결** | `service.py:4617` `not floor or x >= floor` |
> | 3번 "리포트 큰글씨 **미지원**" (P1·5회) | **표현 과장** | `nc-large` 규칙이 `report.html:23,236,267-287,444-456` 에 있다. 잔여는 육각형·페이지네이션 2건뿐 |
> | `/privacy` 광고 개인화 거부·회원 탈퇴 허위 서술 | **해결** | `(예정)` 표기 완비(`privacy.html:46-48,65,85`) |
>
> **남는 교훈은 두 가지다.** ⑴ **티켓이 없으면 고쳐도 사라진다** — 다섯 건 모두 조치는 됐는데
> 기록이 없어 패널이 계속 재지적했고, 그 재지적이 점수를 깎았다. ⑵ **미조치 목록을 세는
> 쪽도 검증해야 한다** — 반복 횟수가 부풀면 "24건이 3회 이상 반복"이라는 숫자 자체를 못 믿게 된다.
>
> 아직 **검증하지 않은 항목이 다수 남아 있다.** 이 표의 나머지를 근거로 "밀린 일 N건"이라고
> 말하지 않는다. 확인한 것만 말한다.

## 완료 (요약 — 상세는 changelog)
- PHASE 0: 7개 서브에이전트 생성(.claude/agents/)
- PHASE 1: 레퍼런스 학습(docs/design-references.md)
- **IMP-R1-01 물건목록 모바일 카드화** ✅ (390px 가로스크롤 제거)
- **IMP-R1-02 가격 신뢰구간 게이지(상세)** ✅ (최저·예상·시세 마커 + 기회/대기 밴드)
- **IMP-R1-04 예상낙찰가 위계 강화** ✅ (상세 2xl 볼드 + 게이지)
- 자가개선 Round 1 재채점: 64 → **81** (+17), pytest 90 통과

## 제품 오너 감사 — 유료 서비스 gap (자가 발굴, 우선순위순)

> 사용자가 일일이 지시하지 않아도 상용화에 필요한 것을 스스로 발굴·기록한다.

| ID | 제목 | 분류 | 상태 | 근거(왜 유료에 필요) |
|---|---|---|---|---|
| PS-01 | 물건별 분석 리포트(PDF/인쇄) V2 | 산출물 | ✅ 완료 | **11섹션 종합 리포트**(결론·신뢰도·개요·감정·시세·취득원가·수익시뮬·민감도·리스크·산출로직·법적). 실산정로직 기반, 미확정 태그·재판매 부적합 경고. 저장·공유·인쇄 = 유료 핵심 산출물 |
| PS-02 | 케이카 수동버튼 제거·상태 정직화 | UX정리 | ✅ 완료 | 자동화된 내부 과정을 사용자에게 노출 안 함 |
| PS-03 | 상세 상단 한줄 판정(plain language) | 전환 | todo | 비전문가가 30초 내 "얼마·지금 가능?" 이해 |
| PS-04 | 랜딩/가치제안 페이지(백테스트 정확도 셀링) | 전환 | ✅ 완료 | `/landing` — 히어로·증명스트립(691/69/79%/±29%)·3대가치·정직배너·작동방식·건당리포트 티저(결제보류)·최종CTA. 실데이터·정직 프레이밍. 사이드바 '서비스 소개' 링크 |
| PS-05 | 매각기일 D-3 알림(이메일/앱내) | 유료 훅 | todo | 재방문·구독 유지의 핵심 훅 |
| PS-06 | 관심목록 후보 비교·정렬(예상낙찰가·여유순) | 산출물 | todo | 여러 물건 중 선택을 돕는 유료 가치 |
| PS-07 | 온보딩·빈 상태·오류 페이지 | 완성도 | todo | 첫 사용 이탈 방지 |
| PS-08 | 배포 준비(운영DB·환경변수·로깅·에러핸들링) | 운영 | todo | 실서비스 안정성 |
| PS-11 | 낙찰 데이터 자체 축적 파이프라인(자동) | 데이터/정확도 | ✅ 완료 | 영구 `sale_results` 히스토리 + 자동기록/백필 + 자동승급 할인모델(모델별≥8→유찰→전역). 엔카 의존 탈피(compliance §2a)의 토대 |
| PS-12 | 인기 신호(엔카 매물수·케이카 조회수) 할인율 반영 | 정확도 | todo | sale_results에 encar_total 스냅샷 축적 중 → 표본 쌓이면 LOO 검증 후 반영 |
| PS-09 | 법원 제공 자료 섹션(매각물건명세 요약·감정요항·사진) | 신뢰/증빙 | ✅ 완료 | 재분석 시 spec_remark 채워짐, 상세·리포트 노출 |
| PS-10 | 감정평가서 원본 인앱 뷰어 | 신뢰/증빙 | ✅ 완료 | WebSquare 실측 → `selectAeeWevlInfo.on`으로 KAPA 원본 URL 해석 → 상세 [감정평가서] 버튼(임베드+새탭). 저장·재배포 없이 원본 연결(compliance-review §1) |

## PHASE 3 (유료화) — 진행 방침 (사용자 결정 2026-08-23)
> 디자인 85 달성 후 진입. **결정: 결제 보류·무료로 사용자 확보 우선 + 수익모델=건당 리포트 결제(설계만, 활성화 나중).**
> 준법 게이트: 엔카/케이카 시세 **상용(유료) 노출은 보류**(compliance §2 대체소스 전환·제휴 전). 유료 가치는 **자체 분석·산정·백테스트·리포트**로.

| ID | 제목 | 분류 | 상태 | 비고 |
|---|---|---|---|---|
| PS-04 | 랜딩/가치제안 페이지 | 전환 | ✅ 완료 | `/landing` 무료 유입 퍼널 |
| PS-03 | 상세 상단 한줄 판정(plain language) | 전환 | ✅ 완료 | `service.plain_verdict` → 상세 히어로 초록/앰버/스카이 배너(ok/wait/caution/closed) |
| PS-06 | 관심목록 후보 비교·정렬 | 산출물 | ✅ 완료 | 정렬(여유·D-day·예상·최저) + 예상낙찰가·여유·D-day 컬럼 |
| PS-05 | 매각기일 D-3 알림(앱내) | 유료 훅 | ✅ 완료 | `service.alert_items/alert_count` → 대시보드 임박 패널 + 헤더 벨 배지. 이메일은 회원 후 |
| MON-03 | 건당 리포트 결제 UI(비활성) | 유료 | ✅ 완료 | 리포트에 '보관·구매 (준비 중)' 비활성 pill. 실결제 게이트 유지 |
| BUG-01 | 리포트 탭 누적(상세로 시 새 탭) | 버그 | ✅ 완료 | 리포트/감정평가서를 이름있는 단일 탭(auctionReport/Appraisal) 재사용 + 상세로=탭 닫고 원탭 복귀 |

## 보류(blocked) — 사람·변호사 결정 필요
| ID | 제목 | 사유 |
|---|---|---|
| MON-01 | 실결제 활성화 + 유료 시세 노출 | 준법(엔카 상용) + 개인정보 마스킹 + 전자상거래 표시의무 + 변호사 최종확인. 사용자 결정=현재 **보류** |
| MON-02 | 회원/인증·요금제 활성화 | 개인정보 체크리스트(compliance §3) + 사람 단계별 승인 |
